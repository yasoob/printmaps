# Browser-safe Mapbox public token (must start with "pk.").
# Enables search, road routing, map matching, and travel-time areas.
# Core editing and OpenFreeMap rendering work without it.
VITE_MAPBOX_PUBLIC_ACCESS=
