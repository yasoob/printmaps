# Printmaps.net feature parity

Evidence refreshed: **2026-08-26 (UTC)**.

This comparison treats the current official Printmaps.net product pages as the external source of truth and the current Print Map Studio source/tests as local evidence. It excludes pricing, checkout, licensing, email delivery, and WordPress presentation.

## Official live-source evidence

1. **Map design and print detail** — [Printmaps features](https://www.printmaps.net/features/) says users can set standard/custom millimetre sizes, choose map styles, selectively show map items, change map language and text scale, rotate freely, and tilt to 60°. It describes worldwide cartography at all zoom levels. [Printmaps home](https://www.printmaps.net/) describes 300-DPI or vector output.
2. **Route authoring** — [Printmaps features](https://www.printmaps.net/features/) describes Easy Mode with searched destinations and Expert Mode with map clicks; routes can be straight, arced, or magnetically road-snapped, with adjustable color/width and optional plane, train, car, walking, cycling, and ship icons.
3. **Recorded-route import** — [Printmaps features](https://www.printmaps.net/features/) explicitly supports GPX, KML, and GeoJSON uploads and says multiple recorded routes can be displayed.
4. **POIs and bulk entry** — [Printmaps features](https://www.printmaps.net/features/) describes single POIs placed by dragging or exact spreadsheet location, configurable size/color/icon, custom pins (minimum 100×100 px), and address lists pasted from Excel into a spreadsheet.
5. **Administrative areas** — [Printmaps features](https://www.printmaps.net/features/) describes selecting countries, municipalities, and regions from its database, with fill color/transparency, invert, and a documented two-shape recipe for highlighted borders. The official page was rechecked directly on 2026-08-24.
6. **Layered output** — [Printmaps features](https://www.printmaps.net/features/) says SVG/PSD downloads retain user-content layers and map-feature layers such as hillshading, roads, and buildings. [Printmaps home](https://www.printmaps.net/) advertises PNG, layered PSD, and vector SVG and says every map feature is a separate layer. Both official pages were rechecked live on 2026-08-23.
7. **Elevation profiles** — [Printmaps elevation profile editor](https://www.printmaps.net/elevation-profile-editor/) says users can upload GPX/KML/GeoJSON or draw a route, inspect distance/altitude and walking/cycling estimates, choose a 50–300 mm print width, configure stroke/gradient colors, font, elevation markers, grid and metric/imperial units, and download a layered SVG. The live controls were rechecked on 2026-08-23.
8. **Layer replacement** — [Printmaps features](https://www.printmaps.net/features/) says content layers can be removed, hidden, edited or replaced. The page was rechecked directly on 2026-08-23.

## Current local parity matrix

| Capability | Status | Concrete local evidence | Strict gap / caveat |
|---|---|---|---|
| Physical page, orientation, canonical viewport, movement lock and geolocation | Implemented | Schema 21 stores validated center/zoom/bearing/pitch/lock state in `src/domain/project.ts`; finished manual movement, Fit page/imported/administrative-area framing and explicit browser geolocation converge through the canonical store; focused unit and Chromium Save/Open coverage proves JSON/ZIP/IndexedDB and style recreation retain exact framing | Browser location is requested only on explicit activation and raw browser-position responses are not stored separately; only the resulting normalized map viewport enters the local project. |
| Open map styles, language and feature detail | Implemented / original open collection | Twelve original semantic-token presets across Minimal, Editorial, Dark, Soft, Natural and Playful families in `src/domain/mapStylePresets.ts`; deterministic first-party Vienna thumbnails and same-origin generated OpenFreeMap styles; canonical local/English/German/French/Italian/Spanish/Chinese labels; independent Roads, Buildings, Labels, Water, Parks, Land detail and Transit controls; focused unit and Chromium project-download/layered-SVG coverage | The collection uses OpenFreeMap/OpenMapTiles only and does not copy Printmaps or Mapiful names, code, images, CSS, tiles or proprietary assets. Local translations depend on names present in the open vector tiles. SVG/PDF basemaps remain raster. |
| Native-detail print PNG | Implemented with explicit multi-region guards | `src/map/NativeMapExport.ts` renders each preflight region through a fresh target-pixel MapLibre map at increased zoom, scales basemap and canonical overlay styling into print pixels, and fails closed for pitched or independently placed symbol seams; `src/export/printSizePng.ts` composes safe overlap regions 1:1; unit and browser export tests prove exact 3508×2480 and guarded two-region 7087×591 output. Both in-memory and streamed PNG encoders embed a valid 300-DPI `pHYs` chunk. | This is native target-resolution raster rendering, not a browser-canvas enlargement. Multi-region jobs require 0° pitch and Labels off; single-region jobs retain labels and pitch. |
| Expert route drawing and vertex editing | Partial / substantially expanded | `src/app/components/CanvasWorkspace.tsx`; `src/app/hooks/useDirectionsAuthoring.ts`; `src/services/mapbox/directions.ts`; `src/services/mapbox/mapMatching.ts`; `src/domain/routeProfiles.ts`; `src/map/RouteVertexEditing.ts`; focused store/component/provider coverage plus `tests/e2e/road-routing.spec.ts` and `tests/e2e/map-matching.spec.ts` | Straight drawing, canonical two-anchor projected/Mercator arcs and Mapbox road routing for driving/walking/cycling are implemented. Selected editable LineString routes with 2–100 source points can also be snapped through bounded Mapbox Map Matching; one canonical schema-21 edit stores compact profile/confidence/source-count provenance, remains Undoable and exports offline without replay. Provider geometry is normalized into editable schema-21 `LineString` state with compact provenance, one-step Undo/Redo, autosave/portable persistence and offline native-PNG/layered-SVG/PDF parity; exports never replay the provider request. Explicit address-search results and map clicks both populate road-route waypoints. Six optional travel-mode markers and direct vertex editing remain available; the compact mode markers are not a full pictographic icon catalogue. |
| POI placement, coordinates, appearance and batch entry | Partial / substantially expanded | Point authoring and coordinate controls in `src/app/`; explicit Mapbox address results become one named schema-21 POI with compact `geocoding-v6` provenance; three canonical marker shapes, six semantic symbols and optional 40-character labels in `src/domain/poiMarkers.ts`; bounded tab-separated coordinate and address batches under `src/domain/poiSpreadsheet.ts`; hash-owned PNG/JPEG/SVG marker validation in `src/domain/customMarkerAssets.ts`; keyboard-operable upload/remove controls; live MapLibre, native PNG and layered-SVG custom-marker coverage in `tests/e2e/layer-appearance.spec.ts`, `tests/e2e/search-poi.spec.ts` and `tests/e2e/poi-authoring.spec.ts` | Coordinate rows create up to 300 POIs locally; address mode resolves at most 25 rows sequentially through Mapbox Geocoding v6 and commits only after every lookup succeeds. Both paths are one-step Undoable, portable and exportable offline without replaying provider requests. Manual coordinate edits remove stale provider provenance. PDF currently refuses custom images instead of substituting the wrong marker; semantic symbols remain compact badges. |
| Polygon and administrative-area authoring | Partial / substantially expanded | Freeform Area drawing and post-creation Points/Transform editing under `src/domain/shapeGeometry.ts`, `src/map/ShapeVertexEditing.ts` and `src/map/ShapeTransformEditing.ts`; Austria, Hungary and Slovakia plus all nine exact-source Austrian states; all 23 Vienna municipal districts generated from the City of Vienna OGD WFS source (canonical feature SHA-256 `dc08d94a2581d4dd70d57e5b010827cd211ea851fe6598c9325a603f739ba35f`, CC BY 3.0 AT); live/native/vector export coverage and true-touch Chromium editing coverage | Custom polygons support draggable/keyboard vertices, midpoint insertion, valid deletion and explicit whole-area transform with 44 px coarse-pointer targets. Tyrol retains its separate main and East Tyrol parts as one canonical MultiPolygon; direct multipart vertex editing remains intentionally unavailable. Connected Vienna districts can be selected together and merged into one undoable/exportable shape without internal borders; disconnected selections fail closed. Broad international municipality/region data and arbitrary non-aligned polygon union remain absent. |
| Travel-time areas | Implemented / provider-bounded | `src/services/mapbox/isochrone.ts`, `src/app/hooks/useIsochroneAuthoring.ts`, `src/app/components/IsochronePanel.tsx`, canonical schema-21 shape provenance and focused unit/Chromium coverage; map/search centers, walking/cycling/driving, cancellation, stale-document suppression, one-step Undo/Redo, autosave/portable persistence and offline PNG/SVG/PDF are covered | This application accepts whole 5–60 minute durations; Mapbox Isochrone v1 permits integer contours from 1 through 60 minutes, so the 5-minute minimum is a Studio product constraint while 60 minutes is the provider maximum. The UI explicitly states that 61–180 minute areas are unavailable with this provider rather than implying Printmaps' longer-duration parity. |
| Elevation profiles | Partial / useful integrated workflow | A selected existing or imported route can sample at most 100 points from Open-Meteo's documented 90 m Copernicus DEM GLO-90 elevation endpoint; the profile source can also switch transiently to one local GPX/KML/GeoJSON route without adding a map layer or changing canonical project state. Users can inspect distance/range/ascent/descent in metric or imperial units, see transparent distance-only walking and cycling estimates at stated 5 km/h and 15 km/h assumptions, choose an exact 50–300 mm print width, independently show or hide the curve stroke, use a solid or two-color gradient fill, set curve/fill/marker colors, choose Sans serif, Serif or Monospace from print-safe SVG/PNG/PDF font stacks, set a validated 20–70 font size, show bounded minimum/maximum markers and independent grids, then download matching attributed SVG, PNG and PDF; focused unit and Chromium coverage under `tests/unit/elevation-profile*.test.*` and `tests/e2e/elevation-profile.spec.ts` proves exact physical/vector dimensions and width-scaled PNG pixels. | This is an on-demand external-service workflow, not offline terrain. The public Open-Meteo endpoint is documented for non-commercial use unless a commercial customer endpoint or self-hosting is arranged. Travel estimates intentionally use route distance only and explicitly exclude terrain, stops and conditions. The local markers intentionally label only minimum/maximum elevations rather than every sample. Profile-only files must contain exactly one supported route; the uploaded route remains ephemeral and the selected map layer stays authoritative. |
| GPX/KML/GeoJSON import | Implemented / expanded | Strict parsers in `src/import/`; bounded all-or-nothing batch orchestration in `src/import/mapDataBatch.ts`; drag/drop and multi-select review in `src/app/components/MapDataImportPortals.tsx`; reviewed add batches apply bounded route color/width, POI color/size/shape and shape fill/outline/width before one canonical commit; selected unlocked layers can replace geometry while retaining canonical identity and appearance; focused unit and Chromium coverage | Up to 10 new files / 20 MiB can be reviewed before one Undoable add, with an explicit fit/retain-view choice. One starting style applies to every imported layer of the same type in that batch; per-layer refinement remains available after import. Replace-in-place stays deliberately one file, one feature and the same layer type. |
| Local autosave, open, portable download and direct file actions | Exceeds official core workflow | IndexedDB autosave under `src/storage/`; validated portable JSON plus JSON/ZIP Open support; an inline project title and direct top-bar **Open**, **Save**, **Import** and **Export** actions; focused unit and Chromium project-file/navigation suites | Autosave remains authoritative. **Save** is an explicit download of the current portable project, not an unsaved-state indicator. Share, the File menu and the archive-download action are intentionally removed; Open retains ZIP compatibility for existing portable archives. |
| PNG, layered SVG, PDF | Partial / exceeds in format breadth | `src/export/printSizePng.ts`, `src/export/layeredSvg.ts`, `src/export/printPdf.ts`; the single accessible format-choice flow in `src/app/components/ExportDialogView.tsx`; structural, keyboard, responsive and browser export tests | SVG/PDF keep user overlays as named vectors but embed a **raster basemap**. They do not provide individually editable roads, buildings, labels, water, or hillshade layers. PDF is an extra format; PSD is absent. |
| Layer editing and history | Implemented / exceeds | Canonical store actions support select, hover preview, hide, lock, reorder, duplicate, delete, compatible geometry replacement, and Undo/Redo; replacement preserves identity and appearance and offers fit/retain-view review | Locked and basemap layers cannot be replaced; replacement intentionally changes geometry only. |

## Priority after this refresh

1. Keep Chromium as the sole browser acceptance target unless the project owner explicitly requests another engine; the current 72-test Chromium milestone gate is clean.
2. Continue municipality packaging and the highest-value remaining parity breadth as complete user-visible workflows, without weakening cancellation, canonical persistence, token safety, provenance or open-map fallback.
3. Preserve the completed provider workflows—travel-time Areas, road routing, searched POIs and bounded spreadsheet address geocoding—through final release verification and documentation.

Address-search results now populate canonical road-route waypoints, one-at-a-time named POIs and bounded 25-row address batches. All committed workflows remain provider-independent and exportable offline; manually moving a searched POI removes stale provider provenance.

## Claims we must not make

- A prior browser-preview resample was not native 300-DPI detail. The current PNG path qualifies only because MapLibre rerenders each preflight region at target pixel dimensions and increased source zoom before 1:1 composition.
- Layered SVG/PDF are not fully vector basemaps and do not match Printmaps' claim of separately editable basemap feature layers.
- Layered PSD, CMYK/PDF-X, account sharing, custom-image PDF embedding, a broad international administrative-boundary database, and arbitrary non-aligned boundary union are not implemented. Connected Vienna municipal districts do support bounded merging.
- Elevation profiles are not offline or equivalent to the official editor's complete marker/font/gradient and travel-time controls; profile-only uploads accept exactly one supported route and remain ephemeral, and deployment must respect the selected Open-Meteo service tier and attribution requirements.
