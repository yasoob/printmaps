# Mission findings

## 2026-08-21 — Mission initialized

- Research and implementation plan exist in the workspace.
- No app or git repository existed at mission start.
- User selected a Figma-like three-pane editor with a bottom Atlas-style tool palette.
- The current model has one print frame, not a page collection: no `Page 1` hierarchy belongs in the Layers sidebar. Layer order is changed by dragging left-sidebar handles; Duplicate/Delete live in the compact layer overflow menu. Hover will preview map content without selecting it; click remains the selection action.
- Every UI interaction must receive automated behavioral coverage as it is implemented.
- React Doctor is installed locally and required as a warning-blocking per-slice quality gate.
- Every autonomous run must deliver the latest 1440×900 UI screenshot at `docs/screenshots/latest-desktop.png`.
- UI constraints: minimal, tight, no gradients, no unnecessary shadows.
- A public Mapbox token is available in local untracked configuration; origin filtering may currently reject this deployment.
- Primary unresolved technical risks remain Mapbox display/storage terms, browser memory limits for large exports, and honest layered-export semantics.

## Evidence log

### 2026-08-21 — Vertical slice 1: editor shell and contextual selection

- Added React 19, TypeScript, Vite, TailwindCSS v4, MapLibre GL JS, Vitest and Playwright foundations.
- Added global design tokens in `src/theme.css`; `tests/unit/design-tokens.test.ts` prevents ad-hoc color literals in component CSS and requires Tailwind integration.
- Implemented a Figma-like 44px top bar, 240px Layers sidebar, full map canvas, 272px contextual properties sidebar, and compact bottom tool palette.
- Project properties are shown with no selection. Selecting `Route 01` from the layer tree switches to Layer properties; clicking the map clears selection.
- Live OpenFreeMap tiles render in Chromium/WebKit. Browsers without WebGL 2 receive an in-canvas fallback while the rest of the editor remains usable.
- Verified commands:
  - `npm run typecheck` — pass
  - `npm run lint` — pass
  - `npm run build` — pass (bundle-size warning remains)
  - `npm test -- --run` — 2 tests pass
  - `npm run test:e2e` — 6 tests pass across Chromium, Firefox and WebKit
- Screenshot evidence: `test-results/editor-desktop-editor-swit-334b3-roject-and-layer-properties-chromium/editor-desktop.png`.
- Screenshot review: core density/hierarchy is strong; next slice should dim map outside the print frame, prevent bottom toolbar/attribution overlap, and populate actual route/POI/shape canvas layers.
- The vendored Liberty style at `public/styles/liberty.json` was retrieved from `https://tiles.openfreemap.org/styles/liberty` on 2026-08-21. It retains the 111-layer OpenFreeMap/OpenMapTiles style and its OpenFreeMap/OpenMapTiles/OpenStreetMap attributions. Three highway-shield filters (`highway-shield-non-us`, `highway-shield-us-interstate`, `road_shield_us`) were changed to coalesce missing `ref_length` values before numeric comparison, eliminating MapLibre filter warnings. SHA-256: `76e7ba1ac44425ff9efdb4a73abd78020cf2d07c370518465891304be88a990c`.

### 2026-08-21 — Vertical slice 2: versioned project/layer store and history

- Added a schema-versioned `ProjectDocument` and a Zustand vanilla store as the canonical layer source of truth.
- Layer visibility, lock, rename, opacity, reorder, duplicate and delete now create immutable history entries; undo/redo controls expose valid disabled states and new edits clear redo history.
- Selection moved into the store. Undo/redo reconcile selection against restored documents; deleting the selected layer clears context and moves focus predictably, while duplication selects the new layer.
- Text and numeric property fields use draft transactions and commit once on blur, so one Undo restores the previous visible value.
- Wired layer-list controls and Layer properties to the store. Drag handles reorder layers directly in the sidebar; Duplicate and Delete are available from the compact layer overflow menu rather than four persistent inspector buttons.
- Removed the misleading `Page 1` sidebar hierarchy because the current document has a single print frame. Fit page now issues a real MapLibre fit-bounds command, orientation controls update state, and attribution is an 8px control at the lower map edge.
- Strict TDD evidence: focused tests were observed failing for the redo-selection invariant, non-finite reorder, duplication, property transaction, access-name, tool-state, clean-style and real-canvas deselection behavior before implementation.
- Review remediation: vendored and sanitized the Liberty style, added pre-load MapLibre error fallback, made console warnings/page errors test-visible, added real-canvas deselection E2E, replaced incomplete tree semantics with an accessible list, exposed pressed/current states, labeled property controls, increased contrast, removed component color literals, opted React Doctor out of telemetry, fixed draft-to-click event loss, and added final-delete focus fallback.
- Verified commands:
  - `npm run typecheck` — pass
  - `npm run lint` — pass, zero warnings
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled
  - `npm test -- --run` — 3 files / 24 tests pass
  - `npm run build` — pass (bundle-size warning remains)
  - `npm run test:e2e` — 10 pass / 2 Firefox WebGL-environment skips across 12 Chromium, Firefox and WebKit cases
- Browser evidence:
  - Stable latest desktop UI: `docs/screenshots/latest-desktop.png` (1440×900), showing the selected Route layer, direct drag handles, compact lower attribution, no fake page hierarchy, and no persistent inspector action stack.
  - 390×844 smoke remains covered by Playwright with no body overflow.
- Remaining known issue: the main JavaScript bundle is about 1.15 MB before gzip; MapLibre lazy loading/code splitting remains a later performance task.

## 2026-08-21 — Vertical slice 3: live content overlays and diagnostic recovery

- Added typed point, line and polygon geometry to the canonical project document and rendered real route, POI and shape GeoJSON through a dedicated MapLibre content adapter.
- Sidebar hover previews only the matching visible overlay without selecting it. Canvas feature hits select back into the canonical store; background clicks still clear selection. Visibility and drag/keyboard ordering propagate to actual MapLibre paint and stacking order.
- Selection/hover paint updates keep stable MapLibre sources and layers. The adapter distinguishes synced/deferred/failed states, retries deferred state on idle, contains readiness/paint/rollback/hit-test/destroy failures, and preserves retryable cleanup state.
- Strict TDD evidence:
  - Earlier focused recovery tests failed before implementation for deferred synchronization, style-error preservation, hit-test recovery, exception-safe teardown, and visible fallback recovery.
  - `npm test -- --run tests/unit/map-content-adapter.test.ts -t "clears a previous hit-test diagnostic"` failed 2/2 with the expected stale `data-map-content-error="true"` assertion after successful feature and background hits.
  - The minimal adapter change clears that diagnostic only after a successful query; the same focused command then passed 2/2.
- Fresh verification on the completed working tree:
  - `npm test -- --run` — pass, 5 files / 54 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled.
  - `npm run build` — pass; existing ~1.16 MB pre-gzip bundle warning remains.
  - `npm run test:e2e` — 12 pass / 3 documented Firefox WebGL-environment skips across 15 Chromium, Firefox and WebKit cases.
- Browser evidence:
  - Fresh 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `6ac8c9cdea89f7ed15db3a431aee572f5896cf732e137f5bcdce22af063cb383`.
  - Live browser inspection reported `mapReady=true`, overlay order `route-01,poi-cafe,area-center`, no visible fallback, no stale content-error diagnostic, no body overflow and zero console errors/page errors. Route, POI and polygon overlays were all visible; screenshot review found no material clipping, overlap, gradient, decorative shadow or hierarchy defect.
- Fail-closed review remediation:
  - Review 1 rejected incomplete old-overlay cleanup, ignored post-load renderer errors, and a lifecycle mock that could hide stale handlers. An independent fix agent added RED-first regressions (`expected 'synced' to be 'failed'` and `Unable to find role="status"`), blocked rebuilds until cleanup succeeds, surfaced an actionable renderer fallback, and added per-map/per-adapter tracking.
  - Review 2 found the empty-target cleanup fast path still reachable and pre-side-effect cleanup exceptions still unproved. A second independent fix agent observed RED failures with one stale MapLibre layer and leaked map `0`, then added an explicit cleanup-pending invariant plus one bounded teardown retry. The first retry helper form triggered React Doctor's `effect-needs-cleanup`; inlining the same bounded retries made React Doctor pass with no suppression.
  - Final independent re-review passed with no security concerns or logic errors. Its only non-blocking suggestion is a combined style-error/post-load-error precedence regression; the implementation was verified to preserve the existing style error.
- `.env.local` and tokens remain untracked/uncommitted. `docs/COMPLETE.md` does not exist and the scheduler remains enabled.

### 2026-08-21 — Vertical slice 4: canonical page orientation and versioned migration

- Moved A4 width, height and orientation from transient React state into schema-versioned `ProjectDocument.page`; Project properties and the canvas now read the same canonical values.
- Portrait/Landscape is one immutable history transaction. Undo and redo restore the segmented control, 297×210 / 210×297 mm fields and canvas-frame orientation together.
- Bumped the document schema to version 2 and added an explicit version-1 migration at the store boundary so pre-page documents receive fresh A4 landscape settings instead of failing at `document.page` access.
- Orientation changes canonicalize edge ordering even when a malformed document declares the requested orientation with swapped dimensions; already-canonical repeated clicks remain history no-ops.
- Strict TDD evidence:
  - `npm test -- --run tests/unit/app-selection.test.tsx -t "commits project orientation"` first failed at the post-Undo landscape assertion (`aria-pressed` remained `false`), then passed after the document/store/UI wiring.
  - Review remediation added RED-first regressions for legacy v1 migration (received schema 1/no page instead of schema 2/A4) and same-orientation canonicalization (landscape remained 210×297); both focused commands passed after the minimal fixes.
- Final verification on the reviewed working tree:
  - `npm test -- --run` — pass, 5 files / 56 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled.
  - `npm run build` — pass; existing ~1.16 MB pre-gzip bundle warning remains.
  - `npm run test:e2e` — 12 pass / 3 documented Firefox WebGL-environment skips across 15 Chromium, Firefox and WebKit cases.
- Browser evidence:
  - Stable 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `6ac8c9cdea89f7ed15db3a431aee572f5896cf732e137f5bcdce22af063cb383`.
  - Fresh live-browser interaction changed to Portrait and Undo restored 297×210 landscape with Redo enabled; `data-map-ready=true`, body overflow was 0 and the browser console/page-error buffers were empty.
  - Screenshot review found the live map and all three editor panes readable with no material clipping, toolbar overlap, gradient or decorative shadow.
- Fail-closed review initially rejected missing schema migration and inconsistent same-orientation dimensions. An independent fix agent added the RED-first regressions and minimal remediation; independent re-review then passed with no security concerns or logic errors. The only non-blocking suggestion is a direct page-object isolation regression across both history directions.
- `.env.local` and tokens remain untracked/uncommitted. `docs/COMPLETE.md` does not exist and scheduler job `3a05bbc81515` remains enabled.

### 2026-08-21 — Vertical slice 5: canonical standard page presets

- Added canonical `A4`, `A3`, `Letter`, and `Custom` preset identity to schema-version 3 page settings. Version-1 documents receive A4 defaults; version-2 documents preserve dimensions/orientation and infer a matching standard preset or `Custom`.
- The Project preset control is now controlled by the project store. Choosing A4, A3, or Letter commits dimensions in the current orientation as one history entry; Undo restores the prior preset, dimensions, and visible frame together. Custom remains visibly disabled until transaction-safe dimension editing is implemented.
- The print frame now reads the canonical page dimensions for its aspect ratio and displays the canonical preset in its label rather than hard-coding A4.
- Strict TDD evidence:
  - `npm test -- --run tests/unit/app-selection.test.tsx -t "applies a standard page preset"` first failed at the expected width assertion (`expected 420`, received `297`).
  - After the minimal store/UI/frame implementation, the focused command passed 1/1. The existing orientation regression then caught a missing preset spread in the full suite; preserving the page object made both focused tests and the full suite pass.
- Fresh verification on the completed working tree:
  - `npm test -- --run` — pass, 5 files / 61 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled.
  - `npm run build` — pass; existing ~1.16 MB pre-gzip bundle warning remains.
  - `npm run test:e2e` — 12 pass / 3 documented Firefox WebGL-environment skips across 15 Chromium, Firefox and WebKit cases.
- Browser evidence:
  - Fresh 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `151c3a8038d928fe31161d88556f527bd7fb9c6ad3a4928ee254e3ce87e72be8`.
  - Live browser selection of A3 produced `420×297`, frame label `A3 · Landscape`, CSS aspect ratio `420 / 297`, enabled Undo, a ready live map, zero body overflow, and no JavaScript errors. The exact-size headless screenshot emitted only the previously classified Chromium `GPU stall due to ReadPixels` diagnostic; the interactive browser console had zero errors or warnings.
  - Screenshot review found the three-pane hierarchy, map overlays, A3 frame, page controls, and bottom palette readable with no material clipping, overlap, gradients, or decorative shadows.
- Fail-closed review initially rejected contradictory version-2 migration states and missing migration coverage. An independent fix agent observed the expected RED failure (`Custom` expected, `A4` received) for `210×297` declared landscape, then made standard-preset inference orientation-aware and added four version-2 migration cases covering landscape, portrait, inconsistent, and custom dimensions. Full verification passed after the fix; independent re-review passed with no security concerns or logic errors. Its only non-blocking suggestion is direct A3/Letter migration and preset-redo coverage.
- `.env.local` and tokens remain untracked/uncommitted. `docs/COMPLETE.md` does not exist and scheduler job `3a05bbc81515` remains enabled.

### 2026-08-21 — Vertical slice 6: transaction-safe custom page dimensions (remediated, pending combined review)

- Added controlled width/height drafts that reject empty, non-finite and non-positive values, expose `aria-invalid`, avoid history while typing, commit once on blur, switch the canonical preset to `Custom`, update the print-frame aspect ratio, and remain undoable.
- Added explicit dirty tracking so merely focusing and blurring an untouched dimension does not create history. Re-entering the same numeric value after an actual edit still creates one `Custom` transaction.
- Strict TDD evidence:
  - Initial focused command `npm test -- --run tests/unit/app-selection.test.tsx -t "validates and commits a custom"` failed 2/2 because both page fields were read-only; after implementation it passed 2/2.
  - Review remediation observed RED for re-entering `297` (`Custom` expected, `A4` received), then GREEN after the store no-op invariant was narrowed.
  - Second remediation observed RED for untouched focus/blur (`A4` expected, `Custom` received), then GREEN after per-field dirty tracking.
- Fresh verification on the current uncommitted working tree:
  - `npm test -- --run` — pass, 5 files / 65 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled.
  - `npm run build` — pass; existing ~1.16 MB pre-gzip bundle warning remains.
  - `npm run test:e2e` — 12 pass / 3 documented Firefox WebGL-environment skips across 15 Chromium, Firefox and WebKit cases.
- Browser evidence:
  - Fresh 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `9e10cbda5725247c5bdaaa6cc6eecb9b778b8544a076781b4f9c7f3333f1c8a8`.
  - Live invalid `-1` input set `aria-invalid=true` without enabling Undo; valid `240` committed `Custom`, produced frame aspect ratio `240 / 210`, enabled Undo, kept the live map ready, had zero body overflow, and emitted no browser console/page errors.
  - Fresh untouched focus/Tab verification preserved `A4` with Undo disabled.
  - Screenshot review found the custom frame, map, page controls and three-pane editor readable with no material clipping, overlap, gradient or decorative shadow.
- **Stale-draft blocker remediated:** the final earlier review found that an external page update could move a canonical dimension away from a dirty draft's source and later restore that source, resurrecting the abandoned draft. A focused RED regression reproduced `297 → dirty 240 → canonical 210 → canonical 297` returning `240`. Each dimension is now an independently keyed transaction field, so any canonical dimension/preset change remounts that edit boundary and permanently discards superseded drafts; untouched blur, same-value Custom, invalid reset, Undo and Redo behavior remain covered.
- Fresh combined verification after remediation: typecheck, ESLint, no-telemetry React Doctor and build pass; Vitest 6 files / 84 tests; Playwright 14 pass / 4 runtime Firefox WebGL skips across 18; audit 0 vulnerabilities. Final combined fail-closed re-review remains required before commit.
- `.env.local` and tokens remain untracked/uncommitted. `docs/COMPLETE.md` does not exist and scheduler job `3a05bbc81515` remains enabled.

### 2026-08-21 — Mobile responsiveness and attribution correction (verified, uncommitted with active slice)

- Reproduced the reported mobile defects at 390×844 and 360×800: both sidebars were `display:none`, making layer/project controls unreachable, while expanded attribution measured about 248×26 and overlapped the bottom toolbar; MapLibre's compact padding made its icon appear disproportionate.
- Added mobile Layers and Properties triggers plus modal left/right slide-in drawers, backdrop dismissal, explicit close controls, bounded toolbar sizing and safe-area-aware bottom placement. Drawers move focus inside after their transition, trap Tab, close on Escape/backdrop/selection, restore focus to the correct trigger, and respect reduced-motion preferences. The layer list scrolls independently for long projects.
- Attribution initializes collapsed on mobile by clearing both MapLibre state markers (`open` and `maplibregl-compact-show`), so the first click expands and the second collapses. Its visual glyph is 8×8 inside a 24×24 touch target; expanded text is 9.5px, constrained to the viewport, and translated above the toolbar. The toolbar and attribution derive placement from the same injectable safe-area variable, and `viewport-fit=cover` enables real iOS insets.
- Strict TDD/browser evidence:
  - The initial mobile test failed because attribution remained open, then exposed the real 6px collapsed-details height and MapLibre's 50×18 computed compact box.
  - The first independent review rejected the apparent fix because only `open` was cleared, expanded attribution still overlapped, drawer focus was unmanaged, safe-area geometry diverged, and the Firefox test assumed renderer availability from browser identity. Each issue was reproduced with focused Playwright probes before remediation.
  - The second independent review exposed three deeper lifecycle bugs: MapLibre drag removed only the class marker, topbar controls could escape the modal drawer, and an open drawer retained dialog semantics after resizing to desktop. New RED browser assertions reproduced each before implementation.
  - The third independent review found bidirectional breakpoint attribution drift, mobile Duplicate focus escaping to `BODY`, incomplete top/inline safe-area use, and a stale screenshot checksum. The browser regression was extended for every case before fixing them.
  - Final behavior now synchronizes both attribution markers on drag and in both breakpoint directions; applies native `inert` to every background application region while a drawer is modal; keeps Duplicate focus inside Properties; consumes top, bottom, left and right safe-area values; and clears drawer semantics/focus timers on desktop transition. The drawer lifecycle was extracted to `useMobilePanels` after React Doctor caught an oversized `App` component.
  - The final E2E case detects actual map-ready/fallback state; verifies pristine, post-drag, desktop-to-mobile and mobile-to-desktop attribution cycles; expanded geometry with all four simulated safe-area insets; a 24px target with 8px glyph; modal focus entry/Tab containment/Escape/backdrop/selection/Duplicate behavior; 320/390 overflow; resize cleanup; and all three browser engines.
- Fresh verification on the combined current working tree:
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm test -- --run` — pass, 5 files / 65 tests.
  - `npm run build` — pass; existing bundle-size warning remains.
  - `npm run test:e2e` — 12 pass / 3 documented Firefox WebGL-environment skips across 15 cases.
  - `npm audit --omit=dev` — 0 vulnerabilities.
- Browser evidence: `docs/screenshots/latest-mobile.png`, `docs/screenshots/mobile/attribution-open.png`, `docs/screenshots/mobile/layers-open.png`, and `docs/screenshots/mobile/properties-open.png`. Measured expanded attribution is 239×28 at y=754–782 versus toolbar y=786–830; the button is 24×24 with an 8×8 glyph. The Tailscale deployment at `https://ubuntu-2gb-hil-1-1.tail7787.ts.net:8443/` serves the corrected source with a clean browser console.
- These edits share `App.tsx`, `MapCanvas.tsx`, styles and tests with the custom-dimension and export corrections, so the combined tree awaits one final fail-closed review before commit.

### 2026-08-21 — Export no-op correction (verified, uncommitted with active slices)

- Reproduced the user report at both 1440×900 and 390×844: clicking `Export` produced no dialog, no download event and no focus change because the button had no handler on either layout.
- Added an accessible `Export map` modal with initial focus, Tab containment, Escape/backdrop/Cancel/close behavior, trigger-focus restoration, busy state and a visible not-ready/error path. The wording explicitly identifies this as a current print-frame preview rather than claiming the later 300-dpi/tiled export gate is complete.
- Added a real browser-only PNG path. MapLibre now preserves its drawing buffer, registers the exporter only after the first rendered idle frame, crops the live canvas to the visible print frame, and downloads `<project-id>.png`. The crop includes the rendered basemap plus route/POI/shape layers and writes the style's visible source attribution into the PNG.
- Strict TDD/browser evidence:
  - The new Playwright case first failed because no `Export map` dialog existed.
  - The attribution unit case then failed because the cropped PNG omitted source attribution.
  - Export-only fail-closed review rejected the first implementation for clipped/outside crop math, busy-modal focus loss, stale exporter availability after renderer errors, unbounded/missing attribution, object-URL cleanup, and a non-awaited browser assertion. Focused RED tests reproduced every defect before remediation.
  - The corrected exporter intersects frame/canvas bounds, rejects zero/outside/missing-attribution states, bounds attribution text, normalizes rendering/encoding errors, sanitizes filenames, and revokes object URLs even when download initiation throws. Map errors now withdraw readiness and exporter capability; late callback replacement and StrictMode cleanup are covered. Busy export focuses the modal container and keeps Escape/Tab contained.
  - Chromium and WebKit now download non-empty files from desktop, 390×844 mobile, and 390×520 short-mobile viewports; Firefox activates the same case automatically when the runtime supplies WebGL 2 instead of being skipped by browser name. The browser test awaits file size, verifies PNG signature/IHDR dimensions against frame×DPR, decodes opaque/nonblank pixels, and checks attribution-strip pixel evidence.
  - Actual PNGs were visually inspected and contain the Liberty basemap, Route 01, Coffee stop, City center and an embedded attribution strip rather than blank/transparent pixels.
  - Direct downloads through the deployed Tailscale HTTPS endpoint produced the same 687×486 and 335×237 files and checksums as the Chromium regression path, with clean browser console/page-error capture.
  - Desktop and 390px modal screenshots have zero body overflow, focus `Download PNG`, remain inside safe-area padding and emit no console/page errors.
- Fresh combined-tree verification: typecheck pass; ESLint pass with zero warnings; no-telemetry React Doctor pass/no issues; Vitest 6 files / 84 tests; production build pass with the existing bundle-size warning; Playwright 14 pass / 4 runtime Firefox WebGL-environment skips across 18; `npm audit --omit=dev` reports 0 vulnerabilities.
- Evidence: `docs/screenshots/export-desktop.png`, `docs/screenshots/export-mobile.png`, `docs/screenshots/export-preview-desktop.png` (687×486, SHA-256 `30fc9413f1c80e9cd2b91a0cb6fde07c66ebbd19ce7cc0e593b22e6b68affb48`), `docs/screenshots/export-preview-mobile.png` (335×237, SHA-256 `655102e9cfbed4e08ccbfb6a124d82fbd220581007e9747fce0fd2705cd3443a`), and `docs/screenshots/export-preview-custom-100x300.png` (223×668, approximately 1:3, SHA-256 `29be3ff44544edb77188417e80d15272a2d9a3c494d3abd2dce529adff2267c9`).
- Scope remains honest: high-resolution tiled PNG, PDF and layered SVG are still future mission stages. This correction makes the existing top-bar control visibly and functionally useful without claiming those formats are finished.

### 2026-08-21 — Combined fail-closed review remediation (pending re-review)

- The first combined review reported no security concerns but rejected six logic/evidence areas. Each was reproduced with a focused RED assertion before correction:
  1. Mobile Delete could leave `BODY` focused while Properties remained modal. Delete now restores focus to the Project heading inside the active Properties dialog (or the appropriate desktop list target), and Chromium verifies focus plus Escape closure.
  2. Orientation-inconsistent Custom dimensions could distort the frame. Custom edits now derive orientation from canonical width/height, and container-query-unit sizing fits the exact ratio inside both axes. A real 100×300 page renders and exports at approximately 1:3 (`223×668`).
  3. Degenerate frame crops could produce unusable 687×2 previews. Export now rejects output below 120×48 with an explicit useful-content/attribution error.
  4. Content synchronization failures could retain an exporter. Failed/deferred synchronization now clears `data-map-ready`, invalidates exporter capability, and blocks idle republishing until content is synchronized.
  5. The 8px attribution glyph repeated as a 3×3 tile. CSS now sets `background-repeat:no-repeat` and centered positioning; computed-style browser assertions and refreshed screenshots verify one glyph in the 24px target.
  6. Evidence was too weak. Busy export now exercises forward and reverse Tab while no controls are enabled; PNG tests verify an exact opaque surface-colored attribution strip plus non-surface text pixels rather than generic bottom-row dominance.
- Fresh final gate after these corrections: typecheck pass; ESLint zero warnings; React Doctor no issues with telemetry disabled; Vitest 6 files / 84 tests; build pass with the existing size warning; Playwright 14 pass / 4 runtime Firefox WebGL skips; audit 0 vulnerabilities; diff/security scan clean; live HTTPS 200.
- Refreshed evidence: desktop SHA-256 `9e10cbda5725247c5bdaaa6cc6eecb9b778b8544a076781b4f9c7f3333f1c8a8`, mobile SHA-256 `224dea1d3944499bc38525d0f282d64059dd2a677c4d82a37bf1eec3916b4d21`; Properties and Export screenshots now show the current disabled Browser preview/required-attribution controls.

### 2026-08-21 — Vertical slice 7: portable versioned project JSON download (verified)

- Wired the existing desktop **Save** action to download the current canonical `ProjectDocument` as human-readable JSON with the portable `.printmap.json` suffix. The filename is derived from a sanitized project ID, and the object URL is revoked after download initiation.
- The saved artifact reflects current canonical state rather than startup defaults: after changing A4 to Portrait, the browser download contains schema version 3, `210×297` portrait page settings, project identity, and all four ordered layers.
- Strict TDD evidence:
  - `npm run test:e2e -- --project=chromium --grep "Save downloads the current project"` first failed after 30 seconds waiting for a download because Save had no handler.
  - After the minimal download implementation, the same focused command passed 1/1 in 5.5 seconds.
- Fresh verification on the current working tree:
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues at warning-blocking severity; telemetry disabled.
  - `npm test -- --run` — pass, 6 files / 84 tests.
  - `npm run build` — pass; existing ~1.17 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 17 pass / 4 documented Firefox WebGL-environment skips across 21 cases; the new portable-save flow passes in Chromium, Firefox, and WebKit.
- Browser evidence:
  - Refreshed exact 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `0d7a2c44d57c9b9546484b83a2d6fc7a237dd999b65fb175a6a123f7e1c81216`.
  - Live browser interaction changed the page to `210×297` Portrait and clicked Save with `data-map-ready=true`, zero body overflow, zero gradients, zero computed box shadows, and no console errors or warnings.
  - Screenshot review found no material clipping, overlap, hierarchy, frame, or legibility defect; the Save action remains visible in the flat top bar.
- Independent fail-closed review passed with no security concerns or logic errors. Its non-blocking suggestion is dedicated filename-sanitization edge coverage plus full-document equality; the current cross-browser test already proves canonical schema, identity, page state, and complete ordered layer IDs.
- Scope remains bounded: this slice adds portable JSON download only. Portable project upload/open, schema validation/error UX, IndexedDB autosave, and ZIP support remain unresolved.
- `.env.local` and tokens remain untracked/uncommitted. `docs/COMPLETE.md` does not exist and scheduler job `3a05bbc81515` remains enabled.

### 2026-08-21 — Vertical slice 8: validated portable project open (verified)

- Added an explicit **Open** action for portable `.printmap.json` files. The browser validates the suffix and rejects files above 10 MiB before reading them, then parses untrusted JSON through a normalizing validator rather than casting it into the project store.
- Current schema-3 projects and migrated schema-1/2 documents pass through the same store boundary. Validation covers the root/schema, project identity, canonical standard-page dimensions, page orientation, layer count/IDs/types/state, geometry/type compatibility, coordinate structure/ranges/ring closure, opacity, and aggregate coordinate limits. Imported objects are rebuilt from known fields so unknown input does not become canonical state.
- A successful open replaces the canonical document, clears selection/hover preview, and establishes a fresh history root with Undo and Redo disabled. Invalid JSON, renamed files, unsupported/contradictory documents, and oversized files leave the current project and history untouched.
- The hidden file input is cleared after every attempt, so the same file can be retried. Success uses an accessible status; failures use an accessible alert with corrective text. Both paths restore focus to **Open** after the chooser closes.
- Strict TDD evidence:
  - The store regression first failed with `openDocument is not a function`, then passed after the history-reset action was added.
  - The parser test first failed because `projectFile.ts` did not exist; the invalid-fixture matrix then failed 8/8 against the unsafe cast-only parser before structural validation was implemented.
  - The nominal Playwright flow first timed out waiting for an **Open** file chooser. The invalid/retry flow then failed with an unhandled `ProjectFileError` and no alert before guarded error handling and focus restoration were added.
  - Geometry/type contradiction and noncanonical standard-page regressions each failed before their focused validation rules were added.
- Fresh verification on the reviewed working tree:
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled. The first run rejected the enlarged `App`; extracting a dedicated project-open component restored the clean gate.
  - `npm test -- --run` — pass, 7 files / 96 tests.
  - `npm run build` — pass; existing ~1.18 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 23 pass / 4 documented Firefox WebGL-environment skips across 27 Chromium, Firefox, and WebKit cases. Save, valid Open, invalid retention/retry, and focus/history behavior pass in all three engines.
- Browser and process evidence:
  - Fresh 1440×900 Chromium screenshot: `docs/screenshots/latest-desktop.png`, SHA-256 `52a5342e40bd762bafa21800a36598a8294f5dd9a723e2bb7f9a74e88fcd10d5`.
  - Exact-viewport inspection reported `data-map-ready=true`, overlay order `route-01,poi-cafe,area-center`, zero body overflow, zero gradients, zero computed box shadows, and no console warnings/errors. Screenshot review found the Open/Save/Share/Export actions, three-pane hierarchy, frame, and toolbar legible with no material clipping or overlap.
  - Seven stale duplicate workspace Vite process groups were stopped; the enabled `print-map-studio.service` preview remained healthy on `127.0.0.1:4178` with HTTP 200 and was the only remaining workspace Vite process.
- Independent fail-closed review passed with no security concerns or logic errors. Non-blocking suggestions were explicit same-file/boundary/legacy/limit coverage, serializing overlapping asynchronous opens, and exposing Open through a future mobile overflow action.
- Scope remains bounded: IndexedDB autosave/recovery and ZIP project packaging are still unresolved. `docs/COMPLETE.md` does not exist.

### 2026-08-22 — Vertical slice 9: IndexedDB autosave and explicit recovery (verification complete, pending final review)

- Added a versioned IndexedDB draft repository that validates every stored document through the portable-project parser before exposing it to application state. Draft writes are debounced and serialized; teardown cancels pending work and prevents queued writes after unmount.
- The editor now shows live autosave state, keeps editing available when storage is unavailable or full, and provides an actionable portable-Save fallback for quota and generic storage errors.
- Startup never replaces the current project silently. A valid local draft opens a focus-trapped recovery decision; a corrupt or unsupported record remains contained until explicitly discarded. Recovering establishes a fresh history root, while successful discard restores focus and permits pending canonical edits to save.
- Interaction coverage exercises recovery/discard, Escape refusal, forward/reverse focus containment, focus restoration, corrupt records, discard failure, quota failure, edits made while storage is loading, queued-save teardown, mobile safe-area geometry, body overflow, reload persistence, and all three browser engines.
- Fresh serial verification on the staged integration tree:
  - `npm test -- --run tests/unit/autosave.test.ts tests/unit/app-selection.test.tsx` — pass, 2 files / 40 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm test -- --run` — pass, 8 files / 108 tests.
  - `npm run build` — pass; existing ~1.19 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 29 pass / 4 documented Firefox WebGL-environment skips across 33 Chromium, Firefox, and WebKit cases.
- Browser and screenshot evidence:
  - Stable 1440×900 editor captured by the fresh Chromium gate: `docs/screenshots/latest-desktop.png`, SHA-256 `d07ce14eb5cfcf9aa4e71cc7c0be7c03f81b4ac26e3a1c23d8049449bd9fa40e`.
  - Recovery dialog: `docs/screenshots/autosave-recovery-desktop.png` (1440×900), SHA-256 `bdb08942bb5915ba08b9afb7da158b74f36e517a9e436ccbb43290e29bb64305`.
  - Mobile recovery dialog: `docs/screenshots/autosave-recovery-mobile.png` (390×844), SHA-256 `850ed42fe68dfaf8a40be57936ca65a60fb089c1a6df3269967b7b5fc8f6c186`.
  - Live preview at `127.0.0.1:4178` returned HTTP 200, changed to Portrait, reported `All changes saved locally`, presented the recovery dialog after reload, and had zero console warnings/errors or page errors.
  - Visual review found the compact footer status and centered recovery dialog readable with no material clipping, overlap, gradient, decorative shadow, or hierarchy defect.
- `docs/COMPLETE.md` does not exist; the mission completion gate remains open.

### 2026-08-22 — Wave 1 audit and bounded GeoJSON UI integration (verified)

- Audited every registered worktree, branch status, live Hermes/test process and preview artifact before acting. No duplicate implementation worker or test server was launched; the enabled preview remained the sole workspace Vite service on `127.0.0.1:4178`.
- Confirmed Wave 1 had already been integrated through verified branch `agent/unicorn-wave1` and merge `4a9ef55`. The three original Wave 1 branches are superseded by that integrated/refactored implementation and are not eligible for a second merge; each is 30 commits behind main and its worktree contains an untracked `node_modules` artifact. Their scoped diffs did not touch integration-owner hotspots.
- Integrated the clean roadmap documentation commit `98aec9d` as `fc229d0` before starting application edits.
- Added a desktop **Import** chooser for `.geojson` files. A valid bounded Feature/FeatureCollection becomes editable route, POI and shape layers, is inserted above the basemap, selects the first imported layer, preserves unique IDs, and commits the entire batch as one Undo step.
- Empty/invalid/oversized or wrongly named files remain contained, report an accessible corrective error, leave history unchanged and reset the chooser for a focused retry.
- Strict TDD evidence:
  - Store batch import first failed because `importLayers` did not exist, then passed after the one-transaction insertion action.
  - The browser flow first timed out because no **Import** control existed, then passed after the chooser and parser wiring.
  - Empty GeoJSON first produced no alert, then passed after explicit no-feature rejection.
  - A colliding imported ID first produced duplicate `route-01` IDs, then passed after deterministic suffixing.
  - Review remediation reproduced a slow import landing in a project opened later, stale success after Open, and an enabled second Import while reading. A document epoch now makes Open/autosave recovery win, remounts stale status, and blocks concurrent import reads. The first pending-state focus attempt then failed the existing browser focus assertion before focus was deferred until the re-enabled button rendered.
- Fresh serial verification: focused Wave 1 tests pass (17 export-preflight, 7 print-scene, 23 GeoJSON parser); typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 23 files / 196 tests; production build passes with the known bundle-size warning; audit reports 0 vulnerabilities; Playwright passes 38 with 4 runtime Firefox WebGL skips across 42 Chromium, Firefox and WebKit cases.
- Browser evidence: fresh 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `6559e34c73f7e433c72b8b2bb0a13ef7d6007f93e34f44e395f9a008cc7fa056`. The new Import action fits the flat top bar without clipping or overlap; the editor screenshot shows no visible error, gradient or decorative shadow. Live preview inspection reported a ready map, zero body overflow, zero computed gradients/shadows and an empty console/page-error buffer; the preview returned HTTP 200.
- A transient OpenFreeMap glyph-origin fetch failure appeared in an earlier temporary Playwright server output while the suite continued successfully; external tile/font availability remains a tracked third-party risk. The first fail-closed review rejected asynchronous import/open races; remediation was verified and the final independent re-review passed with no security concerns or logic errors. Non-blocking follow-ups are clearing a prior status as soon as a new read starts and adding a direct stale-epoch store regression in addition to the app-level race coverage.

### 2026-08-22 — Wave 2 foundations and bounded GPX/KML UI integration (verified)

- Recovered three interrupted Wave 2 worktrees without resetting their partial artifacts. Each worker stayed within its exclusive leaf-module/test scope, avoided manifests and integration hotspots, committed verified work, removed its temporary `node_modules` symlink, and left a clean worktree.
- Integrated the independently reviewed foundations one at a time:
  - raster compositor `6a2177d` via merge `e443ef3`: sequential tile/strip execution, exact overlap crop/destination mapping, monotonic progress, cancellation checkpoints and deterministic release/error behavior (10 focused tests);
  - Mapbox provider core `f44784c` via merge `8d22bbe`: provider-neutral contracts, strict public-token and HTTPS API-host validation, terms/storage-use boundary, abort-race containment, and actionable offline/401/403/422/429/other failure normalization (26 focused tests);
  - GPX/KML import `8803cd3` plus review fix `27c1427` via merge `1bf44d9`: bounded namespace-aware XML parsing, DTD/entity rejection, deterministic canonical layers, points/lines/polygons with KML holes, and strict decimal lexical validation (39 focused tests).
- The first GPX/KML review correctly rejected JavaScript-only numeric literals such as `0x10`; a RED matrix failed 9 cases before complete decimal lexical validation made 39 tests pass. Final re-review found no remaining security, logic or scope blocker. Independent raster and Mapbox reviews also passed.
- Wired one bounded integration surface: the existing **Import** chooser now accepts `.geojson`, `.gpx`, and `.kml`, dispatches to the strict local parser, inserts each file as one undoable editable layer batch, selects the first imported layer, restores focus, and reports format-correct accessible status/errors. Empty GPX, malformed KML, wrong suffix, unchanged history and retry are covered across Chromium, Firefox and WebKit.
- Strict UI TDD evidence: the GPX flow first failed with no `Café Central` heading; the KML flow first failed with no `Café point` heading; an existing empty-GeoJSON regression then failed on the broadened error wording before the exact corrective message was restored. The first UI review rejected a shared zero-layer fallback and missing invalid-format interaction coverage; isolating GeoJSON's empty check and adding the invalid/retry flow produced a clean final re-review (24/24 project-file browser cases).
- Final serial verification on the completed tree:
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm test -- --run` — pass, 26 files / 271 tests.
  - `npm run build` — pass; the known ~1.21 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 47 pass / 4 documented Firefox WebGL-environment skips across 51 Chromium, Firefox and WebKit cases.
- Fresh browser evidence: `docs/screenshots/latest-desktop.png` is a current 1440×900 Chromium capture, SHA-256 `6559e34c73f7e433c72b8b2bb0a13ef7d6007f93e34f44e395f9a008cc7fa056`. Live inspection reported a ready map, overlay order `route-01,poi-cafe,area-center`, GPX/KML accept filters, zero body overflow, zero computed gradients/shadows and an empty console/error buffer. Visual review found no clipping, topbar crowding, overlap or hierarchy defect; the enabled preview remained healthy on `127.0.0.1:4178` with HTTP 200.
- `docs/COMPLETE.md` does not exist. Core authoring, portable ZIP, high-resolution PNG/PDF/layered-SVG UI and the Mapbox renderer/storage decision still keep the mission gate open.

### 2026-08-22 — Export preflight and print-size PNG composition (verified)

- Re-audited the clean main worktree, every registered agent worktree/branch, live Hermes/test processes, artifacts and preview service before editing. Wave 1 and Wave 2 foundations were already integrated; no eligible branch or duplicate worker remained, and the enabled preview stayed healthy on `127.0.0.1:4178`.
- The Export dialog now runs the reviewed preflight planner against the canonical physical page before capture. It reports exact 300-DPI-equivalent placement pixel targets and peak memory, disables download for unsafe jobs before canvas allocation, and gives a corrective page-size action.
- Wired the reviewed raster compositor into PNG generation. It follows the preflight tile/strip plan, calculates tile progress, releases tile/output allocations on failure or cancellation, and preserves exact output dimensions. The A4 300-DPI-equivalent placement target is `3508×2480`; the `100×300 mm` target is `1181×3543`.
- Scope remains explicit and honest: this output resamples the current browser render to those pixel dimensions; it does not invent native map detail, and PNG physical-resolution metadata is not embedded. Native high-resolution map tile rendering, PDF and layered SVG remain unresolved completion-gate work.
- Strict TDD evidence: dialog preflight summary, unsafe allocation guidance, compositor progress, cancellation allocation release and busy-dialog cancellation each failed for the expected missing behavior before their minimal implementation. The focused Chromium export initially exposed the old 10-pixel attribution probe assumption after resampling; the structural check now inspects the scaled attribution band and passes.
- The first fail-closed review rejected cancellation during asynchronous PNG encoding and unsafe-preflight focus remaining outside the modal. A separate fix agent observed RED failures for a delayed `toBlob` callback resolving after abort and for disabled Download receiving no focus; encoding now rechecks cancellation before resolving, releases the output, and unsafe jobs initially focus the enabled Cancel action. Both focused files pass 8/8.
- The second fail-closed review rejected language that called the result a 300-DPI PNG even though fresh files have the exact target pixel dimensions but no embedded physical-resolution metadata. A RED app interaction assertion required both “300 DPI pixel target” and an explicit metadata disclaimer; the dialog now presents the dimensions as targets for 300-DPI placement, states that PNG physical-resolution metadata is not embedded, and retains the resampling/no-new-detail limitation. Matching E2E text assertions cover A4 and custom targets. Focused app-export tests pass 4/4; typecheck and ESLint also pass.
- The final fail-closed review found one remaining blocker: tile render/write/progress callbacks were synchronous, so the microtask chain could finish every tile before the browser painted progress or dispatched a Cancel click. A new interaction regression first failed because `Cancel export` never rendered while the two-tile job was active. Composition now yields to the browser task queue after each non-final tile; the same test observes `1/2 tiles (50%)`, clicks Cancel, resumes the queued task, receives `Export cancelled`, proves only one tile was drawn, and proves encoding/download never started.
- A second RED interaction regression exposed an output-canvas allocation that survived when browser download initiation threw. The composed surface is now released in a `finally` boundary whether download startup succeeds or fails; the regression proves every created tile/output surface and the captured source are reset while the dialog reports the original failure.
- The next fail-closed review rejected four preview-capture failure paths that left an allocated source canvas alive until garbage collection: unavailable drawing context, render exception, null encoding result, and synchronous encoding exception. An independent fix agent added all four assertions first; they failed with the output still at `600×400`, then passed after every rejected capture path explicitly released the surface to `0×0`.
- Fresh serial verification after all corrections: focused export coverage passes 5 files / 50 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 27 files / 281 tests; production build passes with the known ~1.22 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; the final Playwright rerun passes 47 with 4 documented Firefox WebGL-runtime skips across 51 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `0c2057ae314b8bd7a5a60dae7346d5a00467969a4a2d74284d488696249cb8f6`; timestamped preflight dialog evidence remains `docs/screenshots/export-preflight-20260822.png`. The live preview is HTTP 200 with a ready map and empty console/error buffers. Screenshot review found no clipping, overlap, blank/fallback map, gradient, decorative shadow, hierarchy or readability defect.
- One intermediate Playwright run encountered a transient Chromium Import-trigger focus timeout during an OpenFreeMap glyph-origin outage. The exact Chromium case passed immediately in isolation and the complete serial suite then passed cleanly; external glyph availability and this isolated focus flake remain tracked rather than being hidden as application completion.
- Final independent fail-closed re-review passed with no security concerns, logic errors, or blocking suggestions. It independently reran the 50 focused tests plus typecheck and lint. `docs/COMPLETE.md` does not exist because the wider mission gate remains open.

### 2026-08-22 — Layered SVG export integration (verification complete, pending final review)

- Added a second explicit export action for layered SVG. It embeds a clean raster-only capture of the current basemap, keeps route, POI and shape content as ordered named vector groups, preserves exact physical page dimensions/viewBox and adds attribution as its own named vector group.
- The map adapter temporarily hides only Studio overlay layers for the basemap capture, waits for a rendered frame, and restores them even when capture fails. Failed restoration withdraws exporter readiness and reports an actionable renderer recovery state rather than leaving hidden content or a stale capability.
- Geographic coordinates use the live MapLibre screen projection normalized to the intersected print frame, so vector groups align with the captured map under the current camera. The basemap capture omits its raster attribution strip because the SVG scene supplies the required vector attribution group.
- Scope remains explicit in the dialog and SVG metadata: the basemap is raster; user overlays remain vectors. Native high-resolution tile detail and PDF remain unresolved.
- Strict TDD evidence:
  - The app interaction first failed because `Download layered SVG` did not exist, then passed with a mocked embedded PNG and named route/POI/shape groups.
  - The real Chromium flow first timed out waiting for a download because the live capture lacked print-frame projection; it passed after projection and basemap isolation wiring.
  - Focused regressions first failed for normalized frame projection, overlay hide/restore, and duplicate raster attribution before their minimal implementations.
  - The first fail-closed review rejected a partial initial hide failure that could leave a stale exporter, plus unbounded render waits that ignored Cancel and renderer errors. RED regressions reproduced stale readiness after isolation failure, an undefined capture AbortSignal, a pending wait that ignored abort, an unbounded timeout path, and a renderer-error wait; all pass after the bounded remediation.
- Final serial verification after review remediation: typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 27 files / 291 tests; production build passes with the known ~1.24 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 49 with 5 runtime Firefox WebGL skips across 54 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `a62a7e0f75592236bdedb92ead643e8d0f14a46176d91c8a935ef7385edd15a2`. The live preview returned HTTP 200 with a ready map, zero body overflow, zero computed gradients/shadows, and no application console/page errors; Chromium emitted only its previously classified headless `ReadPixels` GPU diagnostics. Visual review found the three export actions readable with no clipping, overlap, fallback map, decorative shadow or hierarchy defect. The generated Chromium SVG is 696,974 bytes (SHA-256 `cf20ba9d3a4026bf539cd4a6ac9f89fbbd41f3ed461951e1b46cbbdfcd1c3dc8`); its embedded 687×486 raster contains zero exact route/POI/shape paint-color pixels, and a rendered artifact inspection found the separate vectors aligned without visible duplication or clipping.
- A pre-remediation WebKit gate logged a transient OpenFreeMap Noto Sans Italic glyph-origin fetch failure while all affected browser cases passed; external font/tile availability remains a tracked third-party risk rather than being hidden as application completion.
- The first post-remediation browser gate hit an isolated WebKit Import-trigger focus timeout in the pre-existing invalid-GPX/KML flow. The exact WebKit case passed immediately in isolation and the complete serial suite then passed; this remains tracked with the earlier third-party/focus flakes rather than being attributed to the export change.
- Final independent fail-closed re-review passed with no security concerns, logic errors, or suggestions. It independently reran the 60 focused remediation/export tests and confirmed bounded abort/error/timeout handling, fail-closed capability invalidation, surface cleanup, PNG preservation, safe downloads, and the raster-basemap/vector-overlay structure. `docs/COMPLETE.md` does not exist because the wider mission gate remains open.

### 2026-08-22 — Exact-page layered PDF export (verified)

- Added a fourth explicit Export action for PDF. It captures the same raster-only basemap used by layered SVG, places it on the exact physical page, and draws route, POI and shape geometry as separate named vector optional-content groups with a required vector attribution layer.
- The default A4 landscape PDF has an exact `841.889764 × 595.275591 pt` MediaBox/CropBox. The generated browser artifact is one PDF 1.7 page with a DCT/JPEG raster XObject, named OCGs, per-layer opacity ExtGStates, no JavaScript and no encryption.
- Layer semantics match the editor: vector commands paint bottom-to-top so the first sidebar layer remains visually topmost; the PDF layer panel lists content top-to-bottom; hidden vector content remains embedded but starts OFF; basemap and vector opacity are preserved.
- The dialog states the export contract honestly: PDF and layered SVG retain vector user overlays over a raster basemap; native high-resolution tile detail remains unresolved. PDF export supports the existing busy focus trap, cancellation, renderer-error withdrawal, object-URL cleanup and source-surface release.
- Strict TDD evidence:
  - The first focused app test failed because `Download PDF` did not exist, then passed with exact page-box, raster image, named OCG and attribution assertions.
  - A rendered artifact exposed a UTF-16 BOM as visible attribution garbage. A RED assertion required WinAnsi-safe middle-dot/copyright escapes; the corrected artifact starts cleanly with `OpenFreeMap`.
  - Review remediation tests failed first for hidden layers omitting their vector commands, missing basemap opacity, and route/POI/shape stacking in document order; all pass after embedding hidden commands behind OFF OCG state, adding the basemap ExtGState, and reversing paint order while preserving sidebar OCG order.
- Final serial verification on the reviewed tree:
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm test -- --run` — pass, 28 files / 296 tests.
  - `npm run build` — pass; the known ~1.24 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 51 pass / 6 documented Firefox WebGL-runtime skips across 57 Chromium, Firefox and WebKit cases. PDF download passes in Chromium and WebKit and is runtime-skipped with the other renderer-dependent exports in Firefox.
- Structural and browser evidence:
  - `qpdf --check` passes for fresh Chromium and WebKit PDFs. `pdfinfo` reports one A4 landscape page, PDF 1.7, exact page size, no JavaScript and no encryption.
  - Current Chromium PDF: 117,582 bytes, SHA-256 `b769fa13eb761e40a6380322905afe612d9becbcc12f94b87ed0d06f7d0a955b`.
  - Rendered PDF evidence: `docs/screenshots/pdf-export-20260822.png`, SHA-256 `ab901118abc70884be2082d75380cebc5ab19117f8f4a90aabb36fe7cdd604a3`. Visual review confirmed the raster map, polygon, POI, route and clean attribution are aligned, unclipped and not duplicated.
  - Fresh 1440×900 editor: `docs/screenshots/latest-desktop.png`, SHA-256 `0c2057ae314b8bd7a5a60dae7346d5a00467969a4a2d74284d488696249cb8f6`. Live preview remained map-ready with zero body overflow, gradients, computed box shadows, console errors or page errors and returned HTTP 200 on `127.0.0.1:4178`.
- The first two fail-closed reviews rejected hidden-content/opacity semantics and then layer stacking/order. Each blocker received a focused RED regression and minimal fix. A fresh final reviewer passed with no security concerns, logic errors or suggestions after independently rerunning 13 focused tests, typecheck and lint.
- One full-suite run logged a transient OpenFreeMap Noto Sans glyph-origin outage while every case still passed. External glyph/tile availability remains a tracked third-party risk. `docs/COMPLETE.md` does not exist because the wider mission gate remains open.

### 2026-08-22 — Straight-segment route authoring (verified)

- The Route tool now accepts map clicks into a visible noncanonical draft with point markers, a straight line, live point count, disabled-until-valid Finish, and explicit Cancel. Finish and Cancel return focus to Select; changing tools discards the draft.
- Finish validates coordinate count/ranges, creates the lowest available canonical `Route NN` ID/name, inserts the route above the basemap, selects it, and records the complete route as one Undo step. Undo removes it and Redo restores it.
- While Route is active, geographic map clicks bypass ordinary feature hit testing. The callback is removed immediately on finish/cancel/tool change, so normal canvas selection and background clearing resume.
- Draft state is excluded from autosave and print/export. Opening or recovering another project invalidates the draft by document epoch, including the stale-source restoration case when Route is reactivated later.
- The complete browser flow creates a route, verifies draft/canonical map synchronization, Undo/Redo, and downloads a layered SVG containing the new route as a named vector group.
- Strict TDD evidence:
  - Store creation first failed because `createRoute` was absent; canonical naming then failed with `route-02` when `route-01` was available, and invalid one-point/non-finite/out-of-range inputs initially created layers before focused fixes.
  - The app flow first failed with no Finish action, then failed before temporary line/point feedback, Cancel behavior, post-Finish focus, project-open invalidation, tool-switch cleanup, and stale-draft nonresurrection were added.
  - The map lifecycle regression first failed because authoring coordinates never reached the application; the minimal routing path now consumes clicks before hit testing only while the authoring callback exists.
  - A full-suite regression exposed that remounting the canvas workspace on document open broke delayed autosave modal focus restoration. Epoch-keyed draft state replaced the remount; the focused autosave arbitration and route suites both pass.
- The first independent fail-closed review rejected two inconsistencies: a composite PNG could include noncanonical draft geometry, and the imperative MapLibre click handler could observe stale authoring mode before its passive callback-ref update. Export is now disabled for the matching document epoch until Finish/Cancel/tool change, with unit and Chromium assertions. A child-before-parent layout-effect regression failed under the passive update and passes after synchronizing click-routing refs during layout.
- Fresh serial verification on the completed tree:
  - `npm test -- --run` — pass, 29 files / 308 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm run build` — pass; the known ~1.25 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e` — 53 pass / 7 documented Firefox WebGL-runtime skips across 60 Chromium, Firefox and WebKit cases. Route authoring/export passes in Chromium and WebKit and runtime-skips in the Firefox fallback environment.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `b01c49876d0ca7fb3ea041b958e00976538699d472d101839bcd6e5832519912`. The live capture showed both draft endpoints, the straight line, and the Finish/Cancel panel with a ready map, zero body overflow, zero gradients/shadows, and empty console/page-error buffers. Visual review found no material clipping, collision, hierarchy, or legibility defect; the preview remained HTTP 200 on `127.0.0.1:4178`.
- Intermediate full browser gates encountered a transient OpenFreeMap glyph-origin outage, then a Chromium PNG case that timed out only after its final download had started; the exact PNG case passed in 9.3 seconds. A later gate exposed the previously tracked Import-trigger focus flake twice in Chromium. Focus restoration now runs from the committed non-pending state rather than a timer plus animation frame. A final review then rejected unconditional refocusing when a user had intentionally moved elsewhere during a pending read; a RED unit regression reproduced the focus theft, and restoration is now gated to neutral/file-input/Import focus. The exact Chromium case and final complete serial suite pass; the suite reports 53 pass / 7 Firefox WebGL-runtime skips. External font/tile availability remains a tracked third-party risk.
- Independent fail-closed route re-review passed with no security concerns, logic errors, or suggestions after 70 focused tests across seven files. The bounded final focus re-review also passed after inspecting all staged files and rerunning 52 focused tests. `docs/COMPLETE.md` does not exist because the wider mission gate remains open.

### 2026-08-22 — Single-click POI authoring (verified)

- Re-audited main, every registered worktree and branch, live Hermes/test processes, artifacts and the enabled preview before editing. Main was clean at `f150e92`; the roadmap, Wave 1 and Wave 2 foundations were already integrated, no agent branch was newly eligible, no duplicate implementation/test worker was running, and `127.0.0.1:4178` remained the sole workspace Vite preview.
- The Pin tool now enters a focused placement mode with visible instructions, an explicit Cancel action, disabled export and an active-tool state. Cancel returns focus to Select without changing history. One valid map click creates the lowest available canonical `POI NN` immediately above the basemap, selects it, returns focus to Select and records one Undo/Redo transaction.
- Created points participate in the existing live MapLibre content adapter and all print paths. The real-browser flow proves the point appears in the map layer order and the layered SVG contains the new POI as a named vector group with a circle.
- Strict TDD evidence: the nominal app interaction first failed because Pin did not disable Export; Cancel first failed because no action existed; three invalid-coordinate cases first created `poi-01`; canonical collision handling first selected duplicate `poi-01` instead of `poi-02`; and the authoring-safe Export title first remained route-specific. Each focused regression passed after its minimal implementation.
- Fresh serial verification: focused POI/route/store/workspace coverage passes 4 files / 36 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 30 files / 314 tests; production build passes with the known ~1.25 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 55 with 8 runtime Firefox WebGL skips across 63 Chromium, Firefox and WebKit cases.
- Browser evidence: `docs/screenshots/latest-desktop.png` and `docs/screenshots/poi-authoring-20260822.png` are exact 1440×900 captures, SHA-256 `b4ce5b6dc442c82473b571609e394ab50b1775f7c6818e0f5e176c81f8d813a4`. The active POI panel is readable above the toolbar with no clipping or overlap; live metrics report a ready map, zero body overflow, zero computed gradients/shadows and no application console/page errors. One full-suite run again logged transient third-party OpenFreeMap glyph fetch fallbacks while all affected cases passed.
- Independent fail-closed review passed with no security concerns or logic errors after independently running 64 focused unit tests, two Chromium authoring/export cases, typecheck and lint. Non-blocking follow-ups are a POI-specific document-open regression and a numbering-gap/insertion-position store assertion. `docs/COMPLETE.md` does not exist because the wider mission gate remains open.

### 2026-08-22 — Polygon shape authoring (verified)

- Re-audited the main integration worktree, every registered agent worktree/branch, running Hermes/test processes, existing artifacts and preview service before editing. Main was clean at `a35317f`; the roadmap and Wave 1/2 foundations were already integrated, the original Wave branches had no commit ahead of main, and no duplicate implementation worker or workspace test server was running. The enabled preview remained the sole workspace Vite process on `127.0.0.1:4178`.
- The Shape tool now captures map-click vertices into a visible noncanonical draft: point markers for every vertex, an open outline after two distinct clicks, and a closed translucent polygon after three distinct vertices. Finish remains disabled until the polygon has three distinct vertices; Cancel discards the draft and restores focus to Select without changing history.
- Finishing validates finite in-range coordinates and at least three distinct vertices, closes the canonical polygon ring, creates the lowest available `Shape NN` layer immediately above the basemap, selects it, and records the complete shape as one Undo/Redo transaction.
- Shape authoring disables export until Finish/Cancel/tool change. Opening another project invalidates the draft by document epoch and cannot resurrect its prior vertices when Shape is activated again. Draft content remains outside the canonical document, autosave and print/export paths.
- Strict TDD evidence: the store test first failed because `createShape` did not exist; distinct-vertex validation then failed by creating `shape-01`; the app interaction first failed because Shape did not disable Export; Cancel first failed because no action existed; and the document-epoch regression first restored one abandoned vertex. Review remediation then observed RED for repeated clicks enabling Finish and for an already-closed ring receiving a second terminal closure; both pass after distinct gating and canonical closure.
- Browser coverage cancels a draft, draws three real map vertices, verifies open/closed draft rendering, finishes and selects the canonical shape, exercises Undo/Redo, and downloads a layered SVG containing `Shape 01` as a named vector path.
- Fresh serial verification: typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 31 files / 324 tests; production build passes with the known ~1.25 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 57 with 9 documented Firefox WebGL-runtime skips across 66 Chromium, Firefox and WebKit cases.
- Browser evidence: `docs/screenshots/latest-desktop.png` and `docs/screenshots/shape-authoring-20260822.png` are current exact 1440×900 captures, SHA-256 `6fcc85435fb08580bfd65c7555eb1b5d5b80010952e1d4aa6eff4eeba8862b73`. Live metrics report a ready map, zero body overflow, zero computed gradients/shadows and no application console/page errors. Visual review found the draft polygon, three vertices, authoring controls, toolbar, print frame and sidebars readable with no material clipping, overlap or crowding.
- The first fail-closed review rejected repeated-click draft loss and double-closing an already-closed ring. Both received focused RED regressions and minimal fixes. Fresh independent re-review passed with no security concerns or logic errors after inspecting every tracked/untracked change and independently running 75 focused tests, typecheck, lint and React Doctor. Its only nonblocking suggestion is normalizing multiply-closed external input in addition to the supported open and once-closed forms.
- `docs/COMPLETE.md` does not exist because portable ZIP, geometry editing/styles, camera/style/text-scale controls, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-22 — Canonical bearing and pitch camera controls (verified)

- Re-audited the clean main integration worktree, every registered agent worktree/branch, running Hermes/test processes, existing artifacts and the enabled preview. Wave 1/2 branches remain already integrated or superseded; none is eligible for another merge, no duplicate implementation/test worker was launched, and `127.0.0.1:4178` remained the sole workspace Vite preview with HTTP 200.
- Added schema-version 4 canonical camera settings. Versions 1–3 migrate to neutral `0°` bearing and `0°` pitch; current portable project files require finite bearing from `-180°` through `180°` and pitch from `0°` through `60°`.
- Bearing and pitch controls now use transaction-safe drafts: typing does not create history, valid blur commits one Undo/Redo step, invalid drafts expose `aria-invalid`, and invalid blur restores the canonical value. External Undo/Redo, Open and autosave recovery remount stale edit boundaries.
- Camera values apply immediately to MapLibre and remain active through **Fit page**. Portable Save/Open and IndexedDB autosave/recovery preserve the values; the browser flows exercise `35°/40°` and `-20°/35°` cases.
- Strict TDD evidence: version-3 migration first returned schema 3 with no camera; camera actions were initially absent; invalid ranges initially entered history; the Project controls were read-only; pitch `61` initially remained aria-valid; the live map initially received no camera update; and Fit page initially omitted bearing/pitch. Each focused assertion passed after its minimal implementation.
- The first final fail-closed review rejected an effect dependency that made Fit page persist after its one-shot command: later bearing/pitch edits refit center/zoom. A focused RED regression received two `fitBounds` calls instead of one; tracking the handled request now preserves current center/zoom while camera edits still apply immediately.
- Fresh serial verification on the final code:
  - `npm test -- --run` — pass, 32 files / 338 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm run build` — pass; known ~1.25 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e -- --workers=1` — 57 pass / 9 documented Firefox WebGL-runtime skips across 66 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `6559e34c73f7e433c72b8b2bb0a13ef7d6007f93e34f44e395f9a008cc7fa056`. Live preview inspection committed bearing `35` and pitch `40`, reported `data-map-ready=true`, zero body overflow, zero gradients/shadows and no JavaScript/page errors. Visual review found no material clipping, overlap, fallback map, decorative shadow, hierarchy or control-legibility regression.
- Fresh independent fail-closed re-review passed with no security concerns, logic errors or suggestions after inspecting the complete staged diff and independently rerunning 338 unit tests, focused camera coverage, typecheck, lint, build and audit. A bounded schema-format correction confirmed the staged diff was unchanged and retained the passing verdict.
- `docs/COMPLETE.md` does not exist because portable ZIP, geometry editing/styles, remaining map design controls, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-22 — Canonical open map styles (verified)

- Re-audited the clean main integration worktree, every registered agent worktree/branch, live Hermes/test processes, artifacts and the enabled preview. The old Wave branches remain already integrated or superseded; none is eligible for another merge, no duplicate implementation/test worker was launched, and `127.0.0.1:4178` remained the sole workspace Vite preview with HTTP 200.
- Added schema-version 5 canonical map style settings with explicit version-4 migration. **Liberty** and **Positron** are now real selectable OpenFreeMap presets rather than a disconnected dropdown; choosing either is one Undo/Redo transaction and keeps the basemap layer label synchronized.
- Vendored the Positron style from `https://tiles.openfreemap.org/styles/positron` and coalesced its three missing `ref_length` numeric filters, matching the existing clean Liberty integration. Vendored SHA-256: `94c301710f3faf136e18b649b9f9aa1ef463b54cf4972c9714b1fa35e3cf1e12`.
- Style changes recreate the renderer through the existing MapLibre boundary, invalidate stale export/readiness state during loading, rebuild editable overlays after the new style loads and recover by selecting another preset after a style failure. Portable Save/Open and IndexedDB autosave/recovery preserve the selected preset; current project files reject unsupported style IDs.
- Strict TDD evidence: the first UI interaction failed because `positron` was not a canonical option; version-4 migration first remained schema 4 with no style; unsupported portable styles initially parsed; the renderer initially stayed on Liberty; the vendored URL test initially received the remote URL; the basemap label initially remained Liberty; an opened Positron project initially retained the Liberty label; a delayed style request exposed stale `data-map-ready=true`, which produced a visibly blank screenshot before the readiness regression and fix; current-project parsing initially rewrote a custom basemap layer name; and explicit style changes initially did the same before both data-preservation paths were restored.
- Two pre-final fail-closed reviews independently rejected style recreation because a project already at non-zero bearing/pitch created the replacement map at renderer defaults while the UI retained the canonical values. A focused regression received only two `jumpTo` calls instead of three across the style switch. Camera synchronization now reruns for the new preset, transition-time camera attributes are invalidated, and Chromium switches to Positron after setting `35°/40°` before checking the replacement renderer state.
- Fresh serial verification on the final tree: Vitest passes 34 files / 344 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.25 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 59 with 10 documented Firefox WebGL-runtime skips across 69 Chromium, Firefox and WebKit cases.
- Final independent fail-closed re-review passed with no security concerns, logic errors or suggestions after inspecting the current staged diff and independently rerunning all 344 unit tests, typecheck, lint, React Doctor, build, audit, the 69-case serial Playwright gate and the focused five-case Chromium editor suite.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `6684690fa3c53cb8bdf8a25b4e16e4aac532acea3534f2485721fc98cb678866`. It visibly shows the Positron basemap plus route, POI and polygon overlays after the `35°` bearing / `40°` pitch style switch, with matching sidebar/control labels. Live metrics report `data-map-ready=true`, overlay order `route-01,poi-cafe,area-center`, zero body overflow, zero gradients/shadows and no console/page errors. Visual review found no material clipping, overlap, fallback, hierarchy or readability defect.
- `docs/COMPLETE.md` does not exist because portable ZIP, geometry editing/styles, global text scale and visibility controls, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-22 — Canonical global map text scale (verified)

- Re-audited main, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. The roadmap and Wave 1/2 foundations were already patch-equivalent or integrated, no agent branch was eligible for another merge, and `127.0.0.1:4178` remained the only workspace Vite preview with HTTP 200.
- Added schema-version 6 canonical global map text scale with a validated `50–200%` range. Version-5 projects migrate to `100%`; portable Save/Open and IndexedDB autosave/recovery preserve current values.
- Text scale is a transaction-safe Project property: drafts do not enter history while typing, valid blur creates one Undo/Redo edit, invalid values expose `aria-invalid` and restore canonical state, and external history/open/recovery changes discard stale drafts.
- MapLibre captures each style's original text-size values once per lifecycle and scales from that immutable baseline, so repeated edits do not compound. Numeric sizes, zoom-safe top-level `interpolate`/`step` expressions, live edits and style recreation are covered; Liberty and Positron both retain ready overlays after scaling.
- Terminal renderer states now share a per-lifecycle failure latch. Renderer errors, live text-resize failures and overlay isolation/restoration failures withdraw readiness/export permanently for that lifecycle even if later content synchronization succeeds; selecting another style creates a fresh recoverable lifecycle.
- Strict TDD evidence included expected RED failures for the missing store action, invalid scale acceptance, version-5 migration, unsupported schema 6 parsing, the read-only Project field, missing invalid-state feedback, the absent renderer-scaling module, invalid nested zoom expressions, live resize readiness resurrection, and overlay-restore readiness resurrection. Each focused regression passed after its minimal implementation.
- Fresh final serial verification: `npm test -- --run` passes 37 files / 356 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.26 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 59 with 10 expected Firefox WebGL-runtime skips across 69 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `7be3bee15dd7617ace90b5b5f20e1ded75390da5198246dec941bfe9a27d364d`. It shows Positron at `125%` with enlarged labels and aligned route/POI/shape overlays. Live preview inspection reported `data-map-ready=true`, canonical `100%` on a fresh project, zero body overflow, zero gradients/shadows and no application console/page errors; visual review found no material clipping, overlap, fallback, hierarchy or readability defect.
- Fail-closed review first rejected live resize recovery, then found the sibling overlay-restoration path. Both received RED-first lifecycle regressions and a shared failure-latch correction. Fresh final independent review passed with no security concerns, logic errors or suggestions after rerunning 114 focused tests, typecheck, lint, React Doctor and changed-line security scans.
- `docs/COMPLETE.md` does not exist because portable ZIP, geometry editing/styles, major feature/label visibility controls, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-22 — Canonical major map-feature visibility (verified)

- Re-audited the clean main integration worktree, all registered worktrees and branch divergence, live Hermes/test processes, existing artifacts and the enabled preview before editing. The roadmap and Wave 1/2 foundations remain integrated or patch-equivalent; no old agent branch is newly eligible, no duplicate implementation/test worker was launched, and `127.0.0.1:4178` remained the sole workspace Vite preview with HTTP 200.
- Added schema-version 7 canonical visibility for Roads, Buildings and Labels. Version-6 and older projects migrate to all categories visible; current portable files require explicit booleans. Each checkbox is one Undo/Redo transaction, and portable Save/Open plus IndexedDB autosave/recovery preserve the state.
- The MapLibre boundary classifies the vendored Liberty and Positron basemap layers by source-layer/text semantics, preserves each style layer's original visibility, applies live category changes, and reapplies canonical state after style recreation. Any visibility update failure withdraws renderer/export readiness for that lifecycle instead of publishing a partially trusted export.
- Strict TDD evidence: the store test first failed because `setMapFeatureVisibility` did not exist; the Project interaction first failed because `Show roads` was absent; the controller test first failed because `MapFeatureVisibility` did not exist; the lifecycle test first received no visibility updates; and current portable-project validation first rejected schema 7. Additional RED regressions now prove that Roads leaves rail/transit infrastructure independent and that a live visibility edit withdraws renderer readiness/export until the next idle frame. Each focused test passed after its minimal implementation.
- Fresh serial verification: focused coverage passes 6 files / 78 tests; Vitest passes 40 files / 364 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.26 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 59 with 10 documented Firefox WebGL-runtime skips across 69 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `7fb98aaedeb20bfaae99435a411b54dee5c2908a9bd6f8089823ce4f157161ff`, with timestamped `docs/screenshots/map-visibility-20260822.png`. It shows Positron with Roads off and the new controls fully visible. Live preview inspection switched Liberty to Positron while preserving `roads:false`, remained map-ready, reported zero body overflow, gradients and shadows, and had no application console/page errors. Visual review found no material clipping, overlap, crowding, fallback, hierarchy or readability defect.
- Fresh independent fail-closed review passed with no security concerns or logic errors after inspecting the complete diff and both vendored style manifests. Non-blocking follow-ups are direct failure-latch coverage for a visibility write exception and manifest-level classification fixtures to detect future style-layer drift.
- `docs/COMPLETE.md` does not exist because portable ZIP, geometry editing/styles, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-22 — Deterministic portable project ZIP round trips (verified)

- Re-audited the clean main integration worktree, every registered worktree/branch, live Hermes/test processes, existing artifacts and the enabled preview before editing. Historical Wave branches remain integrated, superseded or patch-equivalent; no agent branch was newly eligible and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Added a visible **Save ZIP** action while retaining portable JSON **Save**. The ZIP contains exactly `manifest.json` and `project.printmap.json`, uses a fixed archive timestamp, and round-trips the complete validated canonical project through the same Open/history-reset boundary as JSON.
- Open now accepts `.printmap.json` and `.printmap.zip`. Invalid suffixes, malformed archives, unsupported manifests, extra/traversal/duplicate entries, oversized compressed or expanded content, and any embedded-asset declaration remain contained with actionable errors; the current project and history are unchanged. Embedded assets are explicitly unsupported until the canonical schema owns and validates them.
- Archive input and expanded project data are capped at 10 MiB, manifests at 64 KiB, and generated archives are refused before download when they exceed the same bound. Save failures appear as an accessible alert and a successful retry clears it.
- Strict TDD evidence: the archive unit suite first failed because `projectArchive` did not exist; the Chromium nominal flow first timed out because **Save ZIP** did not exist; and the save-error interaction first failed with an uncaught exception and no accessible alert. Each focused test passed after its minimal implementation.
- The first complete browser run exposed two test-only assumptions after the new adjacent action: the old `Save` locator was no longer exact and a dotted ZIP entry name was interpreted as a nested matcher path. Correcting those assertions produced 6/6 focused cross-browser Save tests and a clean final serial gate.
- The first fail-closed review rejected trusting only a ZIP entry's declared expanded size: a stored entry could declare one byte while carrying a 70 KiB manifest. A RED forged-central-directory regression received the manifest JSON error instead of the 64 KiB guard; compressed and declared sizes are now checked before extraction and actual extracted byte lengths are rechecked before decoding.
- The next re-review correctly found that forged DEFLATE metadata could still force unbounded decompression work before the post-extraction guard. A RED 1 MiB highly-compressible forged-manifest regression received a JSON error instead of a compression-method rejection. Project ZIPs now generate and accept only stored method-0 entries, so archive bytes are the hard pre-extraction bound and no untrusted DEFLATE stream runs. The focused archive/save suite passes 9/9 after remediation.
- Fresh final verification: Vitest passes 42 files / 373 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.28 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; Playwright passes 62 with 10 documented Firefox WebGL-runtime skips across 72 Chromium, Firefox and WebKit cases.
- One post-remediation full browser gate timed out after the unrelated Chromium shape-export download action despite the button becoming enabled. The exact shape case passed in 11.3 seconds immediately afterward, and the complete serial 72-case gate then passed cleanly; this remains tracked with the existing external renderer/tile timing flakes rather than being hidden.
- Artifact evidence: all six independent first/second downloads across Chromium, Firefox and WebKit are byte-identical 2,368-byte ZIPs with SHA-256 `588fdc85fab2b65e6eebd274bf3141b95b21bae7c027457f9386ca4cf3978602`; archive listing reports exactly two fixed-time, stored method-0 entries. The refreshed exact 1440×900 `docs/screenshots/latest-desktop.png` has SHA-256 `c50b1f4e5bdd9d490944ec5419eef9e1c786cc33d9475ffbd6cc4df87413a244` and visibly includes **Save ZIP** without clipping or top-bar crowding. Live inspection reported a ready map, zero body overflow, zero gradients/shadows and an empty console/page-error buffer.
- Final independent fail-closed re-review passed with no security or logic blockers. It confirmed fflate invokes the filter before extraction, every non-method-0 entry is rejected there, compressed/declared sizes are bounded, and actual stored byte lengths are checked afterward. `docs/COMPLETE.md` does not exist because geometry editing/styles, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-23 — Canonical content-layer appearance controls (verified)

- Re-audited the integration worktree, all registered worktrees/branches, live Hermes/test processes, existing artifacts and the enabled preview before editing. Historical Wave branches remained integrated or superseded, no branch was newly eligible, no duplicate worker was launched, and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Route layers now expose canonical color and width; POIs expose color and marker size; shapes expose independent fill, outline color and outline width. Every control validates known bounds, marks invalid numeric drafts with `aria-invalid`, commits one history transaction on blur/change, and supports Undo/Redo without resurrecting abandoned drafts after external history changes.
- The same canonical appearance drives incremental MapLibre paint, imported GeoJSON/GPX/KML defaults, portable JSON/ZIP and IndexedDB persistence, layered SVG vectors and named PDF vectors. Print conversions preserve the previous default visual scale; PDF POIs now retain the same white outline as the live map and SVG.
- Applied the pre-release schema policy directly: schema version 8 is the only current portable format, the legacy migration chain was removed, and versions 1–7 are rejected with a reset-oriented obsolete-file message. The Alpine fixture and save/archive assertions were updated atomically instead of extending migration logic.
- Strict TDD evidence included expected failures for missing Route/POI/Shape controls, invalid-state feedback, canonical paint propagation, SVG/PDF appearance fidelity, portable round trips, import defaults, desktop Tab being incorrectly trapped by a mobile-only focus loop, stale numeric-draft resurrection, obsolete schema 7 acceptance and the missing PDF POI outline. Each focused regression passed after its minimal fix.
- Fresh final serial verification: `npm run typecheck` passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 44 files / 376 tests; production build passes with the known ~1.28 MB pre-gzip bundle warning; `npm audit --omit=dev` reports 0 vulnerabilities; Playwright passes 64 with 11 documented Firefox WebGL-runtime skips across 75 Chromium, Firefox and WebKit cases.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `4aad4a77799bd05905e53cb54c50a553a45bbf20c445d5d2247a48c411b1cfe3`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `837b0c7a7e5c646a1aa32830fa3077653d79f8ded32656b24f086f8897f631b6`. Live metrics reported a ready map, zero body overflow, zero gradients/shadows, a focused mobile Properties dialog and no console/page errors. Visual review found the new controls readable with no material clipping, overlap or hierarchy defect.
- The fail-closed review initially rejected ambiguous schema-7 replacement semantics and a missing PDF POI outline. Both received RED-first corrections; the one bounded independent recheck passed both blockers with no new issue. `docs/COMPLETE.md` does not exist because geometry editing, native high-resolution map detail and the Mapbox renderer/storage decision remain completion blockers.

### 2026-08-23 — Direct POI coordinate editing (verified)

- Added Longitude and Latitude fields for selected point layers. Drafts remain outside history while typing; finite in-range values commit on blur as one canonical edit, while empty or out-of-range values expose `aria-invalid`, restore the saved coordinate and leave history unchanged.
- Tab from Longitude continues directly into Latitude even though the committed field receives a fresh transaction boundary. Undo/Redo restores the fields and canonical geometry without stale drafts.
- Coordinate edits rebuild the live MapLibre GeoJSON source and expose synchronized geometry diagnostics. The canonical edit therefore flows through IndexedDB autosave, portable JSON/ZIP, and the existing print scene without a parallel geometry model.
- Strict TDD evidence: focused tests first failed for the missing Longitude field, missing Latitude field, lost Tab focus (`BODY` received focus), and missing live geometry synchronization attribute. The first combined adapter run then caught repeated geometry serialization during selection-only syncs; caching the geometry diagnostic with the existing immutable content revision restored the performance invariant.
- Fresh tiered verification: 4 focused unit files pass 75/75; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the impacted Chromium appearance/geometry browser file passes 2/2. The independent reviewer additionally reran the focused unit set, typecheck, lint and the targeted all-browser file (Chromium/WebKit pass; renderer-fallback cases skip at runtime) and returned a fail-closed pass with no security or logic errors.
- The review's only non-blocking suggestion was to prove the actual MapLibre source definition rather than relying only on the diagnostic attribute. The focused adapter regression now asserts that the rebuilt GeoJSON source contains the moved `[2, 3]` point and passes.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `b6fdd37897c4c71a8ee9e7bf45960cd8b02f282610c47e9df793344a294c4198`. It shows Coffee stop at `16.4° / 48.25°`; live metrics report a ready map, synchronized geometry, zero body overflow, zero gradients/shadows and no console/page errors. Visual review found the Location section readable with no material clipping, overlap, blank map, crowding or hierarchy defect.
- This is the first feature slice after the last complete serial gate, so the fast tier policy retains the prior full-suite/build/audit evidence until the third slice or a cross-cutting/final gate. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Selected route-vertex coordinate editing (verified)

- Selected route layers now expose a vertex picker plus Longitude and Latitude fields. Each finite in-range coordinate commits on blur as one canonical edit; invalid drafts are marked with `aria-invalid`, restore the saved position and leave history untouched.
- Moving a vertex rebuilds the live MapLibre route geometry immediately. Undo/Redo restores both the fields and line, while portable JSON and layered SVG consume the same canonical coordinates; the browser regression proves the downloaded SVG path changes and the saved project contains the moved vertex.
- Strict TDD evidence: the store test first failed because `setRouteVertex` did not exist; the invalid matrix then failed five coordinate/index cases; the same-coordinate test first created history; and the app interaction first failed because no Route vertex selector existed. Each focused test passed after its minimal implementation.
- Fresh tiered verification: 3 focused unit files pass 65/65; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the impacted Chromium appearance/geometry browser file passes 3/3. Static diff/security checks are clean. This is the second feature slice after the last full serial gate, so build, audit and all-browser evidence remain retained until the third slice or a cross-cutting/final gate.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `e03a2671153c475f3cee5a12a3d0dc5d0a422c992128243f3a3f5baa41992813`. It shows Vertex 2 at `16.4° / 48.25°`; live preview inspection reported a ready map, synchronized route geometry, zero body overflow, zero gradients/shadows and no application console/page errors. Visual review found the Vertices section readable with no material clipping, overlap, crowding, blank map or hierarchy defect.
- Final independent fail-closed review passed with no security concerns or logic errors after inspecting every tracked and untracked change and independently rerunning the 65 focused unit tests, typecheck and lint. Non-blocking follow-ups are decimal-lexical edge coverage, a stronger exact SVG path assertion, and mobile input hints. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Bounded polygon-ring vertex coordinate editing (verified)

- Selected shape layers now expose ring and vertex selectors plus Longitude and Latitude fields. Outer rings and holes are edited independently; the duplicated closing coordinate follows the first vertex automatically.
- Every valid coordinate commits as one canonical Undo/Redo transaction and updates the live MapLibre source. Portable JSON and layered SVG consume the same geometry; the browser flow proves the saved ring remains closed and the vector path changes.
- Finite coordinate bounds, valid ring/vertex targets and no-op edits are enforced at the store boundary. An edit that would leave fewer than three distinct ring vertices is marked `aria-invalid`, shows a corrective message, resets on blur and does not enter history.
- Strict TDD evidence: focused tests first failed because `setShapeVertex` did not exist, because a collapsing edit changed the ring, because the Shape ring control was absent, and because a relationally invalid coordinate was not marked invalid. Each focused assertion passed after its minimal implementation.
- Fresh serial verification: focused shape/store/UI coverage passes 34/34; Vitest passes 45 files / 405 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.29 MB pre-gzip bundle warning; audit reports 0 vulnerabilities. The all-browser gate was run serially per browser to stay within the parent command ceiling: Chromium 28/28 pass, Firefox 14 pass / 14 documented WebGL-runtime skips, and WebKit 28/28 pass. The current post-review Chromium shape flow also passes 1/1.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `938588db5ea722d9d2968b844627f729e7991b6a2484d4729ec469fec57ba2d2`. It shows the selected City center polygon with Outer ring, Vertex 2 and coordinate fields over a rendered Liberty map. Live metrics report map-ready state, synchronized geometry, zero body overflow, zero gradients/shadows and no application console/page errors; visual review found no material clipping, overlap, crowding or hierarchy defect.
- The first fail-closed review rejected unclosed or undersized rings showing coordinate fields even though the store refused their commits, which could leave an unpersisted draft visible. A focused regression first found the Shape vertex control still present for an unclosed ring. UI and store now share one ring-eligibility predicate; ineligible rings show corrective status without vertex/coordinate inputs. The one bounded independent recheck passed the blocker with 13/13 targeted tests, typecheck and lint, and reported no remaining issue.
- `docs/COMPLETE.md` remains absent because the Mapbox public-token/origin and renderer/storage decision, native high-resolution map detail, and a final complete nominal workflow still require resolution.

### 2026-08-23 — Public Mapbox token/origin diagnostics and compliance boundary (verified)

- Added a Project-properties Mapbox services surface that accepts only a deploy-time public `pk.` value from `VITE_MAPBOX_PUBLIC_ACCESS`, rejects missing/malformed/secret values before network I/O, never renders the credential, and names the exact browser origin that must be allowed.
- The explicit connection check goes through the provider request boundary to the HTTPS Mapbox Streets style endpoint. It is cancellable on unmount, disables repeat submission while pending, normalizes token/origin failures, permits retry, and does not unlock provider-dependent authoring merely because credentials work.
- Recorded the compliance-first decision in `docs/decisions/0001-mapbox-renderer-and-storage.md` with evidence-verified Mapbox sources: the open MapLibre editor remains independent, temporary provider responses cannot enter durable projects, and Mapbox-backed map matching requires a compliant Mapbox display path or written approval.
- Strict TDD evidence: the component suite first failed because the status module did not exist; Project wiring then failed because no alert was exposed; and the connection-probe regression failed with `probeMapboxConnection is not a function`. Each focused assertion passed after its minimal implementation.
- The first independent fail-closed review rejected two issues: the browser case passed only when its shell command injected a fake credential, so the repository's standard Playwright command failed; and a checked state could survive token/origin/probe prop changes while an old request remained live. The review itself reproduced the standard-command failure, and two new focused unit regressions then failed with stale Ready state and an unaborted signal. Playwright's own isolated web server now supplies only the fake test credential, while configuration identity changes immediately reset the surface and abort stale work.
- Fresh tiered verification after remediation: 3 focused unit files pass 43/43; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the standard impacted Chromium token/origin failure-and-retry command passes 1/1 with a ready live map. Citation evidence verification passes. Static security scanning found only deliberate fake `pk.fake-…` fixtures and no real credential.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png` and timestamped `docs/screenshots/mapbox-configuration-20260823.png`, SHA-256 `b2f45800a23c28d572fa5db4723c91e1031906795d2c1b6645b798bbb49be833`. The configured-success view is readable without clipping, overlap, gradient or decorative shadow. The persistent preview remains HTTP 200 and map-ready; with no value under the new deployment key it shows the exact actionable setup error for `http://127.0.0.1:4178`, has zero body overflow and an empty console/page-error buffer.
- The one bounded independent recheck passed with no security concern, logic error or suggestion. It independently confirmed the parent shell credential was unset, the standard Chromium test passed using only Playwright's fake web-server credential, all 6 status regressions passed, and identity changes immediately return the surface to idle while aborting stale work.
- This is the first feature slice after the polygon-editing full serial gate, so the 405-test/full-build/audit/all-browser evidence remains retained under the tier policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Native target-resolution PNG rendering (verified)

- Refreshed `docs/FEATURE_PARITY.md` from the current official Printmaps feature and home pages. The local matrix now distinguishes native target-resolution PNG rendering from browser-preview resampling and retains the explicit raster-basemap caveat for layered SVG/PDF.
- Print-size PNG now snapshots the ready live camera, style and canonical content, creates a fresh target-pixel MapLibre renderer for every preflight region, raises source zoom, scales style/content paint into print pixels and composes overlap regions 1:1. It no longer enlarges the browser preview.
- Single-region jobs preserve labels and pitch. Multi-region jobs fail closed for non-zero pitch and require the canonical Labels category off so independently placed symbols cannot create seams. Label visibility now controls every symbol layer; icon-only road symbols also require Roads, keeping the live map and export snapshot equivalent.
- Every temporary map has a bounded load/idle wait, renderer-error and cancellation handling, camera/allocation verification and deterministic DOM/MapLibre/canvas cleanup. Source-readiness changes invalidate the job before or after each region. A composing-stage observer failure now releases both the tile and output allocation.
- Strict RED/GREEN evidence from final remediation:
  - A 3508×2480 CSS-pixel temporary map caused Chromium to reload before download. The HiDPI regression first received `pixelRatio: 1`, full CSS dimensions and the higher unadjusted zoom; even target sizes now render at DPR 2 with half-size CSS layout and exact full-size backing pixels. Odd targets use DPR 1 to preserve exact dimensions.
  - The odd-dimension selector test first failed because the helper did not exist; the corrected 236×709 lightweight custom fixture exports exactly.
  - Stage sequencing first returned no events, and the real browser timing check first lacked the `downloading` boundary. Rendering, composition, encoding and local save are now measured separately.
  - The review regressions first left Liberty's icon-only one-way arrows visible when Labels were off and left a 100×100 native tile allocated when the composing observer threw. Both focused tests now pass.
- Final targeted verification after review remediation: 6 files / 48 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the impacted Chromium two-region case passes in 42.2 seconds. The nominal Chromium acceptance flow passes in 33.9 seconds with one real A4 export plus lightweight responsive/custom checks.
- Milestone evidence captured before the final lightweight timing/test adjustment remains green: Vitest 47 files / 423 tests, production build, audit with 0 vulnerabilities, Chromium 30/30, Firefox 14 pass / 16 runtime WebGL skips and WebKit 30/30. The final production adjustment is covered by the 48 focused tests and type/lint/Doctor gates above.
- Fresh A4 acceptance artifact: 3508×2480 RGBA PNG, SHA-256 `23468de69c9f3157436beb8b4866ea509d1a4e1c427a2fbc4c302cd2e6c47bf8`; measured headless Chromium stages were 21.509 s render, 21.7 ms composition, 165.9 ms encoding and 20 ms local save. The 7087×591 two-region evidence at `docs/screenshots/native-png-tiles-20260823.png` has SHA-256 `e4bb47e07d7df67fd177dc76905eac390799245a99c16cf5875fc5b23c6a78b6` and no visible seam in downscaled review.
- Current editor evidence: `docs/screenshots/latest-desktop.png`, exact 1440×900, SHA-256 `1d81c78bb234b8b8940b311e979857c85195fda510e9cfd393bfe22a3df5cdd4`. It shows the native-detail export contract and passed visual review without clipping, overlap, blank map, gradient or decorative shadow. The preview remains HTTP 200 on `127.0.0.1:4178`.
- The independent fail-closed review rejected silent icon-only symbol omission and the composing-observer tile leak. RED-first fixes addressed both; the one bounded recheck passed both blockers and independently reran the two exact regressions. No credential or injection finding was reported.
- `docs/COMPLETE.md` remains absent. Provider-backed search/road snapping is still disabled by the accepted Mapbox display/storage boundary, and the final complete nominal workflow remains unresolved.

### 2026-08-23 — Expert arc routes and travel-mode markers (verified)

- Re-audited the clean integration worktree, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical agent branches remain integrated or superseded; no branch was eligible for re-merge, no duplicate worker/test server was launched, and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Rechecked the current official [Printmaps features](https://www.printmaps.net/features/) and [home](https://www.printmaps.net/) pages. The official route workflow still documents straight, arc and magnetically road-snapped lines plus optional plane, train, car, walking, cycling and ship icons; `docs/FEATURE_PARITY.md` now records the dated local comparison without claiming provider-backed snapping.
- The Route tool now offers Straight or great-circle Arc geometry, Air/Train/Car/Walking/Cycling/Ship mode choices, and an optional mode marker before the first map click. The draft remains outside project history/export, Finish creates one selected undoable route, and the existing Cancel/tool-switch/open invalidation behavior remains intact.
- Route mode and marker visibility are canonical appearance state. They are editable as separate Undo/Redo transactions, drive a centered live MapLibre marker, survive JSON/ZIP/IndexedDB round trips, and print as vector content in layered SVG and PDF. Native target-resolution PNG rescales the marker halo into print pixels rather than leaving it at browser size.
- Applied the pre-release schema policy directly: schema version 9 is the only current portable format and schema 8 is rejected as obsolete. No migration chain was added.
- Great-circle interpolation preserves exact endpoints and samples each segment deterministically. Duplicate waypoints, invalid runtime authoring options and exact/near-antipodal arc segments fail closed without entering history; the UI keeps Finish disabled instead of crashing or publishing invalid canonical state.
- Strict RED/GREEN evidence included the missing route-profile module, absent authoring controls, unchanged straight geometry, missing canonical mode state, absent live-map/SVG/PDF markers, missing property-history controls, invalid-option acceptance, unstable antipodal output and unscaled native marker halo. Each focused regression failed for the expected reason before its minimal implementation and passes now.
- Fresh tiered verification: 11 focused files / 145 tests pass; the persistence-focused archive/project/autosave set passes 3 files / 40 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues. The single impacted Chromium flow passes 1/1 in 14.8 seconds and covers Arc/Air authoring, live marker state, Undo/Redo, layered-SVG download and a 390×844 no-overflow route panel. No full suite, Firefox or WebKit run was used for this first post-milestone slice.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `e55133477eaabc63477afb3bb90c9463dcee21b53c0678d51363626ff32d9ffe`. It shows Arc, Air and Marker controls, two draft points and the focused Finish action above the toolbar; downscaled visual review found no clipping, overlap, crowding, blank map, gradient or decorative shadow. Browser and Playwright console/page-error buffers were clean.
- The first fail-closed review rejected unchecked runtime options and unstable antipodal interpolation. Both received RED-first containment, and the one bounded independent recheck passed with no security concern or logic error after independently rerunning 67 targeted tests plus the exact 17-test remediation set.
- Remaining route caveat: the compact vector mode markers are functional profile badges, not yet a full pictographic icon catalogue. Address search and road snapping remain deliberately disabled by the accepted Mapbox display/storage boundary. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Rich POI marker shapes, symbols and labels (verified)

- Rechecked the current official Printmaps feature page directly. It still documents single POIs with adjustable size/color/icons, standard markers/labels, custom pins and spreadsheet placement; the local parity matrix now distinguishes the implemented standard marker workflow from custom images and batch entry.
- Selected POIs now expose Circle, Square and Diamond marker shapes, six semantic symbol choices and an optional 40-character label. Each choice is canonical, independently undoable and rendered through the live MapLibre map; invalid/oversized/control-character labels reset without entering history.
- The same canonical marker geometry, symbol and label print as vector content in layered SVG and named-layer PDF. PDF preserves Latin-1 labels such as `Café Central` and fails closed for characters its current base font cannot encode instead of replacing them silently.
- Applied the pre-release schema policy directly: schema version 10 is the only current portable format, schema 9 is rejected as obsolete, and JSON/ZIP/IndexedDB plus imported POI defaults use the new canonical appearance without a migration chain.
- Strict RED/GREEN evidence: the first app interaction could not find the marker-shape control; the first live-render test received one descriptor instead of three; the first SVG and PDF tests lacked marker vectors; portable validation accepted a control character; and the first non-Latin PDF test emitted `??` instead of failing. Each focused regression now passes.
- Review inspection and self-review exposed two cross-path blockers with focused RED regressions. Basemap-label suppression stripped every symbol layer from native PNG, including Studio POI diamonds/symbols/labels; it now removes only non-Studio basemap symbols and preserves deterministic user content. Shape/symbol selects also remounted the control group and dropped keyboard focus to `BODY`; those immediate controls now retain focus and Tab order while canonical size/label transaction boundaries remain keyed.
- Fresh targeted verification after remediation: 8 files / 95 unit tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium appearance/export flow passes 1/1 in 15.7 seconds. This is the second post-native-PNG milestone slice, so the prior full unit/build/audit/all-browser evidence remains retained under the tier policy.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `c6af2ae63f876ffa06659f9dbc0e09981201a90f77c307f9c0daee7a2b1a4b27`. It shows the selected Coffee stop as a Diamond/Coffee marker with the `Café Central` label and all new controls. Downscaled visual review found no clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect; live console inspection reported zero errors, and the persistent preview remains HTTP 200 on `127.0.0.1:4178`.
- The bounded independent recheck passed both remediated blockers with no security concern or logic error. It independently reran the native-export and app-field suites (2 files / 34 tests), confirmed the existing multi-region basemap-label rejection remains intact, and verified the staged diff is clean.

### 2026-08-23 — Bounded coordinate POI spreadsheet (verified)

- The Pin workflow now offers a pasteable spreadsheet panel for tab-separated Name, Longitude and Latitude rows. It accepts an optional copied header, requires strict decimal coordinates, bounds longitude/latitude, ignores blank lines, rejects malformed rows, limits names to the printable 40-character POI-label contract, caps each batch at 300 rows and rejects raw input above 64,000 characters before splitting.
- A valid list creates named, visibly labelled canonical POIs immediately above the basemap, selects the first new POI and records the complete batch as one Undo/Redo transaction. Validation is all-or-nothing at both parser and store boundaries, so one bad or oversized row set cannot partially alter the project.
- The editor keeps export disabled while the list is open, initially focuses the textarea, returns parse errors to that field, restores focus to the Paste action when cancelling the list and returns to Select after creation. The compact panel remains inside a 390×844 viewport with zero body overflow.
- Strict RED/GREEN evidence included the missing parser module, five accepted malformed/unsafe row forms, missing 300-row and empty-list guards, absent batch store action, partial acceptance of an invalid batch, acceptance of 301 store entries, the missing Paste workflow and lost focus after cancelling back to map placement. Each focused regression passed after its minimal implementation.
- Fresh targeted verification: 6 focused files / 85 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium flow passes 1/1 in 10.8 seconds and covers error containment, creation, selection, live map synchronization, one-step Undo/Redo, mobile geometry, focus and console cleanliness. This is the third post-native-PNG slice, so the full unit/build/audit/full-Chromium milestone gate remains deferred until five verified slices or an earlier major milestone.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `b2d6043405c07a2ca0620c8894bb56b2bb6aa66b001becac646f99d7033b50f3`. The list panel measured 512×220 at x=448/y=608, stayed above the tool palette and showed the ready Liberty map underneath. Live metrics reported zero body overflow, gradients, shadows, console warnings/errors and page errors; downscaled review found no material clipping, overlap, hierarchy or readability defect.
- The first fail-closed review rejected two security boundaries: row count alone did not bound oversized raw text before splitting, and the shared label check still accepted C1 and Unicode formatting/bidirectional controls. Focused RED regressions reproduced 64,001-character input, U+0085 and U+202E acceptance. The parser and textarea now share the 64,000-character cap, while canonical POI labels reject all Unicode Cc/Cf characters. The one bounded independent recheck passed both blockers and reran all 12 parser regressions with no remaining concern.
- Address columns and geocoding remain intentionally absent: the accepted Mapbox display/storage decision still forbids durable provider-derived coordinates on the current MapLibre path.

### 2026-08-23 — Validated custom POI marker assets (verified)

- Rechecked the live official Printmaps feature and home pages. The feature page still supports user-uploaded custom pins and states a 100×100-pixel minimum; the home page still advertises 300-DPI or vector output. `docs/FEATURE_PARITY.md` now records the implemented custom-image workflow and its exact PDF caveat.
- Selected POIs now accept PNG, JPEG and sanitized SVG markers. Upload is capped at 1 MiB and 2048×2048 pixels, requires at least 100×100 pixels, verifies raster headers/SVG geometry, rejects active/external SVG content, decodes before committing, and derives a SHA-256 content ID that is reverified whenever a project is opened.
- Custom assets are canonical schema-11 project data keyed by content hash. Attaching, replacing and removing are single Undo/Redo edits; duplicated POIs share one asset and final-reference deletion prunes it. Portable JSON/ZIP and IndexedDB preserve the same bounded data. Obsolete schema 10 is rejected directly under the pre-release policy. Projects are capped at 64 custom assets, 8 MiB encoded input and 16 megapixels of decoded texture area.
- The live MapLibre map registers each image only after bounded browser decoding. Native target-resolution PNG registers the same canonical images in temporary print renderers, while layered SVG embeds each marker inside its named POI layer. PDF fails closed with a corrective PNG/SVG message rather than silently substituting a standard symbol.
- Strict RED/GREEN evidence: the asset test first failed because the validation module was absent; store tests first failed because `setPoiCustomMarker` and canonical assets were absent; SVG and MapLibre descriptor tests first lacked custom images; the live Chromium flow first withdrew map readiness while SVG decoding was incomplete; and deletion first retained an orphaned asset. Review-remediation tests then failed on XML processing instructions, SMIL, aggregate decoded pixels, keyboard upload semantics, stale map textures and hidden native-export assets before those paths were tightened. Each focused regression now passes.
- Fresh targeted verification: 15 focused files / 148 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium custom-marker/save/SVG flow passes 1/1 in 7.0 seconds with clean console capture. The five final review regressions pass together as 5 files / 33 tests.
- Independent review first blocked unsafe SVG processing instructions/SMIL and unbounded aggregate textures; its earlier bounded recheck caught CSS-escaped presentation URLs and stale image decode races. The fresh final review then rejected raw-byte/base64 portable-size mismatch, removal during replacement decode, portrait SVG print scaling, generic appearance ownership bypass and fractional SVG boundary rounding. Every blocker received an observed-RED regression and minimal fix: encoded assets and JSON downloads now respect portable limits, removal invalidates pending work, print markers constrain their maximum dimension, only the dedicated action owns asset references and SVG dimensions must be whole pixels. The one bounded blocker recheck passed all five with no unresolved blocker after independently rerunning the exact 33-test remediation set.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `aa632e9c3e11bd210d6482d15edb72a2141466fb30d3851cf64aa32933939fee`. It shows the blue/white custom pin on the live Vienna map and compact Replace/Remove/status controls. Downscaled review plus full-resolution marker/sidebar crops found no fallback, corruption, clipping, overlap, gradient, decorative shadow or hierarchy defect. The persistent preview remains HTTP 200 on `127.0.0.1:4178`.

### 2026-08-23 — Broader open styles, map language and detail controls (verified)

- Rechecked the official Printmaps feature and home pages directly. The feature page still documents multiple map styles, selectable map items, map-language changes and text scaling; the home page still advertises 300-DPI/vector output. `docs/FEATURE_PARITY.md` now records the current local comparison without claiming the wider official catalogue is fully matched.
- Added the same-origin vendored OpenFreeMap Bright preset alongside Liberty and Positron. The upstream style retrieved from `https://tiles.openfreemap.org/styles/bright` had SHA-256 `ada317e9b31c65b726dc46a2b3b14acb9856782051a09e3f4bc92d034373999f`; the three nullable road-shield filters were normalized with the same fail-closed fallback used by the existing presets, producing vendored SHA-256 `87658c3f53cbe5d7e50bd50abee7b2fa401b0091cfa158bf42cb5dbfa0e3b2e4`. The focused Chromium flow is console-clean after style loading.
- Added canonical Local, English, German, French, Italian, Spanish and Chinese label selection. Translated labels fall back to each style's original local-name expression, leave Studio POI labels unchanged, apply live without renderer recreation and remain present in the native-print style snapshot.
- Expanded independent map-feature controls from Roads/Buildings/Labels to include Water, Parks, Land detail and Transit. Style-layer classification preserves original visibility, treats rail/transit independently from roads and applies across live style recreation and native print capture.
- Applied the pre-release schema policy directly: schema 12 is the only current format; schema 11 is rejected with the existing reset-oriented message. JSON/ZIP, IndexedDB and the Alpine fixture use the new language/detail state atomically rather than adding a migration chain.
- Strict RED/GREEN evidence included the absent Bright option, missing language controller/UI, untranslated live labels, missing Water/Parks/Land/Transit controls, incomplete portable parsing, obsolete schema-11 acceptance and Bright renderer warnings. The road-shield warning test then exposed an invalid `road_` sprite lookup before the null fallback was corrected.
- The independent fail-closed review rejected stale schema-12 browser fixtures: portable project Open replaced the complete style object with only the old three visibility fields, while project-file and autosave assertions still expected the obsolete serialized diagnostic. The exact Chromium project-open flow first failed to open Alpine poster, and the autosave flow first received the seven-field diagnostic instead of the old value. Both fixtures now exercise German plus expanded detail state and pass independently.
- The bounded independent blocker recheck passed with no remaining finding after inspecting the three corrected fixture/test paths and independently rerunning both exact Chromium flows, lint and typecheck.
- Fresh targeted verification: 10 files / 70 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the impacted Chromium Bright/language/detail flow passes 1/1 with clean console capture, and the two review-remediation Chromium flows pass 2/2 serially. This is the fourth post-native-PNG slice, so the full unit/build/audit/full-Chromium milestone remains due after the next verified slice.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `a9bf3ae7b976dc716a448979d8752aba237e9052882f8af212f580e0184e53e0`. It shows Bright with German labels and Land detail disabled; live metrics report map-ready state, zero body overflow and no console/page errors. Downscaled review found no clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.

### 2026-08-23 — Bounded administrative-area selection, invert and print parity (verified)

- Re-audited the clean integration baseline, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical agent branches remained integrated or superseded, no branch was newly eligible, and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Refreshed the official Printmaps feature page directly on 2026-08-23. It still documents countries, municipalities and regions, outside-area **invert**, fill/transparency and a two-shape border recipe. The bounded local catalogue now exposes Austria, Hungary and Slovakia from Natural Earth 1:110m Admin 0 Countries; all three coordinate rings are byte-for-byte equal to upstream GeoJSON. Natural Earth's live terms page confirms its raster and vector data are public domain.
- The Shape tool now offers direct country selection alongside freeform drawing. Adding an area creates one selected undoable canonical shape immediately above the basemap, allocates collision-safe IDs, exits authoring with focus on Select and automatically fits the country boundary.
- Selected shapes now own an explicit canonical invert state in schema 13. Fill, outline, width and invert are independently undoable; current portable JSON/ZIP and IndexedDB preserve the state, while schema 12 is rejected as obsolete under the pre-release policy.
- Live MapLibre and native target-resolution PNG use a Web Mercator world mask only for inverted fill. A separate original-geometry source draws only the administrative boundary, and hit testing excludes the outside mask so clicks beyond the boundary retain normal canvas behavior. Layered SVG and PDF use even-odd outside fill plus a separate boundary stroke, never a page-edge stroke.
- Strict RED/GREEN evidence included the missing catalogue/store action/UI, absent invert control/schema rule, missing live world mask, absent SVG/PDF outside fill, missing auto-fit, and the two review blockers for world-ring outlining and outside-mask hit capture. Every focused regression failed for the expected reason before its minimal correction and passes now.
- Final targeted evidence: 8 administrative/appearance files pass 64 tests; map/native remediation coverage passes 5 files / 39 tests; typecheck, ESLint and React Doctor pass with no issues; the exact Chromium administrative-area Save/layered-SVG flow passes. The fifth-slice milestone gate passes 61 files / 510 unit tests, production build, audit with 0 vulnerabilities and full Chromium 34/34. A final Doctor-only authoring-exit refactor is additionally covered by 10 focused unit tests and current Chromium route/shape flows 3/3.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `ebc5c3707f4552124b2fdfb8de1a041760ee071fcc49f4bb22bca414c21b6106`. It shows Austria auto-fit with a readable untinted interior, tinted surroundings, one country-boundary outline and the canonical Invert control. Live metrics report map-ready state, selected `admin-aut`, zero body overflow, zero gradients/shadows and no console/page errors; downscaled visual review found no clipping, overlap, blank map or hierarchy defect.
- The first fail-closed review found the MapLibre world-ring outline and outside-mask hit-capture bugs. Both received RED-first fixes. The single bounded independent recheck passed both blockers with no security concern, logic error or suggestion after rerunning the exact two-test remediation file.
- Scope remains honest: this is a small country-scale catalogue, not Printmaps' large municipality/region database. Adjacent-area merge and precision administrative data remain future work. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Attributed route elevation profiles and exports (verified)

- Refreshed the directly linked official [Printmaps elevation profile editor](https://www.printmaps.net/elevation-profile-editor/). It documents GPX/KML/GeoJSON or drawn-route input, altitude/distance and travel-time inspection, curve/font/marker/grid/unit controls and layered-SVG download; `docs/FEATURE_PARITY.md` now records the dated comparison without claiming complete styling or offline parity.
- Selected existing or imported routes now generate an on-demand terrain profile in Layer properties. The client resamples the route to at most 100 coordinates, sends them only after explicit activation to Open-Meteo's documented 90 m Copernicus DEM GLO-90 endpoint, and discloses that transfer plus the required source attribution in the UI.
- The result shows a contained profile chart with distance, elevation range, total ascent and total descent. Pending requests are cancellable and retryable; route edits/selecting another layer abort or discard the transient result rather than persisting terrain responses into the project.
- Every result downloads as an attributed 150×75 mm SVG, 1800×900 PNG and exact-page 150×75 mm PDF. SVG/PNG preserve Unicode route titles; the current PDF base font fails closed with corrective SVG/PNG guidance when a route title is not safely encodable.
- Strict RED/GREEN evidence included the absent sampling module, export serializer, route-properties surface, responsive mobile action, cancellation control, bounded terrain validation, render-layout validation, Unicode PDF containment, semantic summary group and coordinate-sharing disclosure. Each assertion failed for the expected missing or unsafe behavior before its minimal correction.
- Fresh targeted verification: four focused files pass 13/13 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the one impacted Chromium flow passes and verifies the live interaction, 100-point ceiling, SVG attribution, 1800×900 PNG signature/dimensions, exact PDF MediaBox, mobile retry/accessibility and zero body overflow.
- Browser evidence: exact 1440×900 `docs/screenshots/latest-desktop.png` and `docs/screenshots/elevation-profile-20260823.png`, SHA-256 `41791fc73faf8efa4bb162bfd1f423b081ceca6a933040f69fac5f4da1f7f046`. The persistent preview used the real elevation endpoint and reported 8.4 km, 152–222 m, 59 m ascent and 117 m descent with zero body overflow and no application console/page errors. Downscaled review found the chart, metrics, downloads, disclosure and source link contained with no clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- The first independent fail-closed review rejected unbounded extreme finite elevations and silent PDF title substitution. RED-first remediation added plausible terrain bounds, finite derived-layout validation and explicit PDF encoding refusal. The one bounded independent recheck passed both blockers with 2 files / 6 tests and no remaining security or logic finding.
- Scope remains explicit: Open-Meteo's public endpoint is documented for non-commercial use unless a commercial customer endpoint or self-hosting is arranged. Imperial/styling/grid/marker controls, direct profile-only upload and walking/cycling estimates remain future breadth. This is the first slice after the administrative-area full milestone, so no redundant full suite/build/audit or non-Chromium gate was run. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Austrian region selection and merged administrative boundaries (verified)

- Re-audited the clean integration worktree, every registered agent worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remain integrated or superseded; no branch was eligible for re-merge and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Refetched the current official Printmaps feature and home pages directly on 2026-08-23. The documented countries/municipalities/regions, invert/fill workflow and 300-DPI/vector claims remain available; `docs/FEATURE_PARITY.md` now records the expanded local region workflow without claiming a broad boundary database.
- Added simplified public-domain Natural Earth 1:10m Admin 1 boundaries for Lower Austria and Vienna alongside the existing three-country catalogue. The Shape tool now separates Country and Region workflows, supports accessible multi-selection and creates Lower Austria + Vienna as one selected, auto-fitted, undoable canonical shape with the enclosed Vienna border removed.
- The aligned-boundary merge cancels shared edges, rejects missing/duplicate/non-region selections and normalizes GeoJSON winding. A screenshot-driven regression caught the first clockwise merged exterior rendering invert backwards; a RED winding assertion received signed area `-2.351…`, then the normalized exterior produced a clear region with tinted surroundings in the live MapLibre map.
- JSON/ZIP/IndexedDB require no schema change because the result remains a canonical polygon. Existing live MapLibre, native PNG, layered SVG and PDF paths consume it unchanged; the Chromium flow proves one-ring persistence, invert state and named layered-SVG output.
- Fresh targeted verification: six focused files pass 17 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium region-merge/Save/SVG/mobile flow passes 1/1 with zero body overflow.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `8ebc9934e400a412afd057c8f39ceb4e8a0788ea239aecd3fc7c90e6ab2c93df`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `2a95e5af72bf7a14c4e50444fa7f6d843dbaa5b0d079f92da8d2d6f08439573a`. Downscaled desktop and full mobile review found correct inverted fill, a contained region picker, no toolbar/panel overlap and no material clipping; only Chromium's classified headless ReadPixels diagnostics appeared during capture.
- Fresh independent fail-closed review passed with no security concern, logic error or suggestion after inspecting the complete staged diff, visual evidence and boundary semantics, then independently rerunning all 17 focused tests, typecheck and lint. No file was modified by the reviewer.
- Scope remains explicit: this is one useful Austrian state/enclave merge, not a municipality database or arbitrary polygon-union engine. This is the second slice after the last full milestone, so the prior full unit/build/audit/full-Chromium evidence remains retained.

### 2026-08-23 — Bounded multi-file drag/drop import review (verified)

- Refetched the official Printmaps feature and home pages directly. The feature page still documents GPX/KML/GeoJSON upload and says users may display as many GPX/KML routes as they like; the local parity matrix now records the deliberately stricter client safety boundary.
- Import now accepts drag/drop or file-chooser multi-selection. Up to 10 files / 20 MiB total (5 MiB each) are preflighted before reads, parsed as one all-or-nothing batch, reviewed in an accessible modal and committed as one Undoable edit. Existing single-file chooser behavior remains direct and backward-compatible.
- The review lists every file, supports complete selection replacement and retry, preserves an explicit **Fit imported content** or **Keep current view** choice, and fits the combined geometry extent only after a successful canonical commit. Invalid files never partially alter the project.
- The dialog traps forward/reverse Tab, supports Escape, close, Cancel and backdrop dismissal, restores focus to Import, and yields immediately to autosave recovery or Export. File work remains globally guarded across project opens/remounts until every started read settles; stale results require the exact source-document identity and latest autosave-safe state before the store accepts them.
- Batch limits also preserve the current portable-project ceilings of 1,000 layers and 200,000 positions. Every selected file is validated before I/O, started reads settle together, and bounded parsers run sequentially so rejection/cancellation cannot amplify concurrent parse work.
- Strict RED/GREEN evidence: the first Chromium flow found no drop affordance; replacement reset **Keep current view**; a dropped batch displaced Export; dropped-dialog cancellation lost focus; preflight started a valid read before rejecting an invalid sibling; cancelled reads released the guard early; stale single-file reads committed after edits/autosave; delayed recovery produced two modal dialogs; and project Open remounted past the guard. Each focused regression failed before its minimal correction and passes now.
- Fresh final tiered verification: 6 focused unit files / 66 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the final impacted Chromium set passes 5/5, while the broader import-only Chromium set passed 9/9 before the final nonvisual cross-remount guard correction.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `0f3e67bf36315ab862a0e1444170c238d1b8d725ed4049fb9f01f029f1f8685f`. Live inspection showed the two-file review at 480×301.5 px, focused **Import 2 files**, a ready map, zero body overflow, gradients, shadows, console errors and page errors. Downscaled visual review found no material clipping, overlap, hierarchy or legibility defect.
- The independent fail-closed review rejected abandoned-work amplification, stale asynchronous commits and autosave dual-modal behavior. RED-first remediation closed source identity, current autosave gating, modal priority/focus, preflight/settling and same-instance cancellation. Its one bounded recheck confirmed those paths and exposed a document-open remount bypass; moving the work guard above that remount boundary was then reproduced RED and verified with the final focused gates.
- This is the third slice after the administrative-area full milestone, so the full unit/build/audit/full-Chromium batch remains deferred until the fifth slice or an earlier major milestone. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Canonical map-area lock and permission-aware browser geolocation (verified)

- Rechecked the current official Printmaps feature page directly. Its map-framing guidance still says users drag the map to their desired frame and then lock the map area so the frame cannot move; `docs/FEATURE_PARITY.md` now records the implemented local workflow and the remaining canonical center/zoom persistence caveat.
- Added a canonical schema-14 **Lock map area** setting. Lock/unlock is one Undo/Redo transaction and survives JSON/ZIP/IndexedDB; schema 13 is rejected directly under the pre-release policy. While locked, every MapLibre movement handler (including two-finger pitch), built-in zoom buttons, Pan, Fit page, administrative-area fit and imported-content fit are disabled or consumed without delayed replay.
- Added explicit **Use my location** browser geolocation. Pending, permission-denied, unavailable, timeout, invalid-position, locked and retry states are accessible; stale browser callbacks and pending map commands are scoped to the current document epoch. The UI reports success only after the live map accepts the center command. Coordinates are transient and never written into the project.
- Strict RED/GREEN evidence included the absent lock control, unlocked MapLibre handlers, missing geolocation surface, generic permission failure, stale callbacks after lock/document replacement, false success before map application, locked fit movement, enabled NavigationControl zoom, and the schema-14 portable-open fixture dropping `locked`. Each focused regression failed before its minimal correction and passes now.
- Final targeted verification: 10 focused unit files / 121 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues. The exact Chromium geolocation/lock flow passes with real browser permission and verifies map application, complete movement gating, Undo recovery and a clean console. The exact Chromium portable-open flow passes with `locked: true` retained.
- Independent fail-closed review initially rejected five lock/lifecycle/schema paths. Its bounded recheck independently passed touch-pitch/zoom gating, every fit path, document-epoch containment and application-confirmed location status; the remaining missing `locked: true` portable-open assertion was then reproduced RED and passed after the fixture was corrected.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `8cdf0a35881bc512cdac655445922ccaad976d87087b57bb149d2e113f4a258d`. Live inspection reported a ready map, zero body overflow, gradients, shadows, console errors and page errors. Downscaled visual review found the new controls legible with no material clipping, overlap, blank map, toolbar collision or hierarchy defect.
- This is the fourth slice after the administrative-area full milestone, so the prior full unit/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Canonical viewport framing persistence (verified)

- Added schema-15 canonical map center and zoom alongside bearing, pitch and lock state. Finished manual map movement now commits one normalized six-decimal viewport transaction; Undo/Redo, JSON/ZIP, IndexedDB autosave/recovery and native print snapshots all consume the same document state.
- MapLibre starts every style lifecycle at the canonical camera. Style changes no longer replay an earlier browser-location request, while a fresh request with the same sequence number remains valid after project Open because request identity is scoped to the document epoch.
- Fit page remains a standalone undoable camera action. Administrative-area and imported-content fits amend their initiating layer transaction, so one Undo still removes the created/imported content and restores the previous framing instead of consuming a camera-only history step.
- All map-owned coordinates now fail closed at the Web Mercator latitude limit (`±85.051129`) before reaching MapLibre: browser geolocation, direct map requests, portable projects, GeoJSON, GPX/KML, POI spreadsheet rows, route/shape/POI store authoring and fit bounds share the same boundary. Wrapped world longitudes normalize into the canonical range.
- Applied the pre-release schema policy directly: schema 15 is current and schema 14 is rejected as obsolete; the Alpine fixture and every explicit browser schema assertion were updated atomically without a migration chain.
- Strict RED/GREEN evidence included the missing viewport store action, absent moveend publication, missing App wiring, schema-14 acceptance, non-Mercator camera/fit acceptance, wrapped longitude, renderer precision feedback, style-lifecycle initialization, location replay after style changes, direct invalid-location acknowledgement, document-scope request reuse and import-fit Undo being consumed by a separate camera transaction. Each focused failure passed after its minimal correction.
- The initial independent fail-closed review found two blocker classes: stale geolocation replay on style recreation and inconsistent non-Mercator validation. The one bounded recheck confirmed the broad remediation but caught one remaining direct invalid request being acknowledged without map movement; a final RED regression reproduced it and the boolean application boundary now withholds both applied state and success. Both reviews reported no security concern.
- Fresh final milestone verification on the current tree:
  - `npm test -- --run` — pass, 71 files / 565 tests.
  - `npm run typecheck` — pass.
  - `npm run lint` — pass, zero warnings.
  - `npm run doctor` — pass, no issues; telemetry disabled.
  - `npm run build` — pass; the known ~1.39 MB pre-gzip bundle warning remains.
  - `npm audit --omit=dev` — 0 vulnerabilities.
  - `npm run test:e2e -- --project=chromium --workers=1` — 40/40 pass serially, including geolocation → style → Save, schema-15 Open/style recreation and multi-file import Fit → one-step Undo.
- Browser evidence: `docs/screenshots/latest-desktop.png` remains a current exact 1440×900 capture, SHA-256 `8cdf0a35881bc512cdac655445922ccaad976d87087b57bb149d2e113f4a258d`. The rendered default viewport reports center `16.3725,48.2084`, zoom `11.2`, map-ready state, zero body overflow and an empty application console/page-error buffer. Downscaled visual review found no clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- `docs/FEATURE_PARITY.md` now records framing persistence as implemented and keeps provider-backed address search/road snapping explicitly gated by the accepted Mapbox display/storage decision. `docs/COMPLETE.md` remains absent because the final three-engine completion gate and remaining parity breadth are still open.

### 2026-08-23 — Elevation units and coherent profile styling (verified)

- Refreshed the live official elevation-profile editor. It currently exposes Meter/Kilometer or Feet/Miles, stroke/gradient, font size, elevation-marker and independent horizontal/vertical grid controls; `docs/FEATURE_PARITY.md` now distinguishes the implemented subset from remaining marker/font/gradient breadth.
- Generated route profiles now switch their chart summary and tick labels between metric and imperial units. The profile curve starts with the canonical route color, can be changed independently, and offers explicit area-fill plus horizontal/vertical grid controls.
- SVG, PNG and PDF downloads use the exact visible unit, curve, fill and grid choices. Export boundaries validate six-digit colors; the PDF retains its exact 150×75 mm page and all formats retain Copernicus DEM GLO-90 / Open-Meteo attribution.
- Strict RED/GREEN evidence: the first unit interaction could not find Imperial controls; export serializers remained metric; profile styling controls were absent; the first Chromium download retained the old curve/fill/grid settings; and malformed curve colors serialized unchecked. Each focused regression failed for the expected reason before its minimal correction and passes now.
- Fresh targeted verification: four focused unit files pass 18/18; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium profile flow passes 1/1 and structurally verifies styled SVG/PDF plus 1800×900 PNG output. `qpdf --check` reports no syntax or stream errors, and `pdfinfo` confirms one unencrypted PDF 1.7 page at 425.197×212.598 points.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `21829f5840a6745bc23deba4bb4ebcf5391e6a5ed8d6a00726e86bb2caaa1d0d`. It shows Imperial units, route-derived/custom curve color, fill/grid choices, chart, summary and downloads fully contained in the scrolled right sidebar. Live metrics report a ready map, zero body overflow, gradients, shadows, console errors and page errors; downscaled visual review found no material clipping, overlap, blank map or hierarchy defect.
- Independent fail-closed review passed with no security concern or logic error after inspecting the complete diff and independently rerunning the 18 focused unit tests, typecheck, lint and React Doctor. Its suggestions are non-blocking coverage depth for PNG styling pixels, initial route-color propagation and additional responsive setting interactions.
- This is the first slice after the canonical-viewport full milestone, so the prior 565-test/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Expanded Austrian state catalogue and safe connected merges (verified)

- Re-audited the clean integration worktree, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remain integrated or superseded, no branch was eligible for re-merge, and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Refetched the official Printmaps feature and home pages directly. The official workflow still documents countries, municipalities and regions with invert/fill/border controls, while the home page still advertises 300-DPI/vector output; the local parity matrix retains the strict municipality and raster-basemap caveats.
- Expanded the bundled Natural Earth 1:10m Austrian catalogue from Lower Austria/Vienna to eight exact-source state polygons: Burgenland, Carinthia, Lower Austria, Upper Austria, Salzburg, Styria, Vorarlberg and Vienna. The filtered coordinate payload SHA-256 is `5fa2cd1390bb08ad107700ffd3a13b2d302403dfba851286ab52298b726aceec`; an independent extraction check matched every coordinate to the retrieved upstream GeoJSON.
- Region IDs now use the source-standard ISO 3166-2 values. Connected selections still merge into one canonical undoable polygon and remove aligned internal borders; disconnected selections fail closed, keep authoring open and show an actionable border-sharing message rather than misrendering another exterior as a hole.
- Tyrol is deliberately withheld because the upstream geometry is a disconnected MultiPolygon. Flattening main and East Tyrol into one Polygon would be false geometry; canonical MultiPolygon support remains future work.
- Strict RED/GREEN evidence: the catalogue assertion first received only two legacy IDs; the disconnected-selection test first received a malformed two-exterior Polygon; and the interaction first exited authoring without an alert. Each focused regression passes after the exact-source catalogue, connectivity guard and recoverable UI state.
- Fresh targeted verification: 2 focused files / 12 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium administrative Save/layered-SVG/mobile flow passes 1/1 in 9.5 seconds. The upstream coordinate extraction check passes for all eight regions.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `985ef6fd90a26b01ab646cc27989977f9e117aa97e424eea9fe0585dfc88d67b`, and 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `68e1a08d1629c43753180a872915377fd564b911e5139185393c16255e86ca20`. Both show the two-column eight-state picker with Lower Austria/Vienna selected. Live metrics report a ready map, eight region controls, zero body overflow and no console/page errors.
- The initial independent fail-closed review passed with no security concern or logic error after independently rerunning the 12 focused tests, typecheck, lint, React Doctor and the exact-source verifier. Post-review mobile visual QA then caught truncated state names and MapLibre zoom controls stacking over the authoring panel. New browser assertions failed at a 177 px picker width and at the overlap hit test before the viewport-based width and authoring-panel stacking fixes. The one bounded independent recheck passed the exact Chromium flow and lint, confirmed all eight labels readable, the authoring panel topmost, toolbar/attribution clear and zero horizontal overflow, and matched the final mobile screenshot hash.
- This is the second slice after the canonical-viewport full milestone, so the prior 565-test/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Elevation markers and bounded profile typography/fill styling (verified)

- Refreshed the live official elevation-profile editor. Its current controls explicitly bound Font Size to `20–70`, expose independent curve-gradient and elevation-marker colors, and allow elevation markers to be disabled; `docs/FEATURE_PARITY.md` now records the implemented bounded subset without claiming gradient or arbitrary marker-density parity.
- The integrated route profile now shows labelled minimum/maximum markers, independent curve/fill/marker colors and a validated `20–70` font-size control. Invalid font drafts expose `aria-invalid`, retain the last valid preview and disable every download until corrected.
- SVG, 1800×900 PNG and exact 150×75 mm PDF consume the same units, colors, font size, fill/grid state and marker setting. PDF marker circles use vector paths and labels; SVG keeps semantic marker/fill groups. Export boundaries reject malformed colors and out-of-range/non-integer typography.
- Strict RED/GREEN evidence included the absent fill-color control, absent marker control, absent font-size input, default fill leaking into customized SVG, missing SVG/PDF markers and labels, unsafe appearance values, downloads remaining enabled for `71`, and a maximum-size left-endpoint label colliding with the export title. Each focused regression failed for the expected missing or unsafe behavior before its minimal correction and passes now.
- Fresh targeted verification: three focused files pass 21/21 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium profile flow passes 1/1 and covers invalid gating, live min/max markers, structurally matching SVG/PDF, 1800×900 PNG, mobile containment and a clean console.
- Fresh PDF evidence passes `qpdf --check`; `pdfinfo` reports one unencrypted PDF 1.7 page at `425.197×212.598 pt`, with no JavaScript. Rendered inspection found the purple `525 ft`/`692 ft` markers, amber fill, blue curve, title, summary and Copernicus/Open-Meteo attribution aligned and unclipped.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `f4a2b76aabfb7cd12e6b1d7670373e318288e2f2a2fb35b95bd16e498cc9152e`. Downscaled overview and right-sidebar crop review found all controls, chart, markers, summary, downloads and disclosure contained with no clipping, overlap, blank map, gradient, decorative shadow or toolbar collision. The persistent preview remains HTTP 200 on `127.0.0.1:4178`.
- The first independent fail-closed review rejected maximum-size left-endpoint title collision, near-edge horizontal marker clipping and an overflowing single-line PDF footer. RED-first regressions now move near-top labels below their markers independently of horizontal anchoring, clamp near-edge labels inside the plot and separate/bound SVG/PDF summary and attribution lines. The one bounded independent recheck passed with no security or logic blocker after independently rerunning all 12 export tests, typecheck and lint. Its only non-blocking suggestion is direct PDF/live-preview near-edge assertions beyond the current shared-layout and SVG regression. This is the third slice after the canonical-viewport full milestone, so the prior 565-test/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Canonical MultiPolygon and exact Tyrol workflow (verified)

- Re-audited the clean integration baseline, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remained integrated or superseded, no branch was eligible for re-merge, and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Rechecked the live official Printmaps feature and home pages. Countries/municipalities/regions, invert/fill/border controls and 300-DPI/vector claims remain documented; `docs/FEATURE_PARITY.md` now records the expanded local workflow without claiming a broad boundary database or fully vector basemap.
- Schema 16 adds bounded canonical `MultiPolygon` geometry across deep cloning/history, portable JSON/ZIP and IndexedDB validation, and strict GeoJSON import. Schema 15 is rejected directly under the pre-release policy rather than migrated.
- Added Tyrol as the ninth Austrian state from Natural Earth 1:10m Admin 1. Its exact source `MultiPolygon` retains main and East Tyrol as two parts with 332 and 102 positions; the retrieved upstream file SHA-256 is `22d0e3ad85eb3e27f17cabf8ba2d50e554fbc27a87796ff891d958185da62fb5`, and an exact coordinate comparison passed.
- The Shape picker creates Tyrol as one selected, auto-fitted, undoable layer without geometric distortion. The inspector explicitly reports two disconnected parts and withholds unsupported multi-part vertex controls. Tyrol is added separately; the existing aligned-border merge remains limited to connected single-part regions and now says so accurately.
- Live MapLibre, native PNG style snapshots, layered SVG and PDF preserve every part. Inversion uses one world polygon with every source exterior as a hole, then restores each original interior ring as its own filled `MultiPolygon` island; SVG/PDF retain equivalent even-odd geometry. Bounds include every disconnected part, and the separate outline source remains the only hit-test target for inverted shapes.
- Strict RED/GREEN evidence included missing Tyrol, store flattening to `Polygon`, portable/GeoJSON `MultiPolygon` rejection, undefined multi-part bounds, missing inverted exteriors, print-scene rejection, schema-15 acceptance, absent multi-part UI guidance and inaccurate merge recovery text. The initial independent fail-closed review then exposed interior holes being incorrectly left empty by a flattened world mask; a focused regression failed with `Polygon` instead of the required world-plus-islands `MultiPolygon`, then passed after the minimal correction.
- Final targeted verification: nine focused files pass 98/98 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues. The exact Chromium Tyrol flow passes and proves live rendering, two-part Save, invert state and layered-SVG output; the existing administrative merge flow also passed in the same targeted browser gate before the hole-only remediation. The bounded independent blocker recheck passed 23/23 map tests and confirmed holeless Tyrol behavior is unchanged.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `96b0a7fd1488e4c65f5236ac973b6a967192204f7f224e5e1a61ab91fd1c43ca`. A first mid-fit blank capture was rejected; the screenshot gate now waits for network idle. The final image visibly shows both Tyrol parts, correctly tinted surroundings, a complete basemap, the Geometry status and clean three-pane layout with no clipping, overlap, gradient or decorative shadow. Browser console/page-error capture is clean.
- This is the fourth slice after the canonical-viewport full milestone, so the prior 565-test/build/audit/full-Chromium evidence remains retained until the next verified slice triggers the five-slice milestone. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Direct expert-route reshaping (verified)

- Re-audited the clean main integration worktree, every registered worktree/branch, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remain integrated or superseded; no branch was eligible for re-merge and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Rechecked the current official Printmaps feature page directly. It continues to document Expert Mode map clicks, straight/arc/road-snapped lines and editable route appearance; `docs/FEATURE_PARITY.md` now records direct local reshaping without claiming the provider-gated road-snapping workflow.
- Selected unlocked routes now show one draggable map handle per vertex. Pointer movement previews the line through the live MapLibre source and commits one normalized six-decimal Undo/Redo transaction only at drag end; Arrow keys provide a keyboard nudge equivalent and retain focus after the canonical rerender. Locking or hiding the route removes the handles.
- The Vertices inspector adds explicit **Insert after** and **Remove** actions. Insertion chooses the geographic midpoint on the short side of an antimeridian segment, while removal refuses invalid targets, routes below two positions and results with fewer than two distinct positions. Locked or hidden routes expose the existing values but disable every geometry control.
- Strict RED/GREEN evidence included missing store actions, absent inspector controls, zero map handles, keyboard focus dropping after a canonical nudge, naïve antimeridian midpoint `0°` instead of `180°`, degenerate two-position removal and unreadable full-precision drag coordinates. Review remediation then observed RED for locked/hidden store mutation, hidden inspector controls remaining enabled, repeated Arrow nudges reusing the original coordinate and stale handle focus returning after route selection had left. Each focused regression failed for the expected reason before its minimal correction and passes now.
- Fresh targeted verification: seven focused files pass 97/97 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium route-editing flow passes and covers insertion, removal, pointer drag, lock gating, keyboard nudge/focus and console cleanliness.
- Fifth-slice milestone evidence: Vitest passes 73 files / 607 tests; production build passes with the known ~1.47 MB pre-gzip bundle warning; audit reports 0 vulnerabilities; the final serial Chromium gate passes 42/42 in 6.6 minutes. The first gate timed out only on the existing 90-second large multi-region PNG download, which then passed alone in 38.3 seconds. A second gate exposed Chromium dev-module resource exhaustion on the autosave test's third same-page reload; final verification now opens a fresh page in the same storage context, explicitly waits for renderer readiness after recovery and passes both alone and in the clean complete gate.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `81972f29ccbfb51359e22b362c2ae6a01e0f36f3ee3ac5aa7c2d2c65844d2c72`. It shows four route handles plus the compact insertion/removal controls and six-decimal coordinates. Downscaled visual review found no material clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- The independent fail-closed review rejected incomplete hidden/locked mutation gating, non-cumulative keyboard nudges, stale focus restoration and the screenshot overwritten by a later milestone test. Every blocker received a focused RED regression and minimal correction. The one bounded independent recheck passed all four blockers, independently reran the seven-file focused suite and directly verified the final screenshot dimensions/hash; no security concern remains. `docs/COMPLETE.md` remains absent because wider parity breadth and the final release gate are still open.

### 2026-08-23 — Exact-width elevation profile gradients (verified)

- Rechecked the live official elevation-profile editor. Its current settings expose a `50–300 mm` print width plus curve stroke, gradient and gradient-color controls; `docs/FEATURE_PARITY.md` now records the implemented bounded subset without claiming the remaining stroke toggle or travel-time estimates.
- The integrated profile now accepts only whole print widths from 50 through 300 mm. Invalid drafts expose `aria-invalid`, preserve the last valid preview and disable all downloads. SVG and PDF use the exact selected physical width at the existing 2:1 aspect ratio, while PNG scales deterministically at 12 pixels/mm; a 220 mm selection produces 220×110 mm vectors and 2640×1320 PNG.
- Added an optional two-color area gradient with an independently selectable second color. The live chart, SVG, PNG and PDF consume the same solid/gradient state; PDF uses a clipped axial shading and scales the complete established 150×75 mm design proportionally into the selected page instead of shrinking typography and marks relative to wider output.
- Strict RED/GREEN evidence: the print-width control was initially absent; SVG/PDF ignored 220 mm; PNG rasterization received no target dimensions; the gradient controls/preview were absent; vector exports emitted only a flat fill; and wider PDF content had no proportional page transform. Each focused assertion failed for the expected missing behavior before its minimal implementation and passes now.
- Fresh targeted verification: three focused files pass 26/26 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium flow passes and proves the 220×110 mm SVG, 2640×1320 PNG, exact 623.622×311.811 pt PDF, clipped PDF shading, mobile containment and a clean console. `qpdf --check` reports no syntax or stream errors and `pdfinfo` confirms one unencrypted PDF 1.7 page with no JavaScript.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `5ddfde4aec7ccdaa69fda81800fd37752cbc32eb19cc35d5b29c531cd0b29b37`. Downscaled review found the 220 mm width, gradient controls and amber-to-white chart fill contained with no clipping, overlap, blank map, unintended product gradient, decorative shadow or hierarchy defect. The persistent preview remains HTTP 200 on `127.0.0.1:4178`.
- The first independent fail-closed review rejected the live gradient preview because class CSS overrode its SVG presentation attribute, leaving the old flat fill visible while exports used a gradient. A focused regression failed on the missing inline gradient style before the minimal fix. The targeted Chromium flow now asserts the computed fill, the refreshed screenshot visibly shows the gradient, and the one bounded independent recheck passed with no security concern, logic error or suggestion.

### 2026-08-23 — Replace-in-place map-data geometry with style retention (verified)

- Re-audited the clean integration worktree, every registered worktree/branch, running Hermes processes, artifacts and the enabled preview before editing. Historical branches remain integrated, superseded or patch-equivalent; no branch was eligible for re-merge and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Refetched the official Printmaps feature and home pages directly. The feature page still says content layers can be removed, hidden, edited or replaced; `docs/FEATURE_PARITY.md` now records the local replacement contract and retains the missing pre-commit styling step for newly added batches.
- Preserved the contemporaneous `MISSION.md` / `docs/UI_BRIEF.md` product-coherence update: future inspector growth must use accessible progressive disclosure and focused shared primitives on the existing token/Tailwind stack rather than a wholesale component-library migration.
- Selected unlocked route, POI and shape layers now offer **Replace layer data** from the compact layer menu. One compatible single-feature GeoJSON/GPX/KML file replaces only canonical geometry while retaining ID, name, order, selection, visibility, lock state, opacity and appearance. Fit replacement/Keep current view remains explicit, and the complete change is one Undo/Redo transaction.
- Replacement reuses the bounded local parser and project limits, excludes the target's old geometry from capacity accounting, rejects basemap/locked/stale/malformed/type-mismatched targets at the store boundary and rejects multi-file or multi-feature replacement before commit. Invalid replacement leaves project geometry and history untouched.
- Strict RED/GREEN evidence included the absent store action, contradictory and out-of-Mercator same-type geometry being accepted, the missing layer-menu action, locked replacement remaining enabled, multi-file replacement presenting a commit instead of an alert, and a noninteractive success status intercepting the 390 px Properties trigger. Each focused regression failed for the expected reason before its minimal correction and passes now.
- Fresh targeted verification: six focused unit files pass 33/33; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the dedicated Chromium replacement file passes 2/2 and covers style/identity retention, fit, one-step Undo/Redo, focus restoration, 390×844 containment and all-or-nothing multi-file rejection.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `eef01b805329c99451cdb88f427c04fe3440ddf04cddef1a632934b63a87c864`. It shows the ready Liberty map behind a contained replacement review with the identity/appearance contract, file, fit choice and actions clearly legible. Downscaled review found no clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- The independent fail-closed review rejected three lifecycle/capacity paths: reviewed confirmation used the latest document identity, MultiPolygon positions were undercounted with no store aggregate backstop, and a cancelled replacement chooser could leak replacement intent into a later global review. Every blocker received an observed-RED regression and minimal fix. Reviewed batches now retain parse-time identity, nested geometry counting and a store backstop enforce the 200,000-position cap, and every add/drop preparation synchronously owns add mode. The one bounded independent recheck passed all three blockers after rerunning the exact three remediation files (19/19) and typecheck; no security concern remains. This is the second slice after the direct-route milestone, so the prior 607-test/build/audit/full-Chromium evidence remains retained under the five-slice policy.

### 2026-08-23 — Project-inspector progressive disclosure and readable design tokens (verified)

- Re-audited clean main, every registered worktree/branch status, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remain integrated, superseded or patch-equivalent; no branch is eligible for re-merge, no duplicate worker/test server was launched and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Replaced the flat Project inspector stack with one accessible chevron accordion language. Page and Map style remain open for the primary workflow; Camera & location, Map details, Provider services and Technical export start collapsed with live summaries such as `A4 landscape · 297 × 210 mm`, `Liberty · Local names · 100%` and `7 of 7 visible`.
- Disclosure uses native keyboard-operable buttons with `aria-expanded`/`aria-controls`, 44 px-plus mobile targets and local UI-preference persistence outside the canonical project. Selecting a layer and returning to Project preserves the user's disclosure choices. Blocked local storage falls back safely to the product defaults.
- Removed the duplicate Project eyebrow/title stack and raised shared desktop controls/body to 13 px, labels to 12 px, supporting text to 11 px and controls to 32 px while retaining the neutral one-accent, no-gradient/no-shadow system.
- Strict RED/GREEN evidence: the first focused disclosure test found no Camera & location button or summaries; the first design-token regression still received the old 28 px / 11.5 px scale. Both focused paths passed after the minimal accordion and token implementation.
- Fresh targeted verification: the related page/camera/disclosure/token set passes 4 files / 24 tests, the final disclosure/token rerun passes 2 files / 3 tests, typecheck passes, ESLint passes with zero warnings and React Doctor reports no issues. The single impacted Chromium flow passes 1/1 and covers production defaults, keyboard expansion, dynamic summaries, remount persistence, exact desktop/mobile containment, 44 px-plus touch height, zero horizontal overflow and a clean console.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `14518c42f77011c46bd0b20505e609eb5559edba01a20449989161fabc76f126`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `0656951d7009da40bd645bec47861a4d8745a5e44a0847abae02ab319621edb0`. Downscaled desktop and exact mobile review found the hierarchy, summaries, fields, toolbar and drawer readable with no material clipping, overlap, blank map, gradient or decorative shadow.
- The independent fail-closed review passed with no security concern or logic error after inspecting the complete staged diff. Its non-blocking suggestions are direct blocked-storage/Space-key regressions and narrower test-only preference seeding; production defaults remain covered by the dedicated unit and Chromium flows that explicitly remove every seeded key.
- This is the third slice after the direct-route full milestone, so the prior 607-test/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Pre-commit styling for reviewed map-data batches (verified)

- Re-audited clean main, every registered worktree/branch divergence, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remain integrated, superseded or patch-equivalent; no branch was eligible for re-merge, no duplicate worker/test server was launched and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- Rechecked the current official Printmaps feature page directly. It continues to document GPX/KML/GeoJSON upload plus adjustable route color/thickness, POI size/color/icons and shape color/transparency. `docs/FEATURE_PARITY.md` now records the local reviewed-batch styling contract without claiming address geocoding, per-feature pre-commit styling or wider provider-backed behavior.
- Multi-file add reviews now expose one bounded starting style per present content type: route color/width, POI color/size/shape and shape fill/outline/width. The complete styled batch remains one canonical Undo/Redo transaction; identity, geometry, ordering, fit/retain-view choice and parser limits remain unchanged.
- Invalid numeric drafts are marked with `aria-invalid` and disable the commit action until corrected. Styling state is scoped to the exact parsed batch, resets when files are replaced, stays outside project history until commit and is never offered during replace-in-place, which continues to retain the target layer's existing appearance.
- Strict RED/GREEN evidence: the pure appearance test first failed because `mapDataBatchAppearance` did not exist; the app interaction first found no route/POI/shape style groups; and mobile browser geometry then failed with the primary action ending at y=866.6 outside the 844 px viewport. The minimal helper/UI wiring and a three-row dialog layout made each focused regression pass.
- Fresh targeted verification: four import/style unit files pass 17/17; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the exact Chromium import-styling flow passes 1/1 and proves invalid gating/recovery, all three canonical style families, one-step Undo, desktop/mobile containment and clean page-error/console-error capture.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png` and `docs/screenshots/import-styling-20260823.png`, SHA-256 `00bef077a1ff91113fb3c76483230de0b30672d094f396dbdb133be44503256e`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `ed2e56468d84483706f6e449ef1c97a6352a006efc7ba70549d3f941b97f665e`. Downscaled independent visual inspection found the three groups readable, the mobile footer fully visible and no material clipping, horizontal overflow, blank map, gradient, decorative shadow or hierarchy defect.
- The independent fail-closed review passed with no security concern or logic error after inspecting every tracked/untracked implementation and evidence file, independently rerunning four focused files / 15 tests, and reviewing only downscaled desktop/mobile captures. Its suggestions for replacement-reset, malformed-style and focus-trap coverage are non-blocking; existing race/replacement tests plus the new invalid/recovery and mobile interaction flow remain green.
- This is the fourth slice after the direct-route full milestone, so the prior 607-test/build/audit/full-Chromium evidence remains retained until the next verified slice triggers the five-slice milestone. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Single accessible export format-choice flow (verified)

- Re-audited the clean integration baseline, all registered worktrees/branches, live Hermes/test processes, artifacts and the enabled preview before editing. Historical branches remained integrated or superseded, no branch was eligible for re-merge and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Replaced the four competing Export footer actions with one equal PNG/Layered SVG/PDF format chooser, a concise selected-output summary, a collapsed **Technical details** disclosure and exactly one format-specific primary download action beside Cancel. PNG remains the default and preserves its native-detail preflight, memory guard, progress and cancellation contract; layered SVG/PDF preserve their raster-basemap/named-vector-overlay contract.
- Format choice uses an accessible radio group with click, Arrow-key, Home and End behavior. Busy exports disable every non-cancel interaction and retain the established single-control focus trap. Unsafe PNG preflight focuses Cancel while SVG/PDF remain independently selectable. Project inspector summaries now use labelled/described accordion semantics, avoiding collisions with Portrait/Landscape and Export action names; the obsolete `Browser preview` output label is now the accurate `Format-specific` setting.
- Strict RED/GREEN evidence: the first component test failed because no Export format radiogroup existed. Review remediation then added a mobile browser assertion that failed at a 32 px control height; mobile Close, disclosure and footer actions now render at 45 px minimum, every measured export control is at least 44×44 px, and the undefined selected-format surface token was replaced with the defined accent-soft token.
- Fresh focused evidence: three export/page/disclosure unit files pass 32/32; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the exact Chromium format-choice flow passes with keyboard selection, one-primary-action semantics, 390×844 containment, 44×44 touch targets, zero horizontal overflow and clean console/page-error capture.
- Fifth-slice milestone evidence: Vitest passes 78 files / 629 tests; production build passes with the known ~1.49 MB pre-gzip bundle warning; audit reports 0 vulnerabilities. Serial Chromium reached 46/47 twice with different pre-existing runtime flakes (one invalid-GPX/KML chooser timeout and one delayed-autosave instrumentation timeout); both exact cases passed immediately in isolation. The final serial run's real A4 native PNG acceptance passed in 30.5 seconds, and the dedicated export flow remained green. These flakes remain recorded rather than misreported as a clean full-browser gate.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `33c0f94c4a90195532586492a31ddfc14184ee7bc01037e094242d2b29b61f5f`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `88dfb6ba62a34ddbac1ea9def395cc42a22baf373924076bbd8b8e2338db774f`. Independent downscaled desktop/mobile/crop review found the format hierarchy, output summary, disclosure and two-action footer readable with no clipping, overlap, gradient or decorative shadow.
- The fail-closed review rejected sub-44px mobile controls and identified the undefined selected-format token. Both received RED-first fixes; the one bounded independent recheck passed both blockers with no remaining security, logic or visual finding. Contemporaneous authoritative Felt-inspired updates to `MISSION.md` and `docs/UI_BRIEF.md` were preserved unchanged.

## Next unresolved slice

### 2026-08-23 — Felt-inspired inspector controls and responsive cohesion (verified)

- Re-audited clean main, all registered worktrees/branches, live Hermes/test processes, artifacts and the persistent preview before editing. Historical agent branches remain integrated, superseded or patch-equivalent; none was eligible for re-merge and `127.0.0.1:4178` remained the sole workspace preview with HTTP 200.
- The desktop Properties inspector is now 304 px at both 1440 px and the compact 900 px desktop boundary, with a stable 40 px two-column property rhythm. At 899 px and below the sidebars become working modal drawers instead of forcing a clipped 872 px three-pane grid; focus enters the drawer, Escape closes it, focus returns to the trigger and body overflow remains zero.
- Added shared native-semantic Checkbox and Switch primitives. Checked boxes use a near-black 16 px surface with a white Lucide tick; switches use the same near-black/white state language; focus, disabled and forced-colors states are explicit, and mobile rows are at least 44 px. Project details, layer behavior, route markers, administrative-region selection and elevation options now use the same controls instead of blue native checkboxes plus a separate blue toggle.
- Widened and warmed the neutral surface/field/divider palette while preserving the single blue interaction accent. Applied the contemporaneous authoritative divider-budget update in `MISSION.md` and `docs/UI_BRIEF.md`: inspector accordions and property sections now group through typography, spacing, alignment and borderless field fills rather than stacked interior rules.
- Replaced the functional layer menu's text ellipsis with a 16 px / 1.75 px Lucide icon, removed the inert Project-menu button and duplicate tiny Layer eyebrow, and replaced visible elevation summary arrows with accessible Lucide icons. A source-discipline regression prevents those Unicode menu/arrow glyphs from returning.
- Strict RED/GREEN evidence: the shared-control test first failed because `UiControls` did not exist; the integrated cohesion test first found the old text menu and checkbox semantics; desktop geometry first measured 272 px, then 240 px at 900; the divider assertion first measured 1 px; the 899 px drawer trigger was absent and then could not open until CSS and hook breakpoints matched; the no-op Project menu and Unicode elevation arrows each failed focused assertions before removal/replacement.
- Fresh targeted verification: 11 focused unit files / 84 tests pass; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the exact Chromium inspector flow passes with desktop/compact/mobile geometry, native keyboard semantics, drawer focus/Escape restoration, live checkbox/switch state, 44 px targets, zero overflow and clean console/page-error capture. The independent reviewer additionally passed 12 focused files / 86 tests, typecheck, lint and `git diff --check` without modifying the tree.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `a21c3896688ffde85683b6e6f011b039259a0b0aea5b2dda135a7f54209e6ae2`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `cbeb0395f319b534af2f4e9ffedefec124a9d509c48693bae345fd1284384bd4`. Independent downscaled desktop/sidebar/mobile review found clear borderless grouping, readable controls and no material clipping, overlap, gradient, decorative shadow or hierarchy defect.
- Final fail-closed review passed with no security concern, logic error, accessibility/interaction blocker or material visual defect. This is the first slice after the Export milestone, so the prior full-unit/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

### 2026-08-23 — Strict Export divider budget and quiet format tray (verified)

- Re-audited clean main, every registered worktree/branch status, live Hermes/browser/test processes, screenshot artifacts and the persistent preview before editing. Historical branches remain integrated, superseded or far behind current main; none was eligible for re-merge and `127.0.0.1:4178` remained the sole persistent workspace preview with HTTP 200.
- Replaced the three individually outlined export-format cards with one warm-neutral shared tray and a quiet selected surface. The selected state remains keyboard-operable and visibly distinct without borders or inset shadows.
- Removed the Export-only header/footer rules, Output border, Technical-details rules and Cancel border. Output retains a softly tinted grouping surface, Cancel is a ghost action, the dialog perimeter remains the sole structural border and exactly one dark format-specific primary action remains.
- Removed the visible idle `Ready` band without weakening announcements: the status live region stays mounted and empty/visually clipped while idle or after a format change, then receives progress, completion, cancellation and error text during real work.
- Strict RED/GREEN evidence: the exact Chromium flow first failed because the idle status node was still visibly populated. The first fail-closed review then rejected conditional live-region unmounting; a focused remediation assertion failed because the status element was absent, then passed after the stable empty region was restored.
- Fresh targeted verification: `tests/unit/app/app-export.test.tsx` passes 11/11; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the exact Chromium format-choice flow passes and covers keyboard selection, border/shadow measurements, idle live-region geometry, one-primary-action semantics, 390×844 containment, 44 px controls, zero horizontal overflow and clean console/page-error capture.
- Runtime inspection confirmed the dialog perimeter remains 1 px while Export header, Output, Technical details, footer and Cancel borders are all 0 px; the idle status is visually clipped, initial focus is on **Download PNG**, body overflow is zero and the persistent preview console/error buffers are empty.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `ff4bad9a0d9416dbe4e9825e9466a49fdb46c74a8031014970856b6f996f24f6`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `2119b98a4048460192437ff2391adda251aebdd1df04140a8689a180de7285fb`. Independent downscaled desktop/mobile review found the shared tray, borderless hierarchy, ghost Cancel and primary action calm and readable with no material clipping, overlap, blank map, gradient, decorative shadow or visual blocker.
- The independent fail-closed review reported no security concern or visual blocker but rejected the first idle-status implementation for unreliable live-region announcement. The single bounded recheck passed the stable-node remediation and independently reran the focused unit file plus exact Chromium flow. This is the second slice after the Export milestone, so the prior full-unit/build/audit/full-Chromium evidence remains retained under the five-slice policy. `docs/COMPLETE.md` remains absent.

Return to feature breadth next: deliver one bounded municipality/region catalogue workflow with exact-source provenance, canonical selection and existing live/native/SVG/PDF parity rather than extending UI-only polish.

### 2026-08-24 — Autosave-first project navigation and collapsed-only summaries (verified)

- Re-audited the integration worktree, every registered agent worktree/branch, live Hermes/test processes, artifacts and the persistent preview before editing. Historical branches remain integrated, superseded or far behind current main; none is eligible for re-merge, and `127.0.0.1:4178` remains the sole persistent workspace Vite preview with HTTP 200.
- Removed the misleading direct top-bar **Open**, **Save** and **Save ZIP** actions. The project-title control now opens one accessible File menu with **Open project**, **Download project** and **Download project archive**, using consistent Lucide icons and retaining the existing portable JSON/ZIP validation and download behavior.
- The File menu opens from click, Enter/Space or Arrow keys; supports Arrow/Home/End navigation and Escape restoration; restores focus after downloads, file chooser cancellation, valid Open and invalid/retry Open; closes on outside pointer use; and keeps every mobile menu action at least 44 px tall without horizontal overflow.
- Inspector accordion summaries now render and participate in accessible descriptions only while collapsed. Expanded Page/Map style/Camera/Map details headers are a compact one-title row, and the chevron aligns to the title baseline instead of spanning the title-plus-summary block.
- Strict RED/GREEN evidence: the focused component run first failed because `ProjectFileMenu` did not exist and expanded Page still exposed `A4 landscape · 297 × 210 mm`; both files now pass 4/4. A first Chromium menu run then exposed an ambiguous partial accessible-name locator, and the exact-name regression passed after correction. Screenshot QA rejected an early blank-map capture; the final evidence waits for `data-map-ready=true` before capture.
- Fresh targeted verification: six focused unit files pass 24/24; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; five focused Chromium flows pass serially and cover menu keyboard/focus, portable JSON, deterministic ZIP, validated Open, collapsed-only summaries, desktop/mobile containment and clean application console capture.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `3cfa06bc6803141728c4282d88969e3f0079f426b9943c39da18d0a1d1e7310b`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `015b04c4977352f2c9d92e02c84ffa8f340c5d5f53a5a588508987c0a60bf67f`. Downscaled desktop/mobile review found a ready live map, contained menu, clear hierarchy, consistent icons and no material clipping, overlap, gradient, decorative shadow or visual blocker. Live preview console and page-error buffers were empty.
- The independent fail-closed review found no security or visual blocker but rejected two interaction/layout paths: pointerdown on the title/chevron could dismiss and immediately reopen the menu, and file status was positioned against the narrow title wrapper. RED-first regressions reproduced both. Trigger-descendant containment now preserves the intended close toggle, while status uses a safe-area-aware fixed topbar position and a viewport width cap. The one bounded independent recheck passed both blockers and independently reran the exact project-file action tests; no blocker remains.
- This is the third slice after the Export milestone, so the prior full-unit/build/audit/full-Chromium evidence remains retained under the five-slice policy. A concurrent user-requested research pass added `docs/MAP_STYLE_PRESET_PLAN.md`; its original semantic-token style registry and accessible thumbnail gallery supersede municipality breadth as the immediate next slice. The bounded municipality/region catalogue remains queued afterward. `docs/COMPLETE.md` remains absent.

### 2026-08-24 — Original semantic-token map preset gallery (verified)

- Re-audited clean main, every registered worktree/branch divergence, live Hermes/browser/test processes, artifacts and the persistent preview before editing. Historical branches remain integrated, superseded or far behind current main; none was eligible for re-merge, and `127.0.0.1:4178` remained the sole persistent workspace Vite preview with HTTP 200.
- Replaced the three-style dropdown with twelve original canonical presets across Minimal, Editorial, Dark, Soft, Natural and Playful families: Paper, Graphite, Porcelain, Sandstone, Night Ink, Blueprint, Sea Glass, Rosewater, Alpine, Coastal, Terracotta and Signal. Every preset owns the same eleven semantic cartographic roles and is generated from the same compatible OpenFreeMap Bright layer structure without Mapiful names, code, images, CSS or tile endpoints.
- Added a responsive accessible gallery with theme-family filters, a 3-column desktop / 2-column mobile grid, deterministic first-party 216×144 Vienna thumbnails, distinct focus/selected states and Lucide checkmarks. Arrow/Home/End move focus without changing the map; Enter/Space/click selects immediately as one Undoable transaction. Desktop filters are at least 32 px high and mobile filters are at least 44 px.
- Schema 17 is the single current project format. Schema 16 is rejected as obsolete under the pre-release policy. Portable JSON, deterministic ZIP and IndexedDB use the canonical preset ID; the impacted Chromium flow directly downloaded a schema-17 Night Ink project and a layered SVG with the named `Night Ink basemap` group.
- Live MapLibre switching preserves the established language, text scale, map-detail visibility, camera, content and export boundaries. All twelve generated styles share an identical layer-ID sequence and embed semantic-token provenance; layered SVG/PDF continue to state the honest raster-basemap limitation.
- Strict RED/GREEN evidence: the registry test first failed because `mapStylePresets` did not exist; generated-style verification first failed on missing `paper.json`; the gallery tests first found the old combobox/no radiogroup; schema validation first rejected Night Ink and accepted schema 16; the browser interaction first measured 28 px desktop filters against the required 32 px. Each focused failure passed after its minimal implementation.
- Fresh targeted verification: nine related unit/component files pass 83/83; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the single impacted Chromium gallery/project-download/layered-SVG/mobile flow passes 1/1 with exact thumbnail dimensions, 3/2-column geometry, one-step Undo, 390×844 containment and clean console/page-error capture.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `6d9ff637227158973451a1e9d05bde68caae7aea3903e644d9d448a439d79a96`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `be0d084ad593a6b949a4a820219029356a8995602a19165dac0a9780688c2eaa`. Independent downscaled desktop/mobile review found the Night Ink map, selected state, filters, thumbnails and inspector hierarchy readable with no material clipping, overlap, border clutter, gradient, decorative shadow or visual blocker. A twelve-thumbnail contact sheet was nonblank, distinct and free of crop defects.
- The first fail-closed review rejected a predictable shared thumbnail-capture temporary path and two stale project-download assertions that still expected schema 16. The exact two Chromium project JSON/ZIP flows then failed RED with received schema 17 versus expected 16 and now pass 2/2. The bounded recheck confirmed the assertions, then found cleanup could still be skipped before or during browser close; the final script places every post-`mkdtemp` operation inside an outer `try`, nests close/removal `finally` blocks and passed an intentional missing-`magick` failure cleanup probe. A final independent cleanup-only check passed with no remaining blocker.
- This is the fourth slice after the Export milestone, so the prior full-unit/build/audit/full-Chromium evidence remains retained. The next bounded municipality/region workflow will be the fifth slice and must trigger the milestone batch gate. `docs/COMPLETE.md` remains absent.

### 2026-08-24 — Attributed Vienna municipal-district catalogue (targeted verification complete)

- Re-audited clean main, every registered worktree/branch, live Hermes/browser/test processes, artifacts and the persistent preview before editing. Historical branches remain integrated, superseded or far behind current main; none was eligible for re-merge, and `127.0.0.1:4178` remained the sole persistent workspace Vite preview with HTTP 200.
- Rechecked the official Printmaps feature page directly on 2026-08-24. Its country/municipality/region selection and fill/invert workflow remains documented. The new bounded local municipality tier contains all 23 Vienna districts from the City of Vienna OGD `BEZIRKSGRENZEOGD` WFS response; WFS capabilities identify Open Government Data Wien, CC BY 3.0 AT and the source access-constraints URL.
- The WFS response includes volatile metadata and feature ordering, so reproducibility uses the canonical sorted district-number/name/geometry payload SHA-256 `dc08d94a2581d4dd70d57e5b010827cd211ea851fe6598c9325a603f739ba35f` rather than unstable raw response bytes. The deterministic generator validates that payload, all 23 Polygon features, coordinate/ring bounds, unique IDs, a 500-position ring limit and a 0.3% area-change ceiling before applying documented `0.00008°` simplification. Two independently fetched raw responses generated byte-identical 56,343-byte catalogue output.
- The Shape tool now offers Country, Region and Municipality levels. Municipality exposes all 23 Vienna districts with direct source/license links; adding one creates a selected, auto-fitted, one-step Undoable canonical shape that immediately participates in the existing appearance, live MapLibre, native PNG, portable JSON/ZIP and vector-overlay export paths.
- Licensed district credit is appended to native-PNG preflight/composition plus layered SVG and PDF attribution whenever a Vienna district layer is present. The focused Chromium flow creates and inverts Innere Stadt, downloads schema-17 JSON, verifies bounded canonical geometry and a credited layered SVG, then proves 390×844 picker containment and zero body overflow.
- Strict RED/GREEN evidence: the catalogue test first received zero municipalities; the app test could not select the missing Municipality level; print tests first lacked the City of Vienna credit; and accessible source/license links were initially absent. Every focused failure passed after its minimal implementation.
- Fresh targeted verification after review remediation: three focused unit files pass 22/22; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the final impacted Chromium flow passes 1/1 in 14.2 seconds with clean console/page-error capture.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `2f0b8b85eedaafb64abe58cb7009b0f5454c477e0d3765dab87b929c8759e005`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `d2f4858f6ab85736af509271b453ce57a450e34e4184ee99ede32fb1d45cd5cd`. Downscaled review found the selected/inverted Innere Stadt boundary and mobile municipality picker readable with no material clipping, overlap, horizontal overflow, blank map, border clutter, gradient or decorative shadow.
- The independent fail-closed review rejected the unstable raw-response hash gate and the combined City/basemap credit overflowing narrow custom SVG/PDF pages. The generator failure was reproduced against a fresh semantically identical WFS response; canonical hashing now produces identical output across both captures. A focused 100 mm custom-page regression first received the fixed `2.5 mm` / `6 pt` fonts and now scales both single-line credits to the available width. The single bounded independent recheck passed both blockers with no security or logic concern.
- The first fifth-slice milestone unit run exposed two stale pre-navigation assertions that still queried the removed **Project menu** button; the exact file failed 2/4 before aligning its blur target with the current project-title File menu, then passed 4/4. The first complete Chromium run exposed a real shared-control defect in four unrelated workflows: the white Lucide checkbox tick intercepted pointer actions aimed at the native checkbox. A source-discipline regression first failed on the missing pointer rule; decorative checkbox/switch visuals now ignore pointer events, the four exact browser regressions pass 4/4, and the subsequent complete Chromium gate passes 50/50.
- Final milestone verification on the current tree: Vitest passes 81 files / 640 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known 1.55 MB pre-gzip bundle warning; `npm audit --omit=dev` reports 0 vulnerabilities; serial Chromium passes 50/50 in 8.5 minutes. `docs/COMPLETE.md` remains absent pending the final three-engine release gate and wider mission review.

### 2026-08-24 — Portable project-file sharing handoff (verification complete, pending final review)

- Re-audited clean main, every registered agent worktree/branch divergence, live Hermes/browser/test processes, artifacts and the persistent preview before editing. Historical branches remain integrated, superseded or far behind current main; none is eligible for re-merge, and `127.0.0.1:4178` remains the sole persistent workspace Vite preview with HTTP 200.
- Replaced the inert top-bar **Share** control with a real client-side handoff. Browsers that support Web Share file payloads receive the current validated `.printmap.json` project; unsupported browsers download the same editable portable file with an explicit send-it guidance status. Native share cancellation is a neutral status rather than an error, failures remain actionable, duplicate activation is disabled while pending and focus returns only after the control is re-enabled.
- Sharing captures current canonical state at activation. The Chromium flow injects the Web Share file-payload contract, changes the project to Portrait, then proves the handed-off file is schema 17 with `210 × 297 mm` portrait settings and the expected sanitized filename. It removes that injected support at runtime and proves the real fallback download; it does not claim to open or automate an operating-system share sheet.
- Share is now reachable at 390 px instead of being hidden with secondary quiet actions. Share and Export both provide aligned 44 px mobile hit targets, and the page retains zero horizontal overflow.
- Strict RED/GREEN evidence: the first unit run failed because `ProjectShareButton` did not exist; the fallback test then received zero downloads; native dismissal surfaced an alert; real Chromium focus remained inactive after completion; mobile Share was absent; and the final responsive assertion measured Export at 30 px. Review remediation then reproduced focus theft when a user moved to another control while sharing remained pending. Each focused failure passed after its minimal correction.
- Fresh targeted verification: two related unit files pass 7/7; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the exact Chromium project-navigation file passes 2/2 and covers the mocked Web Share payload contract, cancellation/error handling, real fallback download, canonical payload, conditional focus restoration, desktop/mobile File menu, 44 px action balance, zero overflow and clean console/page-error capture.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `4efbfd5cb14a786f3f7e697e49bfc6bc43be66ec0b93cccedaaa76d5e51dde21`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `72610b7f9b8fcec709131db2e8c4ac93973391970cd918c4f92a583e652fe3af`. Downscaled desktop/mobile review found the File menu, Share/Export actions, map and inspector hierarchy contained and readable with no material clipping, overlap, horizontal overflow, border clutter, gradient or decorative shadow.
- The initial independent fail-closed review rejected unconditional async focus restoration and wording that could be read as automating the native operating-system share sheet. Focus now returns only from a neutral/owned target and never steals an intentional move; the evidence now distinguishes the injected Web Share contract from the real browser download fallback. The bounded independent recheck passed both blockers and independently reran the five share-button regressions with no remaining security or logic concern.
- This is the first slice after the Vienna municipality milestone, so its 640-test/build/audit/full-Chromium evidence remains retained under the five-slice policy. The next slice is the final three-engine nominal completion flow and wider mission-gate review. `docs/COMPLETE.md` remains absent.

### 2026-08-24 — Preview-first style gallery and exact accordion alignment (targeted verification complete)

- Live inspection reproduced the reported sidebar defect rather than inferring it from CSS: every expanded accordion chevron center was 5 px above its title center at 1280×577, and collapsed summaries were auto-placed in the chevron column. The textual theme-family toolbar occupied two rows in the 304 px inspector, and the gallery repeated `Preview data © OpenStreetMap contributors · OpenFreeMap` below thumbnails even though the live map already exposed its required attribution control.
- Accordion grid placement is now explicit: chevron and title share the first row and measure a 0 px center delta in every expanded/collapsed section; collapsed summaries occupy the second row aligned exactly to the title column. The 12-style gallery is directly preview-first at 3 desktop / 2 mobile columns, with existing concise single-line card labels, full accessible names, selection check, Arrow/Home/End behavior and Undo/export semantics preserved.
- Removed only the redundant textual family-filter toolbar and gallery-level attribution. Mandatory provider/data attribution remains visible on the live MapLibre map and remains unchanged in PNG/SVG/PDF output contracts.
- Strict RED/GREEN evidence: the new Chromium geometry test first received a 5 px center delta against the ≤1 px contract; the preview-first test first found one theme toolbar; and the focused component test first failed looking for the removed Dark filter. Each passed after the minimal implementation and expectation update.
- Fresh targeted verification: two related unit files pass 4/4; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; the map-gallery plus visual-cleanup Chromium flows pass 3/3 serially with all 12 thumbnails, desktop/mobile containment, exact alignment, live map attribution and clean gallery hierarchy.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `3615bdfba138d732e5eb5f3a2cfefee3d86fffbde395c59b47ae2e8e7017348f`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `1b416a33739dd8cbc533f5a39a04bbab2c82a4b3b7f2ba35511268fb8499499a`. Visual review confirms aligned titles, a direct preview grid, no duplicate gallery attribution and no material clipping or horizontal overflow.
- Independent fail-closed review approved the staged slice with no security, logic, accessibility, interaction, visual or test-coverage blocker. It independently reran three focused unit assertions, measured 0 px chevron/title deltas and exact summary indentation in both disclosure states, confirmed all 12 presets plus keyboard activation, and verified the staged screenshot dimensions/hashes.
- The longer final three-engine release gate was paused to avoid concurrent worktree edits. Its unrelated unstaged runner/config/test changes were preserved outside this slice and must be resumed only after this focused commit. `docs/COMPLETE.md` remains absent.

### 2026-08-24 — Final three-engine nominal workflow and release gate (verification complete, pending final review)

- Resumed the preserved release-gate slice only after the preview-first gallery commit left main clean. Re-audited every registered worktree and branch, live Hermes/browser/test processes, artifacts and the persistent preview. Historical agent branches remain integrated, superseded or behind main; no branch is eligible for re-merge, no duplicate worker/test server was launched and `127.0.0.1:4178` remains the sole persistent workspace preview with HTTP 200.
- Added one complete nominal browser workflow that changes the open-map style and page, draws and styles a route, imports GeoJSON, waits for autosave, reloads and explicitly recovers, downloads and reopens the portable project, then exports structurally checked layered SVG, PDF and a lightweight exact-size PNG. The same flow passes in headed Chromium, Firefox and WebKit with captured application console/page errors empty and a final 390×844 zero-overflow assertion.
- The release runner now executes all three engines serially under Xvfb with one worker and three bounded shards per engine. Firefox uses a headed software-rendered release path so the nominal workflow and every renderer-dependent editor case exercise real WebGL rather than accepting the headless fallback.
- Hardened only test boundaries exposed by the release run: delayed autosave scenarios use fresh pages in one storage context, style-abort tests await the intercepted request, renderer-dependent assertions await readiness after state changes, coordinate-field Tab coverage is bidirectional, and map-gallery startup safely discards a prior local draft. No production behavior was weakened to make the gate pass.
- Fresh final verification before review remediation: Vitest passes 82 files / 645 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known 1.55 MB pre-gzip bundle warning; `npm audit --omit=dev` reports 0 vulnerabilities. `npm run test:e2e` exited 0 with Chromium 54/54, Firefox 48 pass / 6 legacy skips and WebKit 54/54. Review then rejected those unconditional Firefox skips as dishonest under the headed release runner; all six skip gates and two sibling Firefox assertion omissions were removed. The complete affected Firefox editor/autosave/project-files set now passes 23/23 with typecheck, lint and React Doctor clean. A fresh complete all-engine gate is running before commit.
- One React development warning appeared once between Firefox shard tests in the first successful release run. It did not enter the nominal workflow's captured console, and the complete affected Firefox shard reran 18/18 with clean process output; the six-test Firefox layer-appearance file and both neighboring exact cases also reran clean. This remains recorded as a non-reproducible lifecycle diagnostic rather than hidden.
- Browser evidence: refreshed exact 1440×900 `docs/screenshots/latest-desktop.png`, SHA-256 `07bf053324bb0c200440156de8425b698e46633ee960112ce466facdb2fe946f`; refreshed 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `879ffb7c304b454f0223da7a30c4a76ab28aed615e7700a4302820118468addb`. Downscaled desktop/mobile review found a ready map, contained inspector/municipality controls, clear hierarchy and no material clipping, overlap, horizontal overflow, border clutter, gradient, decorative shadow or visual blocker.
- `docs/COMPLETE.md` remains absent while the wider feature-parity campaign continues. The next slice is the authorized Mapbox-backed address search and road-snapped routing workflow, with canonical editable geometry, Undo/Redo, autosave/portable persistence, export parity and open-map fallback.

### 2026-08-25 — Truthful editing tools, direct geometry editing and mobile touch release gate (verification complete, pending final review)

- Removed the selectable no-op Text tool and renamed generic Shape authoring to the clearer **Area** workflow. Route, Pin and Area authoring clear stale selection, expose Undo-last-point, and the advertised V/H/R/P/S plus Shift+1 shortcuts now work without intercepting text editing, modifiers, IME composition or repeated key events.
- Finished custom areas default to explicit **Points** mode with draggable vertices, midpoint insertion, keyboard nudge, valid deletion and focus restoration; **Transform** remains a separate whole-area move/resize mode. Selected routes retain direct vertex editing and now add click/drag midpoint insertion. Locked, hidden, multipart or unsuitable geometry stays non-editable.
- Mobile touch remediation uses compact visible handles inside 44×44 px coarse-pointer targets with `touch-action: none`. Exact map hit testing remains first; a 12 px fallback radius makes thin routes selectable by a finger without adding rendered/export layers. A trusted Chromium touch flow proves direct area selection/move and near-line route selection/vertex movement.
- Release remediation also restores focus to the replacement layer menu after mobile duplication, registers map-data drop listeners before paint, and hardens dynamically imported startup: one bounded reload is allowed, then an actionable **Reload application** alert replaces a blank screen. Deterministic browser tests cover both bootstrap outcomes.
- React Scan on the final editing path recorded zero unrelated `LayerRow` renders during a real geometry nudge. Scanner initialization remains development-only and the production bundle contains no React Scan identifiers.
- Final non-browser gate on the exact candidate: Vitest passes 98 files / 715 tests; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; production build passes with the known ~1.58 MB pre-gzip main chunk warning; `npm audit --audit-level=high` reports 0 vulnerabilities; `git diff --check` passes.
- Final serial release matrix exits 0: Chromium 66/66, Firefox 65 pass / 1 intentional Chromium-CDP touch-harness skip, and WebKit 65 pass / the same skip. Standard mobile drawers, map selection, route/area editing, autosave/recovery, import/drop, startup recovery, persistence and export workflows pass in Firefox and WebKit.
- Browser evidence remains `docs/screenshots/latest-desktop.png` and `docs/screenshots/latest-mobile.png`; live visual review found compact aligned point/midpoint handles, a non-overlapping Points/Transform control and no material clipping, gradient or decorative shadow. `127.0.0.1:4178` remains active and healthy.
- Final independent fail-closed review remains the only blocker before the verified commit. `docs/COMPLETE.md` remains absent because provider-backed address search and road-snapped routing remain the next broader parity slice.

### 2026-08-25 — Provider-backed travel-time Areas and release hardening (verification complete, pending final review)

- Added browser Mapbox location search plus a travel-time Area workflow under the existing Area tool. A map click or accessible search result selects one shared center; walking, cycling and driving support whole 5–60 minute durations, with automatic debounced search, request cancellation and stale-document suppression.
- The Isochrone v1 boundary now requires an exact FeatureCollection/Feature envelope, matching contour duration, one bounded closed Polygon and finite in-range coordinates. Caller cancellation remains distinct from the bounded 15-second timeout and existing actionable provider failures. RED regressions first accepted a non-FeatureCollection and a non-Feature contour before strict normalization rejected both.
- A successful response commits one selected first-class shape before the basemap with canonical geometry, ordinary fill/outline/opacity/inversion controls and compact Mapbox/service/center/profile/minutes provenance. One-step Undo/Redo, IndexedDB recovery, portable project parsing and offline SVG/PDF/PNG paths use canonical state without replaying the request; required `© Mapbox` print attribution is derived from provenance.
- The intercepted browser acceptance selects Vienna by keyboard search, generates a 20-minute cycling Area, inspects the saved schema-18 project, exercises Undo/Redo and autosave recovery, then downloads a credited layered SVG while proving the provider was called once. It passes in Chromium, Firefox and WebKit.
- Release verification exposed a real route-marker lifecycle defect unrelated to the Area geometry: an unstable callback identity recreated accessible drag markers between browser actions. A focused unit regression first observed two sessions instead of one. The hook now retains one session while routing commits through its latest callback ref; the unit regression and Chromium/Firefox/WebKit direct-route flows pass.
- Final non-browser gate on the exact candidate: typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues; Vitest passes 107 files / 763 tests; production build passes with the known ~2.33 MB pre-gzip main chunk warning; `npm audit --audit-level=high` reports 0 vulnerabilities; `git diff --check` passes.
- Browser release evidence is serial and resource-bounded: Chromium passed 67/67 before the lifecycle correction and the impacted final Chromium route flow passes; final Firefox shards 2 and 3 pass 21 plus one intentional Chromium-CDP touch-harness skip and 22/22, with the unchanged shard 1 previously clean; final WebKit shard 2 passes 21 plus the same intentional skip and shards 1/3 previously pass 23/23 and 22/22. The earlier monolithic WebKit attempt was OOM-killed and replaced by the repository's bounded three-shard policy. One complete `npm run test:e2e` attempt exposed the marker race before the fix rather than producing a false clean result.
- Desktop/mobile screenshot inspection found a ready, readable map/editor with no material clipping, horizontal overflow, blank map, gradient or decorative shadow. The search control is contained at desktop width; the retained mobile evidence keeps the modal Properties drawer and tool/header actions within 390×844.
- Focused staleness hardening on 2026-08-25 found that selecting a different center while generation was pending left the old request live and could commit an Area for the superseded center. The exact test `vitest run tests/unit/app/isochrone-authoring.test.tsx -t "cancels an in-flight generation when the center changes"` first failed because the captured signal remained un-aborted, then passed 1/1 after center changes began invalidating and aborting the request, clearing busy/error state, and retaining the newly selected center. No broad suite or browser matrix was run under the exact-test iteration policy.
- The next three focused cancellation slices now prevent stale work after changing travel mode, changing duration, or leaving and reopening Area authoring. Each exact named Vitest regression first failed on the unsafe state (`AbortSignal.aborted === false` for mode/duration; stale `isGenerating === true` after reactivation), then passed 1/1 after the hook began invalidating the request and clearing pending/error state for each transition. No browser behavior or visible layout changed, so the retained screenshots remain current.
- A final focused document-lifecycle regression first retained `Old document center` after the project epoch changed, then passed 1/1 after the hook cleared its ephemeral center at the epoch boundary. A separate deactivation-race regression disables passive effects and proves a successful provider response cannot commit between authoring deactivation and passive cleanup; it failed before current active/document identity moved into the synchronous layout-effect boundary and now passes.
- The first fail-closed release review rejected three real defects: canonical Arc routes were absent from native PNG maps, valid Arc routes without an explicit appearance were invisible, and the isochrone deactivation race could still commit stale work. RED regressions now require default route appearance, a native raster Arc overlay and synchronous deactivation suppression. The first Arc remediation interleaved the live overlay and caused the exact Chromium route export to fail while restoring layered-SVG overlays; the corrected split keeps the live overlay separate and adds an interleaved Arc overlay only to temporary native raster maps. The exact Chromium route flow then passed 1/1, including layered-SVG restoration.
- The same review found that the excessive-search-feature test mocked `Response.json()` even though the bounded request primitive reads the body stream. The corrected regression sends a real JSON response body and asserts the specific `too many place-search features` failure before any entry is accepted.
- Final Chromium-only milestone verification on the remediated tree exits 0: typecheck; ESLint with zero warnings; React Doctor with no issues; Vitest 108 files / 784 tests; production build with the known ~2.34 MB pre-gzip main chunk warning; `npm audit --audit-level=high` with 0 vulnerabilities; `git diff --check`; and serial Chromium 67/67 across three one-worker shards. Firefox and WebKit were not run, per the project-owner policy.
- The bounded independent recheck passed with no security concern, logic error or residual suggestion. It independently confirmed the live/native Arc split, default appearance, layout-effect isochrone guard and real streamed search-limit fixture; its four targeted files passed 20/20 without modifying the tree.
- `docs/COMPLETE.md` remains absent because road-snapped provider routing remains wider completion work. Scheduler job `5c693a328765` is enabled and must remain enabled until every completion gate passes with a verified commit.

### 2026-08-26 — Bounded Mapbox Directions provider foundation (targeted verification complete)

- Added a browser-token-safe Mapbox Directions v5 adapter behind the existing provider-neutral `DirectionsProvider` contract. It accepts 2–25 distinct valid waypoints and walking/cycling/driving profiles, requests one full GeoJSON route with alternatives/steps disabled, bounds the response to 50,000 valid Mercator positions, detaches canonical coordinates, records finite distance/duration and preserves caller cancellation versus the 15-second timeout boundary.
- Updated the successful Provider services state to reflect the owner-authorized reality without overstating implementation: the verified public-token/origin browser connection is ready for provider requests, search and travel-time Areas are active, and road-routing integration is explicitly next. The token remains deployment-only and is absent from DOM text, screenshots and project artifacts.
- Strict RED/GREEN evidence: `vitest run tests/unit/mapbox-directions.test.ts -t "requests one bounded road route and returns detached canonical geometry"` first failed because the provider module did not exist, then passed 1/1 after the minimal adapter. `-t "rejects routes without two distinct waypoints before requesting"` first reached the mocked fetch and failed with the wrong error, then passed 1/1 after pre-request distinctness validation. The fail-closed review's four blockers each received a focused RED test before correction: non-object envelopes now map to `RESPONSE_INVALID`; malformed runtime waypoint arrays fail as `REQUEST_INVALID` before fetch; the first abort source is latched so caller cancellation cannot become a later timeout; and provider-status copy no longer claims road routing is already wired. The bounded recheck then reproduced a sparse-array hole reaching fetch; its exact RED regression passes after validation densifies the waypoint list before inspection.
- Targeted verification: the five exact remediation tests each pass 1/1; typecheck passes; ESLint passes with zero warnings; React Doctor reports no issues after the URL construction received an explicit `URL.canParse` guard. No full unit suite, build, audit or browser matrix was run under the focused iteration policy.
- Browser evidence: fresh exact 1440×900 Chromium screenshot `docs/screenshots/mapbox-directions-foundation-20260826.png`, SHA-256 `e118e30cff9a9b2aabc3bd54d12fff7e51ee4932dd0b55f44546771448be29a2`. It shows the ready live map and visible **Mapbox connection ready** surface with active search/travel-time Areas and road-routing integration explicitly next, zero body overflow and no console/page errors; React Scan evidence overlays were disabled before capture. Visual review found no material clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- The initial fail-closed review rejected malformed envelope/input classification, abort-source precedence and readiness overclaiming. The first bounded recheck found the remaining sparse-array bypass; its RED-first correction received a final independent pass with no security concern, logic error or suggestion.
- This is the first road-routing slice after the travel-time milestone. The next slice must wire the adapter through route authoring into canonical snapped `LineString` project state with compact provenance, one-step Undo/Redo, autosave/portable persistence and export parity while preserving existing two-anchor `Arc` geometry unchanged. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled.

### 2026-08-26 — Canonical Mapbox road routing (verified)

- The Route tool now offers **Road** beside Straight and Arc. Two or more map waypoints request one bounded Mapbox Directions route for driving, walking or cycling; unsupported marker profiles cannot be submitted. Existing Arc authoring remains canonical two-anchor `Arc` geometry and never enters the Directions path.
- A successful response becomes one selected, editable schema-19 `LineString` layer with compact `directions-v5` provenance (original waypoints, profile, distance and duration). Provider coordinates are normalized to six decimals before Terra Draw receives them, preventing excessive-precision renderer failure. The right inspector identifies Mapbox Directions, summarizes `9.2 km · 22 min · 2 waypoints`, and states that canonical geometry remains editable/exportable offline.
- Requests abort and suppress stale commits when waypoints, route mode, tool state or document identity changes. Canonical store validation independently enforces 2–25 distinct waypoints, 2–50,000 valid route positions, supported profile alignment and finite non-negative metrics. Portable parsing accepts directions provenance only on `LineString` routes and rejects credentials/raw-response fields by construction.
- Existing project attribution derives `© Mapbox` from compact provenance, so native PNG, layered SVG and PDF use local canonical geometry without replaying Directions. The focused Chromium acceptance edits one provider route, exercises Undo/Redo, downloads schema-19 JSON, checks credited layered SVG and proves the provider was called exactly once.
- Strict RED/GREEN evidence: the first interaction could not find **Road**; portable parsing rejected route provenance; authoring deactivation retained a pending state; waypoint changes left an old request live; malformed runtime options threw; an Arc accepted Directions provenance; provider status still said routing was next; and real Chromium rejected excessive coordinate precision and blanked the renderer. Each exact regression passed after its minimal correction.
- Final focused evidence on the staged candidate: exact route creation and waypoint-cancellation Vitest cases pass 1/1 each; exact directions lifecycle, store rejection and portable provenance cases pass 1/1 each; `npm run typecheck` passes; ESLint passes with zero warnings; React Doctor reports no issues; `git diff --cached --check` passes; and the single Chromium road-routing flow passes 1/1 in 14.6 seconds. No full unit suite, build, audit, Firefox or WebKit result was used as final acceptance under the owner-required iteration policy. The first review worker nevertheless ran prohibited broad commands before its verdict; those outputs were treated only as defect discovery, and no broad rerun followed remediation.
- The first independent fail-closed review rejected stale schema handling for version 18 and three unchanged shape-authoring assertions that still expected schema 18. The exact schema-18 regression reproduced the obsolete-file message failure before the constant-based correction; all three Chromium assertions now expect schema 19. The one bounded independent recheck passed with no security concern, logic error or suggestion after inspecting the staged remediation and rerunning only the exact schema regression.
- Browser evidence: fresh exact 1440×900 Chromium screenshot `docs/screenshots/road-routing-20260826.png`, SHA-256 `908e3ee2376d589df4af9e2d391b0860011651a66fdc18db4733106826f048da`. Visual review found the selected route, three editable handles, compact provenance, map and inspector hierarchy fully contained with no material clipping, overlap, horizontal overflow, blank map, product gradient or decorative shadow; console/page-error capture was clean.
- The next provider breadth slice is explicit address-search-result handoff into route waypoints. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled.

### 2026-08-26 — Address-search handoff into canonical road waypoints (verification complete, pending final review)

- Selecting an explicit Mapbox location result while **Route → Road** is active now appends that normalized coordinate to the ephemeral waypoint draft, updates the visible point count and cancels any superseded Directions request. Ordinary location search still recenters the map, and travel-time Area search keeps its existing center-selection behavior.
- Finishing two searched waypoints uses the established bounded Directions adapter and commits the same editable schema-19 `LineString` with compact provenance, one-step Undo/Redo, autosave/portable persistence and offline export parity. Search responses themselves are not persisted or replayed; canonical coordinates and provider route provenance remain the durable project data.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/app-route-authoring.test.tsx -t "adds selected search results as road-route waypoints before provider routing"` first failed because the App did not accept the injected search provider and no result option appeared; after the minimal App/workspace handoff it passed 1/1 and proved two selected results became the exact Directions waypoints. Review remediation then added `vitest run tests/unit/app/route-authoring-actions.test.ts -t "refuses a twenty-sixth searched road waypoint"`; it first failed because the bounded helper did not exist, then passed 1/1 after search and map waypoint drafts were capped at the provider's 25-point limit with actionable UI feedback.
- Focused verification: the exact creation, 25-point limit and searched-waypoint cancellation Vitest regressions each pass 1/1. An isolated staged-candidate worktree passes typecheck, ESLint with zero warnings and React Doctor with no issues; its one impacted Chromium road-routing flow passes 1/1 in 19.5 seconds and proves searched waypoint counts, one provider request, canonical editing, schema-19 download and credited offline layered-SVG export. No full unit suite, build, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Browser evidence: fresh exact 1440×900 Chromium screenshot `docs/screenshots/road-routing-search-20260826.png`, SHA-256 `4671db4d12b59278b10c3c503d8071da9568ff57b2ea184fe5a49af064baacdd`. It visibly shows `Vienna East, Austria` in the search field, the selected road route, editable handles and `Mapbox Directions · 9.2 km · 22 min · 2 waypoints`; visual review found no material clipping, overlap, horizontal overflow, blank map, product gradient, decorative shadow or hierarchy defect, and console/page-error capture was clean.
- The first independent fail-closed review rejected the unbounded searched-waypoint append and insufficient cancellation proof. The 25-point regression failed RED before bounded search/map draft handling and actionable feedback; focused cancellation coverage now proves a newly searched waypoint aborts and suppresses an in-flight Directions result. The one bounded independent recheck passed both original findings with no security concern, logic error or residual suggestion.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while wider parity work remains.

### 2026-08-26 — Searched addresses as canonical POIs (verified)

- Selecting an explicit Mapbox address result while **Pin** is active now creates one selected named POI at the normalized coordinate. The schema-20 layer stores only canonical point geometry, the bounded display label and compact `geocoding-v6` provenance (`providerFeatureId`); no token, request headers or raw response enters project state.
- Creation is one Undo/Redo transaction and the canonical layer flows through IndexedDB, portable JSON/ZIP, live editing and offline PNG/layered-SVG/PDF paths without replaying search. Existing project attribution adds `© Mapbox`. Manual coordinate edits intentionally remove geocoding provenance so a moved POI cannot retain a stale provider-feature identity.
- Search labels are stripped of control/formatting characters and bounded to the existing 40-character POI contract. Rejected results keep Pin authoring active with actionable guidance. Opening another project remounts the search boundary, aborts the old request and clears stale query/results before they can enter the replacement document. Search results consumed by Route, Pin or travel-time Area authoring no longer create a separate camera-history transaction.
- Strict RED/GREEN evidence: the named-POI interaction first found no layer; the compact-provenance store test first failed because `createSearchPoi` was absent; portable parsing first rejected `geocoding-v6`; stale project replacement first left the request signal live; provider labels over 40 characters were rejected instead of bounded; one Undo initially retained the POI because address selection also moved the camera; review remediation first exited Pin silently on rejected creation, accepted schema 19 as current and retained provenance after a coordinate edit. Every exact regression now passes.
- Focused verification on the staged candidate: all named RED/GREEN Vitest cases pass individually; `npm run typecheck`, ESLint with zero warnings and React Doctor pass; `git diff --cached --check` passes; and the single impacted Chromium test `searched address becomes one durable editable and exportable POI` passes 1/1 in 9.6 seconds, proving schema-20 download, one-step Undo/Redo, credited layered-SVG export, one provider request, zero overflow and clean console/page-error capture. No full unit suite, full Chromium gate, build, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Browser evidence: fresh exact 1440×900 `docs/screenshots/search-poi-20260826.png`, SHA-256 `fd71d56184925dbce513e010c3f66461c872ca1b0c1ad1557ca83202a6f83bad`. It visibly shows the searched Café Central POI selected in the layer list, on the live map and in editable Layer/Appearance/Location properties; visual review found no material clipping, overlap, blank map, product gradient, decorative shadow or hierarchy blocker.
- The initial fail-closed review rejected silent failed creation, unversioned provenance expansion and stale provenance after edits. Each blocker received an observed-RED regression and minimal remediation. The bounded independent recheck passed all three original findings with no security concern, logic error or suggestion.
- The next provider breadth slice is bounded batch address geocoding for POI spreadsheet rows. `docs/COMPLETE.md` remains absent while wider parity and the final mission gate remain open.

### 2026-08-26 — Mobile navigation and drawer touch targets (review remediation complete)

- A proposed sidebar-deletion slice was not retained. Independent fail-closed review found that it made Custom page sizing unreachable from a fresh project, removed the user-visible elevation/profile and Directions-provenance workflows, removed the only POI repositioning path, contradicted the required layer filter/direct lock and default Map-style disclosure, and weakened tests to bless those regressions. All proposed production/test removals and their screenshots were restored to `HEAD` before this bounded accessibility slice continued.
- Live Chromium measurement found the Layers/Properties launchers at only 36 px high and all six map-tool buttons at 32 × 32 px. The strengthened exact 390/320 px regression then failed RED at the original 36 px launcher height before any corrected CSS was applied.
- Mobile-only CSS now gives the two panel launchers and every map-tool action a 44 px minimum target, enlarges the palette shell to 52 px and lifts contextual authoring panels. While each modal drawer is open, every button is also asserted at 44 × 44 px or larger; layer rows use matching 44 px visibility, selection, lock and reorder columns, and the Properties close/action/disclosure controls inherit the same minimum target. The initial review caught collisions between the enlarged palette and scale/attribution/zoom controls, between the Properties close and Layer-menu targets, and between 44 px orientation buttons and their 32 px segmented container. Those controls now occupy disjoint regions, and the segmented container encloses its targets. Desktop dimensions are unchanged.
- The exact Chromium test `mobile navigation buttons use full touch targets` passes 1/1 at both 390 × 844 and 320 × 844 with one worker. It checks both closed-shell control groups, every visible button in Project and selected-Layer Properties states and the Layers dialog, pairwise separation from scale/attribution/zoom controls, close/menu separation, segmented-control containment, toolbar edge clearance, body overflow and clean console/page-error buffers. TypeScript passes; ESLint passes with zero warnings; React Doctor reports no issues. No full unit suite, full Chromium suite, build or audit was run under the focused iteration policy.
- Fresh 390 × 844 evidence: `docs/screenshots/mobile-nav-touch-targets.png`, SHA-256 `738f86eb02ecd13a7f2e9d929aa3357107fa02a8d9dc2f2fcf3c1aea5a8b197e`. It visibly shows the larger Layers/Properties launchers and six-button bottom palette with the attribution, scale and zoom controls clearly separated above it; visual review found no material clipping, overlap, horizontal overflow, gradient, decorative shadow or hierarchy defect.

### 2026-08-26 — Mobile Export icon spacing (verified)

- Live 390 px inspection found the dark Export action used only 8 px edge padding and a 6 px icon-label gap, making its download glyph look cramped. The exact Chromium regression first failed because the label had no measurable wrapper and the spacing contract was absent.
- Export now has a semantic label span and mobile-only 12 px left/right insets with an 8 px icon-label gap; desktop spacing is unchanged. The exact Chromium test `mobile Export action gives its icon balanced spacing` passes 1/1 with one worker in 4.6 seconds. Earlier navigation-fit coverage remains green at 320 px. No full unit suite, full Chromium suite, build, Doctor or audit was run under the focused iteration policy.
- Fresh 390 × 844 screenshot `docs/screenshots/mobile-export-spacing.png`, SHA-256 `d1f09fe8d7894f1467c1c026a786758c6a92c16cdb058fbda3cdf4a8bbc142b0`, visibly shows balanced Export icon/label spacing with no clipping, overlap, horizontal overflow, gradient or decorative shadow.
- Independent fail-closed review passed with no security concern or logic error. Its non-blocking suggestions are narrower/breakpoint coverage and a tolerance-based left/right balance assertion; the existing 320 px navigation-fit regression and the measured 12/8/12 px screenshot reduce those risks. The next mission slice returns to provider breadth with bounded batch address geocoding for POI rows.

### 2026-08-26 — Bounded spreadsheet address geocoding (verified)

- The existing **Paste POI list** workflow now offers Coordinates and Addresses modes. Address mode accepts at most 25 bounded `Name · Address` rows, resolves them sequentially through Mapbox Geocoding v6 with `autocomplete=false` and `limit=1`, and commits the complete resolved set as one canonical POI batch only after every lookup succeeds.
- Each POI retains the supplied project label, normalized point geometry and compact `geocoding-v6` provider feature ID. No token, request headers or raw provider payload is persisted. The batch is one Undo step, portable JSON preserves provenance, ordinary coordinate rows remain provider-independent, and existing live/autosave/ZIP/PNG/SVG/PDF paths consume canonical project state without replaying geocoding.
- Stale work is fail-closed: Cancel, unmount and document-epoch changes synchronously abort the active request, suppress remaining sequential lookups and cannot commit into a replacement project. Missing results keep the panel open with a row-specific alert and restore keyboard focus to the now-enabled textarea.
- Strict RED/GREEN evidence: the nominal exact test first failed because the Addresses mode did not exist; exact-address mode then failed until requests set `autocomplete:false`; the document-change regression first observed `AbortSignal.aborted === false`; and the error-focus regression first left focus on the submit button. Each exact named Vitest test passes after its minimal correction.
- Focused verification: `vitest run tests/unit/app/app-poi-address-batch.test.tsx -t "geocodes pasted address rows as one undoable POI batch"` passes 1/1; both exact lifecycle remediation tests pass 1/1; typecheck passes; ESLint passes with zero warnings; and the sole impacted Chromium test `pasted address rows become one durable geocoded POI batch` passes 1/1 with one worker in 7.5 seconds. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh 1440 × 900 Chromium evidence: `docs/screenshots/batch-address-geocoding-20260826.png`, SHA-256 `c049a3dd186e88a9cdc1ecccfc53684ba7206cf579461265e5b19cb4d0475d38`. It visibly shows both resolved Vienna POIs on the live map and in Layers, with Café Central selected and editable; visual review found no material clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- The initial fail-closed review rejected missing document-epoch cancellation and unreliable focus restoration after a lookup error. Both received exact RED regressions and minimal remediation; the bounded independent recheck passed both original findings with no security concern or logic error. Its non-blocking suggestion is a two-row stale-request fixture in addition to the current one-row no-follow-up proof.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while wider parity and the final mission gate remain open.

### 2026-08-26 — Chromium-only provider milestone and sequential-geocoding quality remediation

- Confirmed that travel-time Areas are already complete, independently reviewed and committed in `b6427b8`; canonical road routing, searched waypoints, searched POIs and bounded spreadsheet address geocoding are also present on clean main through `ebf58ae`.
- The requested Chromium-only milestone exposed one real quality blocker: React Doctor rejected `await` inside the bounded spreadsheet-geocoding loop. The implementation now uses bounded asynchronous recursion for at most 25 rows, retaining strictly sequential provider requests, cancellation checks and all-or-nothing commit semantics without lint or Doctor suppression.
- Focused behavioral protection remained green before and after the refactor: `vitest run tests/unit/app/app-poi-address-batch.test.tsx -t "geocodes pasted address rows as one undoable POI batch"` passes 1/1 with one fork and no file parallelism. React Doctor provided the observed RED gate before the minimal correction and now reports no issues.
- Final serial milestone verification exits 0: typecheck; ESLint with zero warnings; React Doctor with no issues; Vitest 115 files / 812 tests; production build with the known 2.35 MB pre-gzip main-chunk warning; `npm audit --audit-level=high` with 0 vulnerabilities; and Chromium 72/72 across three one-worker shards. Firefox and WebKit were not run, per the project-owner policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/batch-address-geocoding-milestone-20260826.png`, SHA-256 `274140f5e872a30200d47031d864f9fe21a5ff661545859e7f9b1ae65583cd23`. It shows the live map and bounded **Addresses** spreadsheet state with two Vienna rows, zero body overflow, no React Scan overlay and empty application console/page-error capture. Visual review found no material clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- `docs/FEATURE_PARITY.md` now reflects the delivered bounded batch-geocoding workflow and the clean 72-test Chromium milestone instead of listing the completed slice as absent. The fresh fail-closed review passed with no security concern or logic error; the milestone remediation and evidence are committed. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled.

### 2026-08-26 — Connected Vienna municipal-district merging (targeted verification complete, pending review)

- Re-audited clean main, live processes, historical worktrees and the persistent preview before editing. Travel-time Areas and the Chromium-only provider milestone were already complete, independently reviewed and committed; this slice resumed the next documented parity priority without changing provider or route behavior.
- Municipality authoring now presents all 23 attributed Vienna districts as accessible checkboxes. One selection retains the existing add behavior; two or more connected districts merge through a bounded polygon-clipping union into one canonical Polygon with aligned internal boundaries removed. Simplification-only sliver holes below a conservative absolute/relative area threshold are discarded while meaningful holes remain; mixed-level, duplicate and disconnected selections fail closed. The City of Vienna OGD source/license and existing print attribution remain unchanged.
- The merged district is one selected, auto-fitted, Undoable schema-20 shape and immediately uses the existing live editing, autosave, portable JSON/ZIP, native PNG, layered SVG and PDF paths. The impacted Chromium flow merged Innere Stadt + Josefstadt, inverted the shape, downloaded canonical JSON, verified one credited layered-SVG group and proved 390×844 containment with a clean console/page-error buffer.
- Strict RED/GREEN evidence: `vitest run tests/unit/administrative-areas.test.ts -t "merges adjacent Vienna municipal districts without an internal border"` first received `undefined`, then passed 1/1 after same-level municipality merging was enabled. `vitest run tests/unit/app/app-shape-authoring.test.tsx -t "merges selected Vienna districts as one fitted undoable shape"` first could not find the Innere Stadt checkbox, then passed 1/1 after the multi-select UI wiring. The retained single-district exact test also passes 1/1.
- Focused verification: typecheck passes; ESLint passes with zero warnings; and the sole impacted Chromium test `Vienna municipality selection and merging preserve source credit through project and print downloads` passes 1/1 in 14.2 seconds with one worker. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Browser evidence: fresh exact 1440×900 `docs/screenshots/vienna-district-merge-20260826.png`, SHA-256 `77b4830fabd1655a3877672af88cffc695123b649c4bb5c3f7d2f598bb1e5ea9`; fresh 390×844 `docs/screenshots/latest-mobile.png`, SHA-256 `16801fd6ae55b728e4b24321030b20a87c53c0e40fc8b5d5ebaf2c69af748ea7`. Visual review found the merged boundary, inspector identity and mobile checkbox picker readable with no material clipping, overlap, horizontal overflow, blank map, gradient or decorative shadow.
- The first independent fail-closed review rejected the exact-edge merger because representative connected district selections produced tiny artificial interior rings. The remediation regression first received two, three or more rings for Leopoldstadt + Brigittenau, Ottakring + Hernals and the complete 23-district selection, then passed 1/1 after a bounded `polygon-clipping` union and conservative simplification-sliver filtering. The bounded independent recheck passed the original topology and documentation blockers with no security concern or logic error and independently reran the exact regression (1 passed / 9 skipped) without modifying the tree. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while wider mission parity remains open.

### 2026-08-26 — Searchable Vienna district catalogue (targeted verification complete, pending review)

- Added a case-insensitive name filter to the 23-item Vienna district picker. Filtering only changes the visible catalogue: selected districts remain selected while hidden, and the live selected-count summary keeps the pending merge state auditable.
- The search field retains the dense 32 px desktop rhythm and expands to a 44 px mobile target. Existing source/license links, connected-selection validation, canonical merging, Undo, persistence and exports are unchanged.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/app-shape-authoring.test.tsx -t "filters Vienna districts by name without losing hidden selections"` first failed because the searchbox was absent, then passed 1/1 after the minimal filter/state implementation. The sole impacted Chromium flow `Vienna municipality selection and merging preserve source credit through project and print downloads` passes 1/1 with one worker and now proves desktop/mobile filtering, the 44 px mobile target, canonical merge/export behavior and clean console/page-error capture.
- Focused gates pass: typecheck; ESLint with zero warnings; React Doctor with no issues. No full unit suite, full Chromium suite, build, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh evidence: `docs/screenshots/vienna-district-filter-20260826.png` is 1440×900 with SHA-256 `ceccbbf9608a5b1cb6c367897682f9f1c2429b4c58247a65082d6aaf608b4adc`; `docs/screenshots/latest-mobile.png` is 390×844 with SHA-256 `46166a5ebbb00f2054f204c450abae3bca9d00ba663425df46ceac6839cb628c`. Visual review found the filtered result, search target, source credit and actions readable with no material clipping, overlap, horizontal overflow, blank map, gradient or decorative shadow.
- Corrected the parity caveat that still listed municipal merging as absent after connected Vienna merging shipped. Broad international municipality coverage and arbitrary non-aligned union remain explicit gaps.
- The first fail-closed review rejected locale-dependent `toLocaleLowerCase()` matching because a Turkish browser locale could turn `Innere` into a dotless-ı form. The exact regression `matches district names independently of browser locale casing` reproduced the missing Innere Stadt result under a simulated locale-sensitive casing boundary, then passed 1/1 after both operands switched to locale-independent `toLowerCase()`. The impacted Chromium flow, typecheck, zero-warning ESLint and React Doctor all pass again on the remediated tree. The bounded independent recheck passed the original blocker with no security concern, logic error or suggestion and did not modify the tree. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled.

### 2026-08-26 — Explicit travel-time provider limit (verified)

- Closed the remaining disclosure gap in the completed travel-time Area workflow: the authoring panel now states that Mapbox supports up to 60 minutes and that 61–180 minute areas are unavailable with this provider. The existing whole-number 5–60 validation and generation behavior are unchanged.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/isochrone-panel.test.tsx -t "explains the provider limit for longer travel-time areas"` first failed because the guidance was absent, then passed 1/1 after the minimal visible note was added.
- Focused gates pass: typecheck; ESLint with zero warnings; and the sole impacted Chromium flow `travel-time area becomes one durable editable and exportable project layer` passes 1/1 with one worker, covering search-selected center, cycling generation, canonical schema-20 persistence, Undo/Redo, autosave recovery, credited layered-SVG export, exactly one Isochrone request and clean console/page-error capture. The flow also performs its expected Geocoding v6 search request. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required iteration policy.
- Fresh exact 1440×900 evidence: `docs/screenshots/travel-time-provider-limit-20260826.png`, SHA-256 `7d5270504d1ea08b34457590f9b1032be5c9e5058b2c3d125da6dc1b13145b20`. It visibly shows the selected Vienna center, Cycling profile, 20-minute value and provider-limit note; visual review found the note legible with no material clipping, overlap, blank map, gradient, decorative shadow or hierarchy defect.
- `docs/FEATURE_PARITY.md` now gives travel-time Areas a dedicated provider-bounded row instead of relying on a priority note. The independent fail-closed review passed with no security concern or logic error and independently verified the screenshot hash/content; its only non-blocking suggestion is to associate the persistent limit note with the duration input for even stronger assistive-technology discovery. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while wider mission parity remains open.

### 2026-08-26 — Chromium-only travel-time milestone verification

- Re-audited clean main after the provider-limit commit, confirmed no duplicate Node/Vitest/Playwright worker was active, and preserved the sole persistent Vite preview on `127.0.0.1:4178`. The travel-time implementation and its focused review were already committed at `3250daf` before this milestone gate.
- The complete resource-bounded Chromium milestone exits 0 serially under the 768 MiB Node old-space cap: typecheck; ESLint with zero warnings; React Doctor with no issues; Vitest 115 files / 818 tests with one fork and no file parallelism; production build; `npm audit --audit-level=high` with 0 vulnerabilities; `git diff --check`; and Chromium 72/72 across three one-worker shards. Firefox and WebKit were not run, per the project-owner policy.
- The travel-time acceptance passed inside shard 1 and still proves search-selected center, cycling generation, exactly one Isochrone request, canonical schema-20 persistence, Undo/Redo, autosave recovery, credited offline layered-SVG export and clean application console/page-error capture. Its expected Geocoding v6 search is a separate provider request. The build retains the documented 2.38 MB pre-gzip main-chunk size warning; Vitest retains its jsdom canvas-not-implemented diagnostics while all 818 assertions pass.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/travel-time-provider-limit-20260826.png`, SHA-256 `b9ca85576224277e1e222ba8ef20e75fa18353780c0f1c3f40895bf24e4916d9`. Visual inspection confirms the Vienna center, Cycling profile, 20-minute value and explicit 60-minute provider boundary are visible with no material clipping, overlap, blank map, horizontal overflow, product gradient, decorative shadow or hierarchy blocker.
- `docs/COMPLETE.md` remains absent because broader mission parity gaps listed in `docs/FEATURE_PARITY.md` remain open. Scheduler job `5c693a328765` stays enabled.

### 2026-08-26 — Travel-time duration guidance accessibility association (targeted verification complete)

- The persistent Mapbox 60-minute provider-limit guidance is now programmatically associated with the **Travel time in minutes** input through `aria-describedby`. Valid values expose the provider boundary as the input's accessible description; invalid values retain both that boundary and the existing corrective error relationship.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/isochrone-panel.test.tsx -t "associates the provider limit with the travel-time input"` first failed with an empty accessible description, then passed 1/1 after the minimal ID/reference wiring.
- Focused verification exits 0: the exact named Vitest assertion; typecheck; ESLint with zero warnings; and the sole impacted Chromium flow `travel-time area becomes one durable editable and exportable project layer` with one worker. The browser flow still proves search-selected center, one Isochrone request, canonical persistence, Undo/Redo, autosave recovery, credited offline layered-SVG export and clean console/page-error capture. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required iteration policy.
- Fresh exact 1440×900 Chromium evidence was regenerated at `docs/screenshots/travel-time-provider-limit-20260826.png`, SHA-256 `7d5270504d1ea08b34457590f9b1032be5c9e5058b2c3d125da6dc1b13145b20`. Visual inspection confirms the Vienna center, Cycling profile, 20-minute value and provider boundary remain clear with no material clipping, overlap, blank map, horizontal overflow, product gradient, decorative shadow or hierarchy defect.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader mission parity remains open.

### 2026-08-26 — Compact route authoring and labeled mobile map palette (targeted verification complete)

- Resumed the in-progress mobile UI audit only after confirming travel-time Areas and their Chromium milestone were already complete and committed. The six bottom map actions now carry concise visible mobile labels, Route/Pin/Area and Fit-page command groups use consistent separators, and desktop icon-only presentation is unchanged.
- Route authoring now presents one compact Straight/Arc/Road control, one explicit optional travel-marker field and task-oriented status text instead of two dense icon-only matrices. Existing canonical Straight, two-anchor Arc and provider-backed Road behavior is unchanged.
- Strict focused evidence: the exact Chromium test `mobile map palette names tools and separates the view command` first received zero `.tool-label` elements. After the minimal palette/panel implementation it exposed a real screenshot defect: the route panel overlapped the map scale. A strengthened no-overlap assertion failed RED, then passed after the active mobile Route state moved scale, attribution and zoom controls above the compact panel. Review remediation observed two visible desktop separators instead of the original one; the same exact Chromium test passes 1/1 after making the Fit-page separator mobile-only and now also proves labels are hidden plus one separator remains on desktop. Mobile labels use an explicitly asserted readable 11 px minimum. The exact Vitest test `keeps the default route panel compact and self-explanatory` passes 1/1; typecheck passes; ESLint passes with zero warnings; `git diff --check` passes. No full unit suite, full Chromium suite, build, audit, Firefox or WebKit run was used as final acceptance under the owner-required focused iteration policy.
- Fresh 390×844 Chromium evidence: `docs/screenshots/mobile-route-palette-20260826.png`, SHA-256 `e0c2229c395214de05a66e14d41efda358cc4e95c0344fcb457bfcf952cd765e`. Visual inspection confirms legible route controls and tool labels, visible group separation, clear attribution/scale/zoom/palette spacing, and no material clipping, overlap, horizontal overflow, gradient or decorative shadow.
- The independent whole-frontend red-team remains fail-closed for final UX sign-off outside this slice. Residual medium-priority inconsistencies are the autosave-versus-`Save` download wording, raw route-vertex/elevation controls lacking Advanced disclosure, different Layers/Properties mobile close language, and the denser Area authoring panel. Export dialog hierarchy, focus containment/restoration, touch-target geometry and application console passed its checks. These residuals should be simplified without deleting capability or weakening canonical project/export behavior.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` remains paused until this interactive audit is reported, then returns to the broader mission.

### 2026-08-26 — Authoring-panel collision and shared-preview remediation

- The independent whole-frontend red-team found that mobile Pin and Area panels still collided with scale/MapLibre controls, native zoom targets measured 29 × 29 px, attribution measured 24 × 24 px, and the shared 4178 preview explicitly enabled React Scan so its overlay could intercept map-tool clicks.
- One generic `has-authoring-panel` state now moves scale, attribution and zoom above Route, Pin and Area authoring. Mobile zoom and attribution targets are 44 × 44 px without enlarging their glyphs, and the scale is offset left of zoom. The shared systemd preview now runs with `VITE_REACT_SCAN=false`; read-back shows the service active and the served page has no scan root.
- Observed RED/GREEN evidence: the exact Chromium regression first received a 29 px zoom target, then passes 1/1 while checking Pin and Area panel separation, zoom/attribution target size and scale/zoom separation. A fresh final rerun of that exact test passed 1/1 in 10.1 seconds. Existing exact Chromium checks for mobile navigation touch targets and accessible/non-overlapping attribution states also pass 1/1 individually. Typecheck, targeted zero-warning ESLint, React Doctor and `git diff --check` pass. A sibling reviewer accidentally launched Firefox without `--project=chromium`; that prohibited run was terminated and none of its output was used for acceptance.
- Fresh 390 × 844 evidence: `docs/screenshots/mobile-pin-controls-20260826.png`, SHA-256 `cc03b11116de306bc471950aa6179b7220ceed8c45fa29b38de906cd65ca1be5`; refreshed `docs/screenshots/mobile-area-controls-20260826.png`, SHA-256 `9aa3686a60622ffd0b448eae062160bee8e81e0e4463b043f420b20a53260e04`. The refreshed Area capture has 390 px body/viewport widths, no application console/page errors and no React Scan root. Visual review found no remaining must-fix control collision, clipping, overflow, gradient or decorative shadow.
- Final UX sign-off remains fail-closed for the larger semantic work: preserve the direct Open/Save/Import/Export contract rather than replacing it with the red-team's conflicting Project-menu proposal, resolve Select/Pan/Place/More semantics, move raw vertex/elevation controls behind Advanced disclosure and simplify the dense Area chooser without removing capability.

### 2026-08-26 — Consistent mobile drawer close affordances (targeted verification complete)

- Reconfirmed from repository evidence that travel-time Areas, their accessibility association, the Chromium-only 72/72 milestone, fail-closed review and verified commits were already complete before continuing the next bounded UX remediation.
- The mobile Layers drawer now uses the same close language and Lucide `X` affordance as Properties while retaining the desktop-only **Collapse layers** label and panel-collapse icon. Closing either modal drawer remains keyboard-focus-safe and restores its launcher.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/app-layer-actions.test.tsx -t "uses matching close language for both mobile sidebars"` first failed because **Close layers** did not exist, then passed 1/1 after the responsive label/icon correction. The sole impacted Chromium regression `mobile shell exposes accessible drawers and non-overlapping attribution states` passes 1/1 with one worker and verifies focus entry, reverse/forward Tab containment, Escape/backdrop closure, trigger restoration, responsive cleanup and attribution geometry.
- Fresh 390×844 Chromium evidence: `docs/screenshots/mobile-layer-close-20260826.png`, SHA-256 `b532810ef10767429efe4125664ec78951f9b24508e4076c311547552405db33`. Live capture reports 390 px body/viewport widths, focus on **Close layers**, no React Scan root and empty console/page-error buffers. Visual inspection found the open Layers drawer, top-right X, rows and footer readable with no material clipping, overlap, horizontal overflow, blank map, product gradient, decorative shadow or hierarchy defect.
- No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader mission parity remains open.

### 2026-08-26 — Progressive disclosure for route geometry and elevation (targeted verification complete)

- Selected `LineString` routes now keep raw vertex coordinates and the on-demand elevation workflow behind one collapsed **Advanced** inspector section. The collapsed summary reads `Vertices · Elevation profile`; Appearance and provider provenance remain immediately visible, while expanding Advanced restores the complete existing edit/export workflow. Disclosure state is a local UI preference and does not enter canonical project state.
- Strict RED/GREEN evidence: `vitest run tests/unit/layer-properties.test.tsx -t "keeps route vertices and elevation behind a collapsed Advanced section"` first failed because no Advanced control existed, then passed 1/1 after the minimal accordion wiring. Directly impacted unit and Chromium fixtures now open the disclosure before exercising route vertices or elevation.
- Focused verification exits 0: the exact named route-disclosure Vitest assertion; the exact evidence-discipline regression; typecheck; ESLint with zero warnings; and the sole impacted Chromium flow `project inspector progressively discloses advanced controls on desktop and mobile` with one worker. The browser case proves collapsed-only summary semantics and control recovery on desktop while retaining the existing desktop/mobile inspector-containment checks and clean console/page-error capture. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required iteration policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/route-advanced-disclosure-20260826.png`, SHA-256 `90fd171b924c88d639b21688b2a80b33110f70ecab37b3271fb425435266b992`. Visual inspection confirms the selected route, visible Appearance controls and collapsed Advanced summary are readable with no material clipping, overlap, blank map, horizontal overflow, product gradient, decorative shadow or hierarchy defect.
- The first independent fail-closed review rejected an evidence race: the fixed documentation screenshot path was writable by every configured browser project. The exact source-discipline regression first failed against the unconditional screenshot call, then passed after only Chromium was allowed to write the evidence path. A bounded recheck found the first regression proved only that one guarded writer existed; the strengthened invariant counts every fixed-path writer and requires the sole writer to be Chromium-guarded. A temporary unguarded mutation produced the expected RED count of two writers before removal restored GREEN. The final bounded recheck passed with no security concern, logic error or suggestion. The screenshot itself and production disclosure behavior were unchanged.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader parity and the final mission gate remain open.

### 2026-08-26 — Compact Area source chooser (targeted verification complete)

- Continued only after repository evidence confirmed that travel-time Areas, their Chromium-only milestone, fail-closed review and verified commits were already complete. The remaining mobile Area panel density defect was the three source tabs wrapping across up to three lines.
- The source chooser now shows concise single-line **Boundaries**, **Draw** and **Travel time** labels while preserving the fuller accessible task names **Find administrative area** and **Draw custom area**. Existing administrative, custom drawing and provider-backed travel-time behavior is unchanged.
- Strict RED/GREEN evidence: `vitest run tests/unit/app/app-shape-authoring.test.tsx -t "keeps Area source choices concise without weakening their accessible names"` first received `Find administrative area` instead of `Boundaries`, then passed 1/1 after the minimal label and no-wrap implementation.
- Focused verification exits 0: the exact named Vitest assertion; typecheck; ESLint with zero warnings; `git diff --check`; and the sole impacted Chromium regression `mobile authoring panels and native map controls stay usable and disjoint` with one worker. The browser check proves all three tabs remain at most 44 px tall and retain their accessible names while the Area panel, scale, attribution and zoom controls remain disjoint. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh 390×844 Chromium evidence: `docs/screenshots/mobile-area-source-tabs-20260826.png`, SHA-256 `02545be938d364f922c962754dde3392711919851088ca3ce50be125d98925cf`. Visual inspection confirms all source labels remain on one line with no material clipping, overlap, horizontal overflow, product gradient, decorative shadow or hierarchy defect.

### 2026-08-26 — Optional elevation-profile curve stroke (verified)

- Closed one explicit elevation-profile parity gap: the ready profile settings now expose a shared accessible **Curve stroke** checkbox. Turning it off preserves the filled terrain silhouette, markers, grids, measurements and attribution while disabling the irrelevant curve-color control.
- SVG, PNG and PDF all consume the same `showCurve` render option. SVG omits the curve path, PDF omits the curve paint commands, and PNG rasterizes the matching SVG; defaults remain unchanged with the stroke visible.
- Strict RED/GREEN evidence: `vitest run tests/unit/elevation-profile-export.test.ts -t "omits the curve stroke consistently across SVG, PNG, and PDF exports"` first found the existing SVG stroke, then passed 1/1 after the minimal shared export option. `vitest run tests/unit/elevation-profile-panel.test.tsx -t "lets the user hide the profile curve without hiding the fill"` first could not find **Curve stroke**, then passed 1/1 after the preview/control wiring.
- Focused verification exits 0: both exact named Vitest assertions; typecheck; ESLint with zero warnings; and the sole impacted Chromium flow `a selected route generates an attributed elevation profile with SVG, PNG, and PDF downloads` with one worker. The browser flow proves the filled preview remains visible with no line, the curve color is disabled, SVG/PDF omit the custom stroke, PNG remains structurally valid, 390×844 containment remains intact and console/page-error capture is clean. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/elevation-profile-curve-toggle-20260826.png`, SHA-256 `5dfc4341b1814c57367d0676a21d375970f66ea65444cb9c2c2edfd81b5d8bc4`. Visual inspection confirms the unchecked control, disabled curve color and filled gradient profile are legible with no material clipping, overlap, blank map, product gradient, decorative shadow or hierarchy defect.
- The first fail-closed review rejected insufficient PNG-toggle proof and the missing explicit unchanged-default assertion. Focused remediation now verifies that the PNG rasterizer receives a curve-free SVG and that default SVG/PDF outputs still contain their curve paint commands. Both exact export tests pass 1/1, and the bounded independent recheck passed the two original blockers with no security concern, logic error or suggestion.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while direct profile-only file upload and broader parity remain open.

### 2026-08-26 — Transparent route travel-time estimates (verified)

- The ready elevation-profile inspector now shows walking and cycling duration estimates from canonical route distance using explicitly stated 5 km/h and 15 km/h assumptions. The UI labels these as distance-only estimates and states that terrain, stops and conditions are excluded rather than presenting them as navigation predictions.
- Strict RED/GREEN evidence: `vitest run tests/unit/elevation-profile-panel.test.tsx -t "shows transparent walking and cycling time estimates for the route distance"` first failed because the accessible **Travel time estimates** group was absent, then passed 1/1 after the minimal formatter and inspector rows.
- Focused verification exits 0: the exact named Vitest assertion with one configured fork/no file parallelism; typecheck; targeted ESLint with zero warnings; and the sole impacted Chromium flow `a selected route generates an attributed elevation profile with SVG, PNG, and PDF downloads` with one worker. The browser case proves the estimates beside the existing attributed chart/export workflow and retains clean console/page-error capture. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/elevation-profile-travel-estimates-20260826.png`, SHA-256 `d0e3928579488cec8ffe3b9a521f4ab9e529269f32c32596c9bbfca676cfe77c`. Visual inspection confirms both estimate rows, assumptions note, elevation chart, map and inspector hierarchy are readable and contained with no material clipping, overlap, blank map, horizontal overflow, product-chrome gradient or decorative shadow.
- The initial fail-closed review rejected positive sub-30-second routes displaying `0 min`. The exact regression `never reports zero minutes for a positive route distance` first received `0 min`, then passed 1/1 after the formatter began returning `<1 min` for positive sub-minute values. Targeted ESLint and typecheck pass on the remediation; the bounded independent recheck passed the original blocker with no security concern, logic error or suggestion. Existing 390 px Chromium containment coverage includes the estimate rows and reports zero body overflow.
- `docs/FEATURE_PARITY.md` now records the bounded estimates and leaves direct profile-only file upload as the remaining explicit elevation-profile breadth gap. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader mission parity remains open.

### 2026-08-26 — Ephemeral profile-only route files (verified)

- The selected-route elevation workflow can now switch its transient profile source to one local GPX, KML or GeoJSON route. The existing bounded import parsers validate the file; exactly one route is required, non-route features are ignored, and **Use selected map route** restores the canonical layer source.
- The uploaded route name and filename remain visible and auditable, but no uploaded profile-only geometry enters the layer list, project document, history, autosave or exports. Open-Meteo still receives at most 100 sampled coordinates only after explicit **Generate elevation profile** activation; changing source aborts any pending terrain request and clears its transient result.
- Strict RED/GREEN evidence: `vitest run tests/unit/elevation-profile-panel.test.tsx -t "switches the profile source between the selected route and one local route file"` first failed because **Profile route file** was absent, then passes 1/1 with one fork and no file parallelism after the minimal source chooser and reset path.
- Focused verification exits 0: the exact named Vitest assertion; typecheck; targeted ESLint with zero warnings; `git diff --check`; and the sole impacted Chromium flow `a selected route generates an attributed elevation profile with SVG, PNG, and PDF downloads` with one worker. The browser case uploads a GPX transiently, proves the layer list remains unchanged, samples the uploaded coordinates, exercises the established SVG/PNG/PDF path, restores selected-layer behavior after remount, and retains clean console/page-error capture. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/elevation-profile-file-source-20260826.png`, SHA-256 `ba5ba2287abf310271b34735f0b4b769b248481b331957b24e2602f35f7517d1`. Visual inspection confirms **Danube profile route · danube-profile.gpx**, the unchanged selected Route 01 layer, attributed chart and inspector hierarchy are readable and contained with no material clipping, overlap, horizontal overflow, blank map, product-chrome gradient or decorative shadow.
- The independent fail-closed review passed with no security concern or logic error. Its non-blocking suggestions are exact browser-query coordinate assertions, additional malformed/oversized/stale-parse fixtures, a live file-reading announcement and a narrower browser-test title; none blocks this bounded source-switch workflow.
- `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader mission parity remains open.

### 2026-08-26 — Embedded 300-DPI PNG physical resolution (verified)

- Closed the explicit PNG metadata gap without changing pixel geometry or the one-file large-raster contract. Standard canvas exports now replace any stale density chunk with one CRC-valid `pHYs` chunk immediately after `IHDR`; streamed large PNGs write the same 11,811-pixels-per-metre metadata before their `IDAT` stream. Both paths therefore advertise 300 DPI while preserving the established native-detail render and bounded-region composition.
- Strict RED/GREEN evidence: `vitest run tests/unit/png-physical-resolution.test.ts -t "embeds a valid 300-DPI pHYs chunk immediately after IHDR"` first failed because the metadata module was absent, then passed 1/1. The standard-path regression first received no `pHYs` density, the streamed-path regression first received no density, and the export disclosure regression first displayed the obsolete “not embedded” claim; each exact named test passes 1/1 after its minimal correction.
- Focused verification exits 0: the exact standard, streamed and disclosure Vitest assertions; typecheck; targeted ESLint with zero warnings; `git diff --check`; and the sole impacted Chromium flow `export downloads the current print frame as PNG on desktop and mobile` with one worker. The browser test downloads a real 3508×2480 PNG, validates its 11,811-pixels-per-metre `pHYs` value, decodes nonblank/opaque/attribution pixels and retains the existing desktop/mobile/custom-aspect checks. No full unit suite, full Chromium suite, build, Doctor, audit, Firefox or WebKit run was used under the owner-required focused iteration policy.
- Fresh exact 1440×900 Chromium evidence: `docs/screenshots/png-300dpi-metadata-20260826.png`, SHA-256 `28663428792f8cfb32019cd2526e2de9a89dcc0267f18abefa2d97e31e150bbf`. It visibly shows the expanded PNG Technical details statement, exact 3508×2480 output and 300-DPI target; visual review found no material clipping, overlap, horizontal overflow, hierarchy defect, product gradient or decorative shadow.
- The initial fail-closed review rejected permissive source-PNG parsing and incomplete proof for checksum, ordering and replacement behavior. The exact malformed-input regression first accepted a corrupt source CRC, then passed after source chunks began validating CRCs, the 13-byte first `IHDR`, terminal zero-length `IEND`, required/consecutive `IDAT`, palette/metadata ordering and unknown critical chunks. The final bounded recheck passed after focused fixtures also proved non-terminal IEND, short IHDR, PLTE-after-IDAT, split IDAT, 150-to-300-DPI replacement, byte-idempotence, X/Y density, metre units, unique ordering and CRC validity; no security concern, logic error or suggestion remains.
- `docs/FEATURE_PARITY.md` now records the embedded metadata and removes the superseded caveat. `docs/COMPLETE.md` remains absent and scheduler job `5c693a328765` stays enabled while broader mission parity remains open.
